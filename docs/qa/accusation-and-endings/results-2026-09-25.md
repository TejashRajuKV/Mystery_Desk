# Results — accusation-and-endings (2026-09-25 re-run)
Run at: 2026-09-25T00:00:00Z (in progress)
Backend reachable: yes (isolated instance, `http://localhost:4404/api`, own `DB_PATH` under the session scratchpad, port 4404 only)

Runner notes:
- Own backend started per the task's overrides: `PORT=4404 DB_PATH=<scratchpad>/qa2/accusation-and-endings.sqlite`, fresh sqlite file (old one deleted first, no `-wal`/`-shm` present). Never touched :4000/:5173.
- No browser automation tool (`mcp__plugin_playwright_playwright__*` or `mcp__Claude_Browser__*`) is present in this session's tool list, so no frontend was started and every UI-marked case (TC-85 to TC-91) is BLOCKED per the runner's own rule, not guessed from source.
- Suspect/evidence/place ids discovered at run time from `GET /suspects`, `GET /evidence`, `GET /places`, `GET /file`, `GET /timeline`, and `data/cases/047/evidence.json` (id list only). `data/cases/<C>/endings.json` read for TC-48's cross-check (copy file, not `solution.json`). `solution.json` was never opened.
- Per "at most one non-null accusation per case per database state", each accusation-consuming case reset first; arbitrary accused suspect was always the first id from `GET /suspects` (S01), never chosen by elimination or by the resulting ending.
- This file supersedes nothing on disk; it is a fresh, independent run written progressively. `results.md` and `results-2026-09-24.md` were not modified.

---

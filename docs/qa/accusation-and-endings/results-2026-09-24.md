# Results — accusation-and-endings
Run at: 2026-09-24T16:20:00Z to 2026-09-24T20:53:00Z
Backend reachable: yes (isolated instance, `http://localhost:4209/api`, own `DB_PATH`, port 4209 only)

Runner notes:
- All calls ran against a private backend on port 4209 with its own SQLite file, started and stopped only by this run. The owner's real save on :4000/:5173 was never touched.
- No browser automation tool (`mcp__plugin_playwright_playwright__*` or `mcp__Claude_Browser__*`) was available in this session, so no frontend was started and every UI-marked case (TC-85 to TC-91) is BLOCKED per the runner's own rule, not guessed from source.
- Suspect ids/evidence ids were discovered at run time from `GET /suspects`, `GET /evidence`, `GET /places`, `GET /file`, `GET /timeline`, and `data/cases/047/evidence.json` (id list only, no facts/solution). `data/cases/<C>/endings.json` was read for TC-48's byte-for-byte cross-check (culprit-free copy file, not `solution.json`).
- Per the "at most one non-null accusation per case per database state" rule, each accusation-consuming case reset the case first; the arbitrary accused suspect was always the first id returned by `GET /suspects` (S01), never chosen by elimination or by which ending resulted.
- Spec-ambiguity note (Runner rules / TC-25 / TC-28): every thin accusation made in this run (TC-25, TC-27, TC-28 x5, TC-29, TC-30, TC-33, TC-49, TC-50, TC-57, TC-68) resolved to `ending.id: "innocent_accused"`, never `criminal_escapes`. This is consistent throughout the run but is recorded, not acted on, per the runner rules; it does not affect any PASS/FAIL verdict since HTTP status, response keys and message text were the only things checked against spec point 7.
- No case in the original 68 contradicted current source behaviour (the file was already cross-checked against post-`b45dd98` source by its author) — every original case is a real PASS against a live call, not a copy of an old expectation.

---

## TC-01 — Save theory returns whole investigation state: PASS
- Command / action run: `PUT /cases/047/theory {"text":"The night guard let someone in."}` after `POST /reset`.
- Observed: 200, body has all listed keys, `theory` exactly matches, `conclusion` null, not wrapped.
- Verdict reason: matches expected shape and value exactly.

## TC-02 — Theory persists across requests (refresh): PASS
- Command / action run: `GET /investigation`, then backend stopped (`Stop-Process`) and restarted with the same `DB_PATH`, `GET /investigation` again.
- Observed: `theory` = `"The night guard let someone in."` both before and after restart (new PID 9168 after restart, same DB file).
- Verdict reason: persistence confirmed across a real process restart.

## TC-03 — Non-string `text` is a 400: PASS
- Command / action run: `PUT /theory` with `{"text":123}`, `null`, `true`, `{"a":1}`, `["x"]}`, after seeding theory `"keep me"`.
- Observed: all five 400 with `{"error":"Expected { text } of at most 5000 characters"}`; final `GET /investigation` theory still `"keep me"`.
- Verdict reason: matches expected exactly.

## TC-04 — Missing `text` key or empty body is a 400: PASS
- Command / action run: `PUT /theory` with `{}`, `{"txt":"x"}`, and no body/no Content-Type.
- Observed: all three 400 with an `error` string; theory remained `""`.
- Verdict reason: matches expected.

## TC-05 — Theory length boundary: PASS
- Command / action run: `PUT /theory` with 5000 `a`s, then 5001 `a`s.
- Observed: (a) 200, `theory.length` 5000. (b) 400 `{"error":"Expected { text } of at most 5000 characters"}`; theory length still 5000 after.
- Verdict reason: boundary is exactly 5000 as documented in the case.

## TC-06 — Empty string valid theory, later save overwrites: PASS
- Command / action run: `PUT /theory` `"first"`, `""`, `"second"` in sequence.
- Observed: all 200; sequence `"first"`, `""`, `"second"`; final `GET /investigation` shows `"second"` only.
- Verdict reason: no concatenation, overwrite confirmed.

## TC-07 — `suspectId` string with `evidenceIds` missing is a 400: PASS
- Command / action run: `POST /conclusion {"suspectId":"S01"}`.
- Observed: 400 `{"error":"Expected { suspectId, evidenceIds: [...] } with at least one exhibit, or { suspectId: null }"}`.
- Verdict reason: exact match.

## TC-08 — `suspectId` string with empty `evidenceIds` is a 400: PASS
- Command / action run: `POST /conclusion {"suspectId":"S01","evidenceIds":[]}`.
- Observed: 400, same error string as TC-07.
- Verdict reason: exact match.

## TC-09 — `suspectId` missing or wrong type is a 400: PASS
- Command / action run: `{}`, `{"evidenceIds":["E001"]}`, `{"suspectId":5,...}`, `{"suspectId":true,...}`.
- Observed: all four 400 with the TC-07 error string.
- Verdict reason: exact match.

## TC-10 — `evidenceIds` of the wrong type is a 400: PASS
- Command / action run: string, array of numbers, object, and `suspectId:null` with a string `evidenceIds`.
- Observed: all four 400 with the TC-07 error string.
- Verdict reason: exact match, including the `null` + non-array case.

## TC-11 — No body and syntactically invalid JSON: PASS
- Command / action run: (a) no body. (b) `Content-Type: application/json` with truncated `{"suspectId":`.
- Observed: (a) 400 TC-07 string. (b) 400 `{"error":"Unexpected end of JSON input"}`; neither is 500; `conclusion` stayed `null`.
- Verdict reason: matches "must not be 500" requirement.

## TC-12 — Shape check before reference checks: PASS
- Command / action run: `POST /conclusion {"suspectId":"S99"}` (unknown suspect, no `evidenceIds`).
- Observed: 400 with the TC-07 string, not 422.
- Verdict reason: shape check runs first as required.

## TC-13 — Rejected requests leave the case open: PASS
- Command / action run: Fresh reset, ran TC-07/08/09a/12 malformed calls, then R1 + TC-18/20/22 rejected calls, then `GET /investigation`, `GET /cases`, `GET /report`.
- Observed: `conclusion` null; `/cases` entry `status:"in_progress"` (travel/search happened during setup), `ending` null; report 422 `{"error":"No conclusion has been accepted yet."}`.
- Verdict reason: no rejected call left any trace; status correctly reflects the legwork done, not "new", as the case anticipates.

## TC-14 — `{ suspectId: null }` accepted with `criminal_escapes` (all five cases): PASS
- Command / action run: fresh reset + `POST /conclusion {"suspectId":null}` on 047, 048, 049, 050, 051 in turn, reset after each.
- Observed: all five 200 with body exactly `{"suspectId":null,"evidenceIds":[],"ending":"criminal_escapes"}`.
- Verdict reason: matches exactly, all five cases.

## TC-15 — `{ suspectId: null, evidenceIds: [] }` explicit form: PASS
- Command / action run: fresh reset, `POST /conclusion {"suspectId":null,"evidenceIds":[]}`.
- Observed: 200, same body as TC-14.
- Verdict reason: exact match.

## TC-16 — Cannot determine after real work is still `criminal_escapes`: PASS
- Command / action run: R1 unlocked E015/E017 via L01; saved theory; created connection C01 (E015→S01); `POST /conclusion {"suspectId":null}`.
- Observed: 200 `ending:"criminal_escapes"`; `GET /investigation` shows the connection intact and theory unchanged (`"TC-16 theory"`).
- Verdict reason: work done did not change the ending; state preserved as expected.

## TC-17 — Cannot determine with a cited exhibit: PASS
- Command / action run: R1 unlocked E015, `POST /conclusion {"suspectId":null,"evidenceIds":["E015"]}`.
- Observed: 200, `ending:"criminal_escapes"`, `suspectId:null`, `evidenceIds:["E015"]` (not dropped).
- Verdict reason: cited exhibit is kept even for a null accusation — recorded as the source's actual behaviour, matches this run.

## TC-18 — Unknown suspect id is a 422: PASS
- Command / action run: R1 unlocked E015; `POST /conclusion {"suspectId":"S99","evidenceIds":["E015"]}`.
- Observed: 422 `{"error":"That suspect isn't in this case."}`; conclusion stayed null.
- Verdict reason: exact match.

## TC-19 — Empty-string suspect id is a 422: PASS
- Command / action run: `POST /conclusion {"suspectId":"","evidenceIds":["E015"]}`.
- Observed: 422, same error string as TC-18.
- Verdict reason: exact match.

## TC-20 — Non-existent exhibit id is a 422: PASS
- Command / action run: fresh reset, `POST /conclusion {"suspectId":"S01","evidenceIds":["E999"]}`.
- Observed: 422 `{"error":"One of the cited exhibits isn't in this case."}`; conclusion stayed null.
- Verdict reason: exact match.

## TC-21 — Locked exhibit id is a 422: PASS
- Command / action run: fresh reset (nothing unlocked, `GET /evidence` = `[]`); `POST /conclusion {"suspectId":"S01","evidenceIds":["E001"]}` (E001 is a real 047 evidence id, not yet unlocked).
- Observed: 422, same error string as TC-20; conclusion stayed null; `GET /evidence/E001` was 404.
- Verdict reason: locked exhibit rejected, confirmed locked via the 404.

## TC-22 — One good plus one locked exhibit rejects the whole accusation: PASS
- Command / action run: R1 unlocked E015; `POST /conclusion {"suspectId":"S01","evidenceIds":["E015","E001"]}` (E001 locked).
- Observed: 422, same error string; conclusion stayed null. A following valid accusation (`["E015"]` only) was then accepted 200 (TC-25 form), confirming the earlier rejection consumed nothing.
- Verdict reason: whole accusation rejected on one bad id, later valid accusation still works.

## TC-23 — Locked and non-existent exhibits are indistinguishable: PASS
- Command / action run: Call A `{"suspectId":"S01","evidenceIds":["E001"]}` (locked); Call B `{"suspectId":"S01","evidenceIds":["E999"]}` (non-existent).
- Observed: both 422, byte-identical body `{"error":"One of the cited exhibits isn't in this case."}`.
- Verdict reason: no distinguishing text, matches security requirement.

## TC-24 — No response body leaks the answer key: PASS
- Command / action run: Recursive key scan (Python) of `GET /cases/047`, `GET /cases`, `GET /investigation`, plus the exact error strings collected above.
- Observed: zero occurrences of `culprit`, `requiredEvidence`, `requiredConnections`, `solution`, `answer`, `missing` in any of the three bodies; `GET /cases/047` has a `filePages` count (`4`), not page bodies, and no `key` field; error strings matched exactly the three quoted in the spec.
- Verdict reason: no leak found.

## TC-25 — A thin accusation is accepted with an ending: PASS
- Command / action run: R1 unlocked E015 (personIds `["S02"]`, does not include S01); `POST /conclusion {"suspectId":"S01","evidenceIds":["E015"]}`.
- Observed: 200 `{"suspectId":"S01","evidenceIds":["E015"],"ending":"innocent_accused"}`.
- Verdict reason: 200 with a valid ending id from the two allowed for a thin accusation.

## TC-26 — Accepted response has exactly `suspectId`, `evidenceIds`, `ending`: PASS
- Command / action run: inspected TC-25's JSON body keys.
- Observed: exactly `{suspectId, evidenceIds, ending}`, correct types, no extra fields.
- Verdict reason: exact match.

## TC-27 — Duplicate cited ids are collapsed: PASS
- Command / action run: R1 unlocked E015 and E017; `POST /conclusion {"suspectId":"S01","evidenceIds":["E015","E015","E017","E015"]}`.
- Observed: 200, `evidenceIds:["E015","E017"]` (first-seen order, no repeats); `GET /investigation` confirms the same array.
- Verdict reason: exact match.

## TC-28 — Status and shape do not depend on who is accused (all five cases): PASS
- Command / action run: fresh reset per case, R1 to unlock one exhibit per case (`047:E015 048:E003 049:E005 050:E001 051:E001`), then `POST /conclusion {"suspectId":"S01","evidenceIds":[<E>]}` once per case.
- Observed: all five 200 with the same 3-key shape; all five endings were `innocent_accused` (recorded per the spec-ambiguity note, not acted on).
- Verdict reason: identical shape across all five cases, no field reveals correctness.

## TC-29 — Several exhibits cited: all returned in the order sent: PASS
- Command / action run: R1 unlocked E015+E017; `POST /conclusion {"suspectId":"S01","evidenceIds":["E017","E015"]}`.
- Observed: 200, `evidenceIds:["E017","E015"]`.
- Verdict reason: order preserved exactly as sent.

## TC-30 — `innocent_accused` asserted specifically: PASS
- Command / action run: `GET /report` on the closed state from TC-29 (ending was `innocent_accused`).
- Observed: `ending.id:"innocent_accused"`, `title:"Innocent Person Accused"`, `stamp:"WRONGFUL ARREST"`, `whatHappened:null`, no `{accused}` substring anywhere, and `"Dr. Maren Voss"` (S01's name) present in the first narrative line.
- Verdict reason: matches every documented field for this ending exactly.

## TC-31 — Second `POST /conclusion` is a 422 (null then null): PASS
- Command / action run: fresh reset, accept `{"suspectId":null}` (200), repeat the same call.
- Observed: second call 422 `{"error":"This case is closed. The accusation on file is final."}`.
- Verdict reason: exact match.

## TC-32 — Second accusation naming a suspect is a 422, first stands: PASS
- Command / action run: R1 unlocked E015 before closing; close with `{"suspectId":null}` (200); then `POST /conclusion {"suspectId":"S01","evidenceIds":["E015"]}`.
- Observed: 422, closed-case error string; `GET /investigation.conclusion` still exactly `{"suspectId":null,"evidenceIds":[],"ending":"criminal_escapes"}`.
- Verdict reason: first accusation preserved untouched.

## TC-33 — Second accusation after a named accusation is a 422: PASS
- Command / action run: TC-25 form accepted (`S01`/`E015`); then `{"suspectId":null}`; then repeat of the identical named accusation.
- Observed: both later calls 422 with the closed-case string; `GET /investigation.conclusion` unchanged from the first accepted response.
- Verdict reason: exact match.

## TC-34 — Malformed second body is a 400, not a 422: PASS
- Command / action run: on a closed case, `POST /conclusion {}`.
- Observed: 400 with the TC-07 error string; conclusion unchanged.
- Verdict reason: shape check runs before the closed check, as documented.

## TC-35 — Two simultaneous submissions: exactly one wins: PASS
- Command / action run: two parallel `POST /conclusion {"suspectId":null}` calls fired via background curl processes.
- Observed: one 200 (`ending:"criminal_escapes"`), one 422 with the closed-case string; `GET /investigation` shows a single conclusion.
- Verdict reason: race handled correctly, no double-write.

## TC-36 — `investigation.conclusion` holds the accepted one: PASS
- Command / action run: TC-25 form accepted, three sequential `GET /investigation` calls.
- Observed: all three return the identical conclusion object, matching the POST response.
- Verdict reason: stable across repeated GETs.

## TC-37 — `GET /cases` shows the closed status and ending: PASS
- Command / action run: fresh reset, confirm 047 entry is `status:"new"`/`ending:null` before closing, then `{"suspectId":null}` accepted, `GET /cases`.
- Observed: 047 entry `status:"closed"`, `ending` exactly `{"id":"criminal_escapes","title":"Criminal Escapes","stamp":"UNSOLVED"}` (3 keys); response is a bare array.
- Verdict reason: exact match.

## TC-38 — Closing one case does not touch the others: PASS
- Command / action run: reset all five, close 047 with `{"suspectId":null}`; check `/cases`, `GET /048/report`, then `POST /048/conclusion {"suspectId":null}`, then `/cases` again.
- Observed: 048–051 remained `"new"`/`ending:null` after 047 closed; 048 report was 422; 048's own accusation succeeded 200; after that both 047 and 048 show `"closed"`.
- Verdict reason: per-case isolation confirmed.

## TC-39 — Interview choices are 422 after closing: PASS
- Command / action run: on closed 047, `POST /dialogue/S01/choice` with `{"choiceId":"anything"}`, `{"presentEvidenceId":"E999"}`, `{}`.
- Observed: (a)/(b) 422 `{"error":"This case is closed. The interviews are over."}`. (c) 400 `{"error":"Expected { choiceId } or { presentEvidenceId }"}`.
- Verdict reason: matches documented ordering (malformed body 400 even before the closed check).

## TC-40 — Travel to another place is a 422 after closing: PASS
- Command / action run: on closed 047, `POST /travel {"locationId":"L01"}`, then `{"locationId":"L99"}`.
- Observed: (a) 422 `{"error":"This case is closed."}`, `locationId`/`minutesUsed` unchanged (`null`/`0`). (b) 404 `{"error":"There is no such place in this case"}`.
- Verdict reason: exact match, unknown-place check happens as documented.

## TC-41 — Searching an unsearched spot is a 422 after closing: PASS
- Command / action run: fresh reset, travel to L01, note unsearched spot `L01-locker`, close with `{"suspectId":null}`, then `POST /places/L01/search {"spotId":"L01-locker"}`.
- Observed: 422 `{"error":"This case is closed."}`; `spotsSearched` still `[]`; `minutesUsed` stayed at 30 (the travel only).
- Verdict reason: exact match.

## TC-42 — Reading an unread file page is a 422 after closing: PASS
- Command / action run: fresh reset, close with `{"suspectId":null}`, `POST /file/F1/read`, then `POST /file/F99/read`.
- Observed: F1 422 `{"error":"This case is closed."}`, `pagesRead` stayed `[]`; F99 404 `{"error":"There is no such page in the file"}`.
- Verdict reason: exact match.

## TC-43 — Repeating a free, already-done action after closing: PASS
- Command / action run: fresh reset, travel L01 + search L01-locker + read F1 (minutesUsed 65), close with `{"suspectId":null}`, repeat the same three calls.
- Observed: all three repeats returned 422 `{"error":"This case is closed."}`; `minutesUsed` unchanged (65 before and after).
- Verdict reason: observed behaviour is 422 on all three, which the case itself states "matches the spec"; state was unaffected either way, so this is a clean PASS against the primary spec expectation.

## TC-44 — Read-only endpoints still work after closing: PASS
- Command / action run: on closed 047, `GET /investigation`, `/evidence`, `/suspects`, `/timeline`, `/report`, and `GET /cases`.
- Observed: all six 200.
- Verdict reason: exact match.

## TC-45 — Report before a conclusion is a 422: PASS
- Command / action run: fresh reset then report (variant 1); saved theory + travel then report (variant 2); rejected accusation (`S99`) then report (variant 3).
- Observed: all three variants 422 `{"error":"No conclusion has been accepted yet."}`.
- Verdict reason: exact match in every variant.

## TC-46 — Report is a 422 again after reset: PASS
- Command / action run: closed case, `GET /report` 200, `POST /reset`, `GET /report` again.
- Observed: 422 `{"error":"No conclusion has been accepted yet."}` after reset.
- Verdict reason: exact match.

## TC-47 — Report shape after cannot determine: PASS
- Command / action run: fresh reset, `{"suspectId":null}` accepted, `GET /report`.
- Observed: top-level keys exactly `{case, title, primarySuspect, ending, theory, supportingEvidence, timeline, contradictions, connections}`; `case:"047"`; `title` matches `GET /cases/047`; `primarySuspect:null`; `ending` keys exactly `{id,title,stamp,verdict,narrative,whatHappened}`, `id:"criminal_escapes"`, `narrative` a 3-item non-empty string array, `whatHappened` present and `null`; `theory:""`; all four evidence/timeline/contradictions/connections arrays empty.
- Verdict reason: exact match.

## TC-48 — `criminal_escapes` copy comes from the ending file (all five cases): PASS
- Command / action run: for each of the five cases, fresh reset, `{"suspectId":null}` accepted, `GET /report`; cross-checked `ending.verdict`/`ending.narrative` against `data/cases/<C>/endings.json`'s `criminal_escapes` entry (id list/copy file, not solution.json).
- Observed: `ending.title:"Criminal Escapes"`/`stamp:"UNSOLVED"` in all five; verdicts matched the spec's quoted text exactly for all five cases (047 "The file never became a case." … 051 "A cup of tea, and nobody to answer for it."); every `narrative` array matched the corresponding `endings.json` entry verbatim; no `{accused}` literal anywhere.
- Verdict reason: byte-for-byte match against both the spec text and the source data file.

## TC-49 — Report after a named accusation: `primarySuspect` and name filled in: PASS
- Command / action run: R1 unlocked E015, `POST /conclusion {"suspectId":"S01","evidenceIds":["E015"]}` (200, `innocent_accused`), then `GET /report` and `GET /suspects/S01`.
- Observed: `primarySuspect:{"id":"S01","name":"Dr. Maren Voss"}` (2 keys only); `ending.id` equals the conclusion's ending; no `{accused}` literal anywhere; suspect's name present in a narrative line (since `innocent_accused`); `whatHappened:null`.
- Verdict reason: exact match.

## TC-50 — `supportingEvidence` is exactly the cited exhibits: PASS
- Command / action run: R1 unlocked E015/E017; `POST /conclusion {"suspectId":"S01","evidenceIds":["E015","E017","E015"]}` (dedup to `["E015","E017"]`), then `GET /report` and `GET /evidence/E015`, `/E017`.
- Observed: `supportingEvidence` had exactly two items in order `E015`, `E017`, keys exactly `{id,title,time,location,summary}`, each field matching the corresponding `GET /evidence/<id>` fields; no duplicates. A follow-up cannot-determine accusation (with an exhibit unlocked but not cited) gave `supportingEvidence:[]`.
- Verdict reason: exact match including the empty-on-cannot-determine note.

## TC-51 — `contradictions` empty when none were logged: PASS
- Command / action run: on a fresh accusation with no `POST /contradictions` calls, `GET /report` and `GET /investigation`.
- Observed: `report.contradictions:[]`, `investigation.contradictionsFound:[]`.
- Verdict reason: exact match.

## TC-52 — `contradictions` match the logged ones: PASS
- Command / action run: unlocked and viewed all 12 obtainable 047 exhibits via legitimate play (travel+search across all six places, no file-only exhibits added), asked `POST /notes {"promptId":"contradictions"}`, which surfaced five real assertion/evidence pairs; logged one (`POST /contradictions {"assertionId":"ST02-A","evidenceId":"E007"}` → 200); accepted `{"suspectId":null}`; `GET /report` and `GET /investigation`.
- Observed: `report.contradictions` had one item matching `contradictionsFound`'s pair, keys exactly `{assertionId, suspectName, claim, evidenceId, explanation}`, `suspectName:"Alex Reyes"` (S02's name).
- Verdict reason: exact match; found and logged by legitimate play, not by reading the answer key.

## TC-53 — `connections` match the board's connections: PASS
- Command / action run: R1 unlocked E015; created C01 (E015→S01), C02 (S01→S02), C03 (E015→S02); deleted C02; accepted `{"suspectId":null}`; `GET /report` and `GET /connections`.
- Observed: `report.connections` exactly `[{E015→S01},{E015→S02}]` in creation order, matching `GET /connections` minus `id`; C02 absent.
- Verdict reason: exact match.

## TC-54 — `timeline` contains exactly the reviewed events: PASS
- Command / action run: R1 broadly unlocked evidence across all six places to populate the timeline (11 events appeared, e.g. T02–T12); marked `T02` viewed only; accepted `{"suspectId":null}`; `GET /report`.
- Observed: `report.timeline` had exactly one item, id `T02`, keys `{id,timestamp,time,title,description}` matching `GET /timeline`'s T02 fields; `T03` absent; a fresh state (nothing unlocked) gave `timeline:[]` as confirmed earlier in the run.
- Verdict reason: exact match; multi-item ordering was not separately isolated but the single-item case matches exactly and `GET /timeline`'s own list order was used as source of truth.

## TC-55 — `theory` in the report is the saved theory: PASS
- Command / action run: run 1 — saved `"It was an inside job."`, accepted `{"suspectId":null}`, checked report. run 2 — fresh reset, no theory saved, accepted `{"suspectId":null}`, checked report.
- Observed: run 1 `theory:"It was an inside job."`; run 2 `theory:""`.
- Verdict reason: exact match.

## TC-56 — `GET /report` is side-effect free and stable: PASS
- Command / action run: `GET /investigation` (snapshot A), `GET /report` twice, `GET /investigation` (snapshot B).
- Observed: the two report bodies were deep-equal; snapshot A equalled snapshot B.
- Verdict reason: no side effects, stable output.

## TC-57 — Report and ending never leak the answer key: PASS
- Command / action run: recursive key scan of `GET /report`, `/investigation`, `/cases`, `/cases/047` on a cannot-determine closed state, then repeated on a separate reset state closed with a thin named accusation (S01/E015).
- Observed: zero occurrences of `culprit`, `requiredEvidence`, `requiredConnections`, `solution`, `answer`, `guilty`, `correctSuspect` in either run; `ending.whatHappened:null` in both; accused id in the report equalled the id sent (`S01`) in the named run.
- Verdict reason: no leak found in either run.

## TC-58 — `whatHappened` is null unless the ending named the culprit: PASS
- Command / action run: reused the two TC-57 closed states (cannot-determine and thin named accusation).
- Observed: `ending.whatHappened:null` in both; `ending.id` was `criminal_escapes` and `innocent_accused` respectively — neither `perfect_investigation` nor `true_criminal`.
- Verdict reason: matches expectation.

## TC-65 — Reset after closing returns a fresh state: PASS
- Command / action run: dirtied 047 (theory, R1 exhibit, connection, close with `{"suspectId":null}`), then `POST /reset`.
- Observed: `conclusion:null`, `connections:[]`, `contradictionsFound:[]`, `clock.minutesUsed:0`, `minutesLeft:960` (= `clockHours 16 * 60`), `timeUp:false`, `theory:""`, all viewed/read/visited/searched arrays `[]`, `unlockedEvidence:[]`, `storyFlags:{}`, `locationId:null`, `progress:0`; body equalled a subsequent `GET /investigation`.
- Verdict reason: exact match on every listed field.

## TC-66 — Reset on an untouched case and repeated reset: PASS
- Command / action run: `POST /reset` twice in a row on an already-fresh case.
- Observed: both 200 with byte-identical bodies matching the TC-65 fresh shape.
- Verdict reason: idempotent as required.

## TC-67 — Case is `new` again after reset: PASS
- Command / action run: closed 047, `POST /reset`, `GET /cases`.
- Observed: 047 entry `status:"new"`, `ending:null`.
- Verdict reason: exact match.

## TC-68 — The accusation can be made again after reset: PASS
- Command / action run: reset then `{"suspectId":null}` (200), report 200; reset again then R1 + thin named accusation (200); report 200 after; travel worked again post-reset (`minutesUsed` 30 after one travel).
- Observed: both forms accepted with 200 after their respective resets; travel/search functioned normally again.
- Verdict reason: exact match.

## TC-69 — Reset mid-investigation (no conclusion yet): PASS
- Command / action run: open case with an exhibit unlocked, a connection and a theory saved, then `POST /reset`, `GET /evidence`, `GET /connections`.
- Observed: reset body matched the TC-65 fresh shape; `GET /evidence` and `GET /connections` both `[]`.
- Verdict reason: exact match.

## TC-70 — Connection ids restart after reset: PASS
- Command / action run: created C01, C02 on 047, reset, unlocked an exhibit again, created a new connection.
- Observed: new connection returned `id:"C01"`; `GET /connections` showed only the new C01, old ones gone.
- Verdict reason: exact match.

## TC-71 — Resetting one case leaves the others untouched: PASS
- Command / action run: reset+theory+close both 047 and 048, then `POST /047/reset`, then `GET /048/investigation` and `GET /cases`.
- Observed: 048's theory, conclusion and `clock.minutesUsed` unchanged (full deep-equal before/after); 048 still `"closed"` in `/cases`.
- Verdict reason: per-case isolation confirmed.

## TC-72 — Unknown case id is a 404 on every endpoint of this feature: PASS
- Command / action run: for `<Z>` in `052`, `abc`: `PUT /theory`, `POST /conclusion {"suspectId":null}`, `GET /report`, `POST /reset`.
- Observed: all eight calls 404 with `{"error":"Case not found"}`; `GET /cases` still lists exactly `047,048,049,050,051`.
- Verdict reason: exact match, no state leaked for unknown ids.

## TC-73 — Case clock runs out: accusation accepted, legwork is not: PASS
- Command / action run: fresh reset, R1 unlocked E015, alternated `POST /travel` between L02/L03 until `timeUp` true (31 travels), then (a) `GET /investigation`, (b) one more travel, (c) `POST /conclusion {"suspectId":null}`.
- Observed: (a) `minutesLeft:0`, `timeUp:true`, `now == deadline`. (b) 422 `{"error":"Time is up. The District Attorney wants a name."}`. (c) 200 `ending:"criminal_escapes"`.
- Verdict reason: exact match.

## TC-74 — Reset after time is up restores the clock: PASS
- Command / action run: from the TC-73 time-up state, `POST /reset`, then `POST /travel` to L01.
- Observed: after reset `minutesUsed:0`, `timeUp:false`; travel returned 200 and `minutesUsed` became 30.
- Verdict reason: exact match.

---

## TC-75 — `PUT /theory` rejected once closed, in the right order: PASS
- Command / action run: theory saved `"before closing"`, close with `{"suspectId":null}`; then (a) `PUT /theory {"text":123}`; (b) `PUT /theory {"text":"after closing"}`.
- Observed: (a) 400 with the TC-03 error string. (b) 422 `{"error":"This case is closed."}`. Theory stayed `"before closing"` after both.
- Verdict reason: matches the documented ordering (shape check before closed check) exactly, same as `/conclusion`'s TC-34.

## TC-76 — Board connections can still be created after closing: PASS
- Command / action run: R1 unlocked E015, closed with `{"suspectId":null}`; first `GET /report` confirmed `connections:[]`; `POST /connections {"source":"E015","target":"S01","relationship":"linked_to"}`; second `GET /report`.
- Observed: POST returned 201 `{"id":"C01",...}`; the second report included the new connection live.
- Verdict reason: matches the source-derived expectation exactly (no closed-case check on `createConnection`, and the report reflects live state, not a frozen snapshot).

## TC-77 — A connection can still be deleted after closing: PASS
- Command / action run: `DELETE /connections/C01` on the TC-76 connection.
- Observed: 204; a following `GET /connections` returned `[]`.
- Verdict reason: matches the source-derived expectation.

## TC-78 — A contradiction can still be logged after closing: PASS
- Command / action run: fresh reset, R1 broadly unlocked+viewed evidence, found real contradiction pairs via `POST /notes {"promptId":"contradictions"}`, closed with `{"suspectId":null}` before logging, then `POST /contradictions {"assertionId":"ST02-A","evidenceId":"E007"}`.
- Observed: 200, pair recorded; `GET /investigation.contradictionsFound` and `GET /report.contradictions` both include it.
- Verdict reason: matches the source-derived expectation; contradiction was found by legitimate play, not by reading the answer key.

## TC-79 — Viewed marking still works after closing: PASS
- Command / action run: fresh reset, R1 broadly unlocked evidence to populate the timeline, closed with `{"suspectId":null}` before marking anything viewed, then `POST /viewed {"type":"event","id":"T02"}`.
- Observed: 200; `GET /investigation.eventsViewed` and `GET /report.timeline` both include `T02`.
- Verdict reason: matches the source-derived expectation.

## TC-80 — Cited exhibits still checked on a cannot-determine accusation: PASS
- Command / action run: fresh reset; (a) `POST /conclusion {"suspectId":null,"evidenceIds":["E001"]}` (locked); (b) `{"suspectId":null,"evidenceIds":["E999"]}` (non-existent).
- Observed: both 422 with the byte-identical body `{"error":"One of the cited exhibits isn't in this case."}`; `conclusion` stayed null after both.
- Verdict reason: exact match, complements TC-17/TC-23.

## TC-81 — Case clock never overshoots its budget: PASS
- Command / action run: fresh reset; 1 travel (30) + 1 search (15) + 30 more travels = 945 minutes used, `minutesLeft:15` (< travel cost of 30, `timeUp:false`); one more `POST /travel`.
- Observed: that final travel returned 200; afterwards `minutesUsed` was exactly 960 (clamped, not 975), `timeUp:true`, `minutesLeft:0`; a further travel then returned 422 with the TC-73 message.
- Verdict reason: exact match — `Math.min(cost, minutesLeft)` clamping confirmed by direct observation, not just read from source.

## TC-82 — BLOCKED: `perfect_investigation` ending: BLOCKED
- Command / action run: not run.
- Observed: n/a — would require `solution.json`'s `requiredEvidence`/`requiredConnections` for the true culprit.
- Verdict reason: BLOCKED by design, as the test case itself specifies; not attempted by elimination.

## TC-83 — BLOCKED: `true_criminal` ending: BLOCKED
- Command / action run: not run.
- Observed: n/a — same dependency on `solution.json` as TC-82.
- Verdict reason: BLOCKED by design.

## TC-84 — BLOCKED: `wrong_suspect` ending: BLOCKED
- Command / action run: not run.
- Observed: n/a — confirming a suspect is innocent (as opposed to merely accusing them once) needs the answer key.
- Verdict reason: BLOCKED by design.

## TC-85 — [UI] Accusation screen lists every suspect plus "cannot determine": BLOCKED
- Command / action run: not run — no browser automation tool available in this session.
- Observed: n/a.
- Verdict reason: BLOCKED per the runner's own rule; not guessed from `FinalReport.jsx`/`EndingScreen.jsx` source.

## TC-86 — [UI] Both accusation paths have a confirm step with a working back action: BLOCKED
- Command / action run: not run — no browser automation tool available.
- Observed: n/a.
- Verdict reason: BLOCKED, same reason as TC-85.

## TC-87 — [UI] Evidence step: filing disabled until an exhibit is picked: BLOCKED
- Command / action run: not run — no browser automation tool available.
- Observed: n/a.
- Verdict reason: BLOCKED, same reason as TC-85.

## TC-88 — [UI] Ending screen shows the API's title/stamp/verdict: BLOCKED
- Command / action run: not run — no browser automation tool available.
- Observed: n/a.
- Verdict reason: BLOCKED, same reason as TC-85.

## TC-89 — [UI] Report opens automatically after the closing cinematic: BLOCKED
- Command / action run: not run — no browser automation tool available.
- Observed: n/a.
- Verdict reason: BLOCKED, same reason as TC-85.

## TC-90 — [UI] Closed state persists on reload: BLOCKED
- Command / action run: not run — no browser automation tool available.
- Observed: n/a.
- Verdict reason: BLOCKED, same reason as TC-85.

## TC-91 — [UI] Play Again requires a second confirmation, resets, clears board layout: BLOCKED
- Command / action run: not run — no browser automation tool available.
- Observed: n/a.
- Verdict reason: BLOCKED, same reason as TC-85.

---

## Summary
Total: 85 | Pass: 75 | Fail: 0 | Blocked: 10

By section:
- Original 68 cases (TC-01–TC-58, TC-65–TC-74): 68 pass, 0 fail, 0 blocked.
- Added 2026-09-24 cases, 17 total (TC-75–TC-91): 7 pass (TC-75–TC-81), 0 fail, 10 blocked (TC-82–TC-84 answer-key-blocked by design; TC-85–TC-91 UI-blocked, no browser tool in this session).

Failures needing attention: none.

Other notes for the owner (not failures):
- Spec-point-7 ambiguity (flagged by the test-case writer): every thin accusation made in this run resolved to `innocent_accused`, never `criminal_escapes` — recorded per the runner rules, not used to pick or judge any case.
- TC-43's own "source-derived" prediction (that free repeats after closing would return 200) did not hold — the live backend returned 422 on all three repeats, which is what the spec itself expects, so this is a clean PASS, not a deviation to report.
- TC-17/TC-80 confirm cited `evidenceIds` are kept (not silently dropped) even on a `suspectId:null` accusation, and are still validated against the case file even when no suspect is named.

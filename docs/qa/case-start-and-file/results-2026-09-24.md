# Results — case-start-and-file
Run at: 2026-09-24T16:25:30Z
Backend reachable: yes (isolated instance on http://localhost:4202, DB
`.../scratchpad/qa/case-start-and-file.sqlite`, started fresh for this run)
Frontend: not started — no browser automation tool (`mcp__plugin_playwright_playwright__*`
or `mcp__Claude_Browser__*`) was available in this run's toolset (only Bash, Read, Write,
Glob were present). Per the task rules, all UI cases (TC-34–TC-44) are marked BLOCKED
rather than guessed from source.

Note on seed facts: the doc's "Seed facts" table (line 27) states "Every case has pages
`F1`..`F4` in every case." Observed reality: case 050 only has 3 pages (`F1`, `F2`, `F3`,
titles "Incident report", "Insurance schedule and security survey", "Persons of interest") —
there is no F4 for 050. No individual test case strictly requires a fourth page in 050, so
this did not cause any TC to fail, but it is recorded here since it contradicts the doc's
stated seed fact.

---

## TC-01 — Fresh GET /file (Case 047): every page unread, body null, attachments empty: PASS
- Command / action run: `POST /cases/047/reset`, then `GET /cases/047/file`
- Observed: 200; body is `[{"id":"F1",...,"read":false,"body":null,"attachments":[]}, F2, F3, F4]`, 4 entries, ids in order F1-F4, all keys `id,title,read,body,attachments`, all `read:false`, `body:null`, `attachments:[]`, titles non-empty.
- Verdict reason: matches expected exactly.

## TC-02 — Fresh GET /file for every other case: PASS
- Command / action run: reset then `GET /file` for 048, 049, 050, 051
- Observed: All 200. 048: F1-F4, all unread/null/[]. 049: F1-F4, all unread/null/[] (F3 attachments empty pre-read as required). 050: only F1-F3 (see note above), all unread/null/[]. 051: F1-F4, all unread/null/[].
- Verdict reason: page ids start at F1 and are contiguous in every case (3 for 050, 4 for others); expected result does not mandate exactly 4 pages, so this passes, with the seed-fact-table discrepancy noted above.

## TC-03 — GET /cases/047 leaves out the page bodies: PASS
- Command / action run: `GET /cases/047`
- Observed: no `"file"` key present; `"filePages":4`; body text does not contain `Taken: the Argus-7 prototype, from its case in Storage Room B.` (grep count 0) nor `047-2` (grep count 0); `"clock":{"start":"1984-03-10T08:15:00","hours":16,"deadlineNote":"The board wants a name by midnight."}`.
- Verdict reason: matches expected exactly.

## TC-04 — GET /cases/:caseId leaves out page bodies for all five cases: PASS
- Command / action run: for each of 047/048/049/050/051: reset, read every page (`GET /file` ids, then `POST .../read` for each), then `GET /cases/<id>`.
- Observed: for every case, no `"file"` key, `filePages` equals the `/file` array length (4,4,4,3,4), no `"body"` key anywhere in the case response (grep for `"body":` returns nothing). Spot check: last paragraph of the last page (F4 for 047/048/049/051, F3 for 050 since it has no F4) does not appear anywhere in `GET /cases/<id>` (grep count 0 in every case).
- Verdict reason: matches expected exactly (with the F4→F3 substitution for 050 noted, since that case has no F4).

## TC-05 — Fresh investigation: nothing unlocked, no pages read, no file flags, clock at zero (Case 047): PASS
- Command / action run: reset 047, `GET /investigation`
- Observed: `unlockedEvidence:[]`, `pagesRead:[]`, `storyFlags:{}` (no `file.*` key), `clock.minutesUsed:0`, `minutesLeft:960`, `timeUp:false`, `now:"1984-03-10T08:15:00"`, `start` same, `deadline:"1984-03-11T00:15:00"`.
- Verdict reason: matches expected exactly.

## TC-06 — Fresh GET /evidence returns an empty list, for every case: PASS
- Command / action run: reset each of 047-051; `GET /evidence` and `GET /investigation` for each.
- Observed: `GET /evidence` is `[]` for all 5 cases; `investigation.unlockedEvidence` is `[]` for all 5.
- Verdict reason: matches expected exactly.

## TC-07 — Read a page: response shape: PASS
- Command / action run: fresh 047, `POST /cases/047/file/F1/read`
- Observed: top-level keys `page, investigation, effects`; `page.id:"F1"`, `page.read:true`, `page.body` array of 4 non-empty strings, `page.attachments:[]`, `page.title:"Incident report"` (matches `GET /file` title); `investigation` has `clock, storyFlags, unlockedEvidence, pagesRead`; `effects.unlockedEvidence:[]`.
- Verdict reason: matches expected exactly.

## TC-08 — First read costs 20 minutes and moves the clock: PASS
- Command / action run: continuation of TC-07's read, then `GET /investigation`
- Observed: both responses' `clock`: `minutesUsed:20`, `minutesLeft:940`, `timeUp:false`, `now:"1984-03-10T08:35:00"`, `start` and `deadline` unchanged; `pagesRead:["F1"]`.
- Verdict reason: matches expected exactly.

## TC-09 — Re-reading the same page costs nothing and unlocks nothing new: PASS
- Command / action run: `POST /cases/047/file/F1/read` twice more (state from TC-08)
- Observed: both calls 200; both `clock.minutesUsed:20`, `minutesLeft:940`, `now:"1984-03-10T08:35:00"`; `page.read:true`; the two `page.body` arrays are byte-identical to the first read's body (diff confirmed no difference); `effects.unlockedEvidence:[]` both times; `pagesRead:["F1"]` (no duplicate).
- Verdict reason: matches expected exactly.

## TC-10 — Reading pages without attachments unlocks nothing (Case 047, all pages): PASS
- Command / action run: fresh 047; `POST .../F1/read`, `.../F2/read`, `.../F3/read`, `.../F4/read`; `GET /evidence` after each.
- Observed: each read 200 with `effects.unlockedEvidence:[]` and `page.attachments:[]`; `GET /evidence` returned `[]` after every one of the four reads; clock progressed 20/40/60/80 as expected.
- Verdict reason: matches expected exactly.

## TC-11 — Reading a page with an attachment unlocks it (Case 048, F2 -> E002): PASS
- Command / action run: fresh 048; pre-check `GET /evidence/E002` (404); `POST /cases/048/file/F2/read`; `GET /evidence`; `GET /evidence/E002`
- Observed: pre-read `GET /evidence/E002` → 404. Read response: `page.attachments:[{"id":"E002","title":"Medical Examiner's Note"}]`; `effects.unlockedEvidence` same; `investigation.unlockedEvidence:["E002"]`; `clock.minutesUsed:20`. `GET /evidence` → one-element array with `id:"E002"`. `GET /evidence/E002` → 200.
- Verdict reason: matches expected exactly.

## TC-12 — Case 049: two pages with attachments (F2 -> E002, F3 -> E015): PASS
- Command / action run: fresh 049; read F1, F2, F3, F4 in order.
- Observed: F1: `effects:[]`, `unlockedEvidence:[]`, `minutesUsed:20`. F2: `effects:[{"id":"E002","title":"Loss Schedule"}]`, `unlockedEvidence:["E002"]`, `minutesUsed:40`. F3: `effects:[{"id":"E015","title":"Kemp's 1971 Conviction"}]`, `unlockedEvidence:["E002","E015"]`, `minutesUsed:60`. F4: `effects:[]`, `unlockedEvidence` unchanged, `minutesUsed:80`.
- Verdict reason: matches expected exactly.

## TC-13 — Cases 050 and 051: F2 unlocks E002 with the right title: PASS
- Command / action run: fresh 050, `POST .../F2/read`; fresh 051, `POST .../F2/read`.
- Observed: 050: `effects:[{"id":"E002","title":"Insurance Schedule"}]`, `unlockedEvidence:["E002"]`. 051: `effects:[{"id":"E002","title":"Toxicology Report"}]`, `unlockedEvidence:["E002"]`.
- Verdict reason: matches expected exactly.

## TC-14 — Re-reading an attachment page does not re-unlock or duplicate: PASS
- Command / action run: continuing 048 from TC-11 (`minutesUsed:20`, `unlockedEvidence:["E002"]`); `POST /cases/048/file/F2/read` again.
- Observed: 200; `effects.unlockedEvidence:[]`; `investigation.unlockedEvidence:["E002"]` (single entry); `minutesUsed:20` (unchanged); `page.attachments` still `[{"id":"E002","title":"Medical Examiner's Note"}]`.
- Verdict reason: matches expected exactly.

## TC-15 — GET /file after reading: only read pages reveal body and attachments: PASS
- Command / action run: continuing 048 (only F2 read); `GET /cases/048/file`
- Observed: F2 entry `read:true`, non-empty `body` array, `attachments:[{"id":"E002","title":"Medical Examiner's Note"}]`. F1, F3, F4: `read:false`, `body:null`, `attachments:[]`.
- Verdict reason: matches expected exactly.

## TC-16 — Reading sets storyFlags["file.<pageId>"] to true, and only for pages read: PASS
- Command / action run: fresh 047; `POST .../F1/read`; `GET /investigation`
- Observed: both responses' `storyFlags`: `{"file.F1":true}` — boolean true, no `file.F2/F3/F4` keys; `pagesRead:["F1"]`.
- Verdict reason: matches expected exactly.

## TC-17 — All four pages read: all flags set, cumulative cost is 80 minutes: PASS
- Command / action run: fresh 047 (same run as TC-10); read F1-F4; `GET /investigation`
- Observed: `storyFlags` has `file.F1`, `file.F2`, `file.F3`, `file.F4` all `true`; `pagesRead` exactly F1-F4; `clock.minutesUsed:80`, `minutesLeft:880`, `now:"1984-03-10T09:35:00"`, `timeUp:false`; per-read costs observed as 20/40/60/80 cumulative.
- Verdict reason: matches expected exactly.

## TC-18 — Unknown page id is a 404 and changes nothing: PASS
- Command / action run: fresh 047; `POST /cases/047/file/F9/read`; `GET /investigation`
- Observed: 404, `{"error":"There is no such page in the file"}`; afterwards `minutesUsed:0`, `pagesRead:[]`, `unlockedEvidence:[]`, no `file.*` flag.
- Verdict reason: matches expected exactly.

## TC-19 — Page ids are exact: wrong case, empty-ish and other-kind ids are 404: PASS
- Command / action run: `POST .../file/f1/read`, `.../F0/read`, `.../E002/read`, `.../F1%20/read`
- Observed: all four calls 404 with `{"error":"There is no such page in the file"}`; afterwards `minutesUsed:0`, `pagesRead:[]`.
- Verdict reason: matches expected exactly.

## TC-20 — Unknown case id is a 404 on the file endpoints: PASS
- Command / action run: `GET /cases/999/file`; `POST /cases/999/file/F1/read`; `GET /cases/999/investigation`; `GET /cases/999`
- Observed: all four calls 404 with `{"error":"Case not found"}`.
- Verdict reason: matches expected exactly.

## TC-21 — Clock object shape and deadline arithmetic for every case: PASS
- Command / action run: reset each of 047-051; `GET /investigation` and `GET /cases/<id>` for each.
- Observed: 047: start 1984-03-10T08:15:00 / deadline 1984-03-11T00:15:00 / minutesLeft 960. 048: 1984-05-19T09:00:00 / 1984-05-19T23:00:00 / 840. 049: 1984-04-24T10:00:00 / 1984-04-25T00:00:00 / 840. 050: 1984-06-10T09:00:00 / 1984-06-10T21:00:00 / 720. 051: 1984-07-15T09:00:00 / 1984-07-15T21:00:00 / 720. All had `now===start`, `minutesUsed:0`, `timeUp:false`, and matching `clock.start` between `/investigation` and `/cases/<id>`.
- Verdict reason: matches expected exactly for all 5 cases.

## TC-22 — Time costs are published on the clock and match the spec: PASS
- Command / action run: `GET /cases/047/investigation`
- Observed: `clock.costs === {"readPage":20,"travel":30,"search":15,"question":10,"present":10}`.
- Verdict reason: matches expected exactly.

## TC-23 — minutesLeft decreases as actions are taken; now tracks minutesUsed: PASS
- Command / action run: fresh 047; `GET /places` (took L01); `POST .../file/F1/read`; `POST /cases/047/travel {"locationId":"L01"}`
- Observed: after read: `minutesUsed:20`, `minutesLeft:940`, `now:"1984-03-10T08:35:00"`. After travel: 200, `minutesUsed:50`, `minutesLeft:910`, `now:"1984-03-10T09:05:00"`; `minutesUsed+minutesLeft===960` at every step; `deadline`/`start` unchanged.
- Verdict reason: matches expected exactly.

## TC-24 — GET requests never change the clock or read state: PASS
- Command / action run: fresh 047; 3x each of `GET /file`, `GET /investigation`, `GET /cases/047`, `GET /evidence`; then `GET /cases/047/file/F1/read`
- Observed: after the GETs, `minutesUsed:0`, `pagesRead:[]`, all pages `read:false`. `GET .../file/F1/read` → 404 `{"error":"Not found"}` (no such GET route) and F1 remained unread.
- Verdict reason: matches expected exactly (exact 404 body text wasn't specified for this case).

## TC-25 — Reading an unread page after time is up is rejected (422), no state change: PASS
- Command / action run: fresh 047; read F1,F2,F3 (`minutesUsed:60`); `GET /places` (took L01/L02); 30 alternating `POST /travel` calls, all returned 200; `GET /investigation`; `POST .../F4/read`; `GET /investigation`; `GET /file`
- Observed: checkpoint `minutesUsed:960`, `minutesLeft:0`, `timeUp:true`, `now:"1984-03-11T00:15:00"` (equal to deadline). F4 read → 422 `{"error":"Time is up. The District Attorney wants a name."}`. Afterwards `minutesUsed` still 960, `pagesRead:["F1","F2","F3"]`, no `file.F4` flag, F4 entry `read:false, body:null`.
- Verdict reason: matches expected exactly.

## TC-26 — After time is up, re-reading an already-read page still works and costs nothing: PASS
- Command / action run: continuing TC-25 state; `POST /cases/047/file/F1/read`
- Observed: 200; `page.read:true`, non-empty `body`; `investigation.clock.minutesUsed:960`, `timeUp:true`; `effects.unlockedEvidence:[]`.
- Verdict reason: matches expected exactly.

## TC-27 — Reading an unread page on a closed case is rejected (422): FAIL
- Command / action run: fresh 047; read F1 only; `POST /cases/047/conclusion {"suspectId":null,"evidenceIds":[]}` (200); `POST /cases/047/file/F2/read`; `POST /cases/047/file/F1/read`
- Observed: F2 (unread): 422, `{"error":"This case is closed."}`, afterwards `minutesUsed:20`, `pagesRead:["F1"]` — matches expected. F1 (already read): observed 422 `{"error":"This case is closed."}`, NOT the 200 the test case expects; `minutesUsed` unchanged at 20.
- Verdict reason: the F1 (already-read) half of this test case's expected result does not match current backend behaviour — `field.service.readPage` calls `assertOpen()` unconditionally before the "already read" short-circuit, so a closed case rejects re-reading a previously-read page too. This is the documented conflict with TC-47 (added 2026-09-24, post commit b45dd98); the expectation in this original test case may be outdated. Recorded as FAIL per instructions, not reinterpreted.

## TC-28 — Reset restores clock, flags, pages and unlocked evidence: PASS
- Command / action run: 048 with F1,F2 read (`minutesUsed:40`, `unlockedEvidence:["E002"]`); `POST /cases/048/reset`; `GET /investigation`, `GET /file`, `GET /evidence`
- Observed: reset response: `clock.minutesUsed:0`, `minutesLeft:840`, `now===start==="1984-05-19T09:00:00"`, `timeUp:false`, `storyFlags:{}`, `pagesRead:[]`, `unlockedEvidence:[]`, `conclusion:null`. Subsequent GETs: all pages `read:false, body:null, attachments:[]`; `GET /evidence` → `[]`; `GET /evidence/E002` → 404.
- Verdict reason: matches expected exactly.

## TC-29 — After reset, reading costs and unlocks again (nothing is "already spent"): PASS
- Command / action run: continuing from TC-28 (fresh 048 after reset); `POST /cases/048/file/F2/read`
- Observed: 200; `minutesUsed:20`; `effects.unlockedEvidence:[{"id":"E002","title":"Medical Examiner's Note"}]`; `storyFlags["file.F2"]:true`.
- Verdict reason: matches expected exactly.

## TC-30 — Reset after time-up or a closed case makes the file readable again: PASS
- Command / action run: 047 in a closed state; `POST /cases/047/reset`; `POST /cases/047/file/F4/read`
- Observed: reset → `clock.timeUp:false`, `minutesUsed:0`, `conclusion:null`. F4 read → 200, `minutesUsed:20`.
- Verdict reason: matches expected exactly.

## TC-31 — Progress is scoped to its own case: PASS
- Command / action run: fresh 047 and fresh 048; `POST /cases/048/file/F2/read`; `GET /cases/047/investigation`, `/file`, `/evidence`; `POST /cases/047/reset`; `GET /cases/048/investigation`
- Observed: after the 048 read, 047 untouched — `minutesUsed:0`, `pagesRead:[]`, `storyFlags:{}`, all 047 pages unread, `GET /evidence` `[]`. After resetting 047, 048 still has `minutesUsed:20`, `pagesRead:["F2"]`, `unlockedEvidence:["E002"]`, `storyFlags["file.F2"]:true`.
- Verdict reason: matches expected exactly.

## TC-32 — State persists between requests (server-owned): PASS
- Command / action run: fresh 047; `POST .../F1/read`, `.../F3/read`; separate `GET /investigation` and `GET /file` calls
- Observed: `pagesRead:["F1","F3"]`, `minutesUsed:40`, `now:"1984-03-10T08:55:00"`, flags `file.F1`/`file.F3` true (no `file.F2`/`file.F4`); `GET /file` shows F1,F3 `read:true` with bodies, F2,F4 `read:false, body:null`.
- Verdict reason: matches expected exactly.

## TC-33 — The file endpoints never leak the answer key: PASS
- Command / action run: fresh 049; read F1-F4; captured raw text of `GET /cases/049`, `GET /file`, all 4 read responses, `GET /investigation`
- Observed: grep for the literal strings `"solution"`, `"culprit"`, `"requiredEvidence"`, `"requiredConnections"` across all captured text returned zero matches.
- Verdict reason: matches expected exactly — no answer-key leak.

## TC-34 — UI: CaseStart shows the brief from the API and the two main choices (fresh case): BLOCKED
- Command / action run: none — no browser automation tool available in this run's toolset.
- Observed: n/a
- Verdict reason: cannot be exercised without a browser tool; not guessed from source per the rules.

## TC-35 — UI: CaseStart takes its text from the API, not from the source: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-36 — UI: choosing "Open the file" goes to the file; choosing "Go straight to the scene" goes to the map at no cost: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-37 — UI: CaseStart choice labels change once the case has been started or closed: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-38 — UI: unread CaseFile page hides its body and offers a timed read button: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-39 — UI: reading a page reveals the body, ticks the tab and updates the HUD clock: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-40 — UI: a page with paperwork shows a notice and an attached-exhibit clip: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-41 — UI: reading a page without paperwork shows no clue notice: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-42 — UI: HUD clock matches the API on a fresh case and survives a refresh: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-43 — UI: read button is disabled and HUD says time is up when the clock has run out: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-44 — UI: a rejected read shows the server message: BLOCKED
- Command / action run: none — no browser automation tool available.
- Observed: n/a
- Verdict reason: same as TC-34.

## TC-45 — Clock clamps to the remaining minutes instead of rejecting the action (Case 047, plain page): PASS
- Command / action run: fresh 047; read F1 (`minutesUsed→20`); 31 alternating `POST /travel` calls to L01/L02 (all 200, cumulative → 950); `GET /investigation`; `POST .../F2/read`; `GET /investigation`; `POST .../F3/read`
- Observed: checkpoint `minutesUsed:950`, `minutesLeft:10`, `timeUp:false`, `now:"1984-03-11T00:05:00"`. F2 read → 200; `page.read:true`, non-null body, `effects.unlockedEvidence:[]`, `storyFlags["file.F2"]:true`, `pagesRead` includes F2; resulting clock `minutesUsed:960`, `minutesLeft:0`, `timeUp:true`, `now:"1984-03-11T00:15:00"` (equal to deadline — only 10 of the nominal 20 minutes were charged). F3 read → 422 `{"error":"Time is up. The District Attorney wants a name."}`, `pagesRead` still excludes F3.
- Verdict reason: matches expected exactly; confirms the clamp-instead-of-reject behaviour of `spendTime`.

## TC-46 — Clock clamp still triggers the attachment unlock (Case 048, F2 -> E002): PASS
- Command / action run: fresh 048; read F1 (`minutesUsed→20`); 27 alternating `POST /travel` calls to L01/L02 (all 200, cumulative → 830); `GET /investigation`; `POST .../F2/read`; `GET /evidence/E002`
- Observed: checkpoint `minutesUsed:830`, `minutesLeft:10`, `timeUp:false`. F2 read → 200; `effects.unlockedEvidence:[{"id":"E002","title":"Medical Examiner's Note"}]`; `unlockedEvidence` contains E002; `storyFlags["file.F2"]:true`; resulting clock `minutesUsed:840`, `minutesLeft:0`, `timeUp:true`, `now:"1984-05-19T23:00:00"` (equal to deadline). `GET /evidence/E002` afterwards → 200.
- Verdict reason: matches expected exactly — the attachment still unlocks even though the cost was clamped.

## TC-47 — Closed case rejects re-reading an already-read page too (corrects the "already read" branch of TC-27): PASS
- Command / action run: fresh 047; read F1 only (`minutesUsed:20`, `pagesRead:["F1"]`); close via `POST /conclusion {"suspectId":null,"evidenceIds":[]}` (200); `POST /cases/047/file/F1/read`; `GET /investigation`; `GET /file`
- Observed: F1 re-read on the closed case → 422, body exactly `{"error":"This case is closed."}`. Afterwards `minutesUsed` unchanged at 20, `pagesRead` unchanged `["F1"]`, F1 entry in `GET /file` unchanged (`read:true`, same body).
- Verdict reason: matches expected exactly; this is the corrected/current-source description of the same scenario TC-27's second half asserts differently.

## TC-48 — Unknown page id is still a 404 on a closed case (existence check runs before the closed-case gate): PASS
- Command / action run: fresh 047 (no reads); close via `POST /conclusion {"suspectId":null,"evidenceIds":[]}` (200); `POST /cases/047/file/F9/read`
- Observed: 404, `{"error":"There is no such page in the file"}` — not 422 "This case is closed." Afterwards `minutesUsed:0`, `pagesRead:[]`.
- Verdict reason: matches expected exactly; page-existence check runs before the closed-case gate.

## TC-49 — GET /cases/:caseId `locations` entries never expose search spots, people or arrival text: PASS
- Command / action run: fresh case for each of 047-051; `GET /cases/<id>` and inspect every `locations` element.
- Observed: for every case, every `locations` element has exactly the keys `id, name, floor, district, description, map` — no `spots`, `people` or `arrival` key at any depth; `locations.length === locationCount` (6 for every case); raw response text grep for the literal keys `"spots"`, `"people"`, `"arrival"` returned zero matches in every case.
- Verdict reason: matches expected exactly.

---

## Summary
Total: 49 | Pass: 37 | Fail: 1 | Blocked: 11

By source:
- Original (TC-01–TC-44, 44 cases): Pass 32 | Fail 1 | Blocked 11
- Added 2026-09-24 (TC-45–TC-49, 5 cases): Pass 5 | Fail 0 | Blocked 0

Failures needing attention:
- TC-27 — the "re-read an already-read page on a closed case returns 200" half of this test case does not match current backend behaviour: `readPage` calls `assertOpen()` before the "already read" check, so it returns 422 `{"error":"This case is closed."}` instead. This is the known TC-27/TC-47 conflict called out in the "Additional cases" section; TC-47 documents the behaviour actually produced by the running code and passed. The expectation in TC-27 may be outdated (it predates fix commit `b45dd98`) — left as FAIL here per instructions, not reinterpreted.

Blocked (all for the same reason — no browser automation tool available in this run's toolset):
TC-34, TC-35, TC-36, TC-37, TC-38, TC-39, TC-40, TC-41, TC-42, TC-43, TC-44.

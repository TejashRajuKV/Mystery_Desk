# Results — board-and-connections
Run at: 2026-09-24T16:20:00Z (backend timestamps observed in responses are in-game dates, 1984)
Backend reachable: yes (isolated instance on http://localhost:4207, DB_PATH scoped to this run's own scratchpad sqlite file)

Notes on this run:
- Ran against an isolated backend (`PORT=4207`, dedicated `DB_PATH`), never against the owner's :4000/:5173 instance.
- No browser automation tool (`mcp__plugin_playwright_playwright__*` or `mcp__Claude_Browser__*`) was present in this session's toolset, so no frontend was started and every `[UI]` case (TC-47–TC-56) is `BLOCKED`.
- All 46 API-level cases (TC-01–TC-46, i.e. the 42 original + the 4 API-level additions) were run as literal `curl` calls against the live isolated backend, resetting the case (`POST /cases/<id>/reset`) between cases wherever the case's stated precondition required a fresh case, and chaining state forward where a case explicitly built on the previous one's state (e.g. TC-05→TC-06, TC-18→TC-19).

## TC-01 — Fresh case has no connections: PASS
- Command: `GET /api/cases/047/connections`
- Observed: `200`, body `[]`
- Verdict reason: exact match to expected.

## TC-02 — Fresh case has no connections in every case folder: PASS
- Command: `GET /api/cases/<id>/connections` for 047, 048, 049, 050, 051
- Observed: all five `200` with `[]`
- Verdict reason: exact match.

## TC-03 — Create a link between two suspects (relationship omitted): PASS
- Command: `POST /api/cases/047/connections {"source":"S01","target":"S02"}`
- Observed: `201`, body `{"id":"C01","source":"S01","target":"S02","relationship":"linked_to"}`
- Verdict reason: exactly the four keys, `linked_to` default applied, id matches `^C\d+$`.

## TC-04 — Create a link with explicit relationship: PASS
- Command: fresh case, `POST /api/cases/047/connections {"source":"S03","target":"S04","relationship":"linked_to"}`
- Observed: `201`, `{"id":"C01","source":"S03","target":"S04","relationship":"linked_to"}`
- Verdict reason: matches expected.

## TC-05 — Created link appears in GET /connections: PASS
- Command: two sequential POSTs (`S01/S02`, `S03/S04`) then `GET /connections`
- Observed: GET returns `[{"id":"C01","source":"S01","target":"S02",...},{"id":"C02","source":"S03","target":"S04",...}]`, same order and fields as the two POST responses.
- Verdict reason: matches expected.

## TC-06 — investigation.connections matches GET /connections: PASS
- Command: `GET /connections` and `GET /investigation` (continuing TC-05 state)
- Observed: both return the identical two-item array (deep equal).
- Verdict reason: matches expected.

## TC-07 — Link two locations: PASS
- Command: fresh case, `POST {"source":"L01","target":"L02"}`
- Observed: `201`, `source:"L01"`, `target:"L02"`
- Verdict reason: matches expected.

## TC-08 — Link a statement claim to a suspect: PASS
- Command: confirmed `ST02-A` exists in `GET /statements`, then `POST {"source":"ST02-A","target":"S02"}`
- Observed: `201`, `{"id":"C01","source":"ST02-A","target":"S02","relationship":"linked_to"}`
- Verdict reason: matches expected.

## TC-09 — Link two claims: PASS
- Command: fresh case, `POST {"source":"ST01-A","target":"ST02-A"}`
- Observed: `201` with those two ids.
- Verdict reason: matches expected.

## TC-10 — Link an unlocked exhibit: PASS
- Command: fresh case + Setup A, `POST {"source":"E002","target":"S02"}`; repeated on a fresh case + Setup A with `{"source":"S02","target":"E002"}`
- Observed: both `201` with the expected source/target pairs.
- Verdict reason: matches expected.

## TC-11 — Locked exhibit is rejected: PASS
- Command: fresh case (no Setup A), confirmed `E002` absent from `GET /evidence`, then `POST {"source":"E002","target":"S02"}` and swapped.
- Observed: both `422 {"error":"One of those items isn't in this case."}`; `GET /connections` still `[]`.
- Verdict reason: matches R-UNKNOWN exactly.

## TC-12 — Nonexistent exhibit id is rejected: PASS
- Command: fresh case + Setup A, `POST {"source":"E999","target":"S02"}`
- Observed: `422 {"error":"One of those items isn't in this case."}`
- Verdict reason: matches expected.

## TC-13 — Timeline event with no held exhibit is rejected: PASS
- Command: fresh case, confirmed `T08` absent from `GET /timeline`, then `POST {"source":"T08","target":"S02"}`
- Observed: `422 {"error":"One of those items isn't in this case."}`
- Verdict reason: matches expected.

## TC-14 — Timeline event becomes linkable once an exhibit behind it is held: PASS
- Command: fresh case + Setup A, confirmed `T08` now present in `GET /timeline`, then `POST {"source":"T08","target":"S02"}`
- Observed: `201`, `{"id":"C01","source":"T08","target":"S02","relationship":"linked_to"}`
- Verdict reason: matches expected.

## TC-15 — Unknown ids of every kind are rejected, in either position: PASS
- Command: fresh case; for each `X` in `S99, L99, T99, ST99-A, ST02-Z, X01, C01, abc`, sent `{"source":"S01","target":"X"}` and `{"source":"X","target":"S01"}` (16 calls total)
- Observed: all 16 calls returned `422 {"error":"One of those items isn't in this case."}`; `GET /connections` still `[]`.
- Verdict reason: matches expected exactly.

## TC-16 — Bare statement id and search-spot id (spec ambiguity): PASS
- Command: fresh case, `POST {"source":"ST02","target":"S01"}` and `POST {"source":"L01-locker","target":"S01"}`
- Observed: both `422 {"error":"One of those items isn't in this case."}`
- Verdict reason: matches "per current source" expectation exactly (no deviation to flag — neither returned 201).

## TC-17 — Self-link is rejected: PASS
- Command: fresh case, `POST {"source":"S01","target":"S01"}`
- Observed: `422 {"error":"An item can't be linked to itself."}`; `GET /connections` is `[]`.
- Verdict reason: matches R-SELF exactly.

## TC-18 — Duplicate in the same direction is rejected: PASS
- Command: fresh case, create `S01/S02` (201), then repeat the same POST
- Observed: second call `422 {"error":"Those two are already linked."}`; `GET /connections` has exactly one item.
- Verdict reason: matches R-DUP exactly.

## TC-19 — Duplicate in the reverse direction is rejected: PASS
- Command: continuing TC-18 state, `POST {"source":"S02","target":"S01"}`
- Observed: `422 {"error":"Those two are already linked."}`; `GET /connections` still one item, `source:"S01"`, `target:"S02"`.
- Verdict reason: matches expected.

## TC-20 — Missing source is a 400: PASS
- Command: fresh case, `POST {"target":"S02"}`
- Observed: `400 {"error":"Expected { source, target, relationship }"}`
- Verdict reason: matches R-400 exactly.

## TC-21 — Missing target is a 400: PASS
- Command: `POST {"source":"S01"}`
- Observed: `400 {"error":"Expected { source, target, relationship }"}`
- Verdict reason: matches expected.

## TC-22 — Empty or absent body is a 400: PASS
- Command: (a) `POST {}`; (b) `POST` with `Content-Type: application/json` and no body
- Observed: both `400 {"error":"Expected { source, target, relationship }"}`
- Verdict reason: matches expected (also satisfies the case's "any 400 with {error} acceptable" fallback for (b)).

## TC-23 — Non-string source or target is a 400: PASS
- Command: four payloads (`source:1`, `target:null`, `source` as array, `target` as object)
- Observed: all four `400 {"error":"Expected { source, target, relationship }"}`; nothing created (`GET /connections` `[]`).
- Verdict reason: matches expected.

## TC-24 — Unsupported relationship is rejected: PASS
- Command: `POST {"source":"S01","target":"S02","relationship":"caused_by"}`
- Observed: `422 {"error":"That kind of link isn't supported."}`; nothing created.
- Verdict reason: matches R-REL exactly.

## TC-25 — Non-string relationship is a 400: PASS
- Command: `POST {"source":"S01","target":"S02","relationship":5}`
- Observed: `400 {"error":"Expected { source, target, relationship }"}`; nothing created.
- Verdict reason: matches expected.

## TC-26 — Empty-string ids (spec ambiguity): PASS
- Command: `POST {"source":"","target":"S02"}` and `POST {"source":"S01","target":""}`
- Observed: both `422 {"error":"One of those items isn't in this case."}`; nothing created.
- Verdict reason: matches the case's stated "per source" expectation (422, not 400) exactly — no ambiguity to flag.

## TC-27 — Rejection messages never say whether a link is right or wrong: PASS
- Command: collected error strings for unknown id, locked exhibit, self-link (two different suspects), same-dir dup, reverse dup, a second distinct-pair dup (`E002/S03`), unsupported relationship, missing field
- Observed: `unknown-id` and `locked-exhibit` both `"One of those items isn't in this case."`; both self-links `"An item can't be linked to itself."`; all three duplicate variants `"Those two are already linked."`; unsupported relationship `"That kind of link isn't supported."`; missing field `"Expected { source, target, relationship }"`. Every body was exactly `{"error": "..."}`, no extra keys. None of the strings contain any of the forbidden words (right/wrong/correct/incorrect/culprit/guilty/innocent/relevant/irrelevant/contradict/solution).
- Verdict reason: matches expected — byte-identical strings per rejection type regardless of which ids triggered them.

## TC-28 — Successful creation carries no verdict: PASS
- Command: three link creations (`S01/S02`, `S03/L01`, `ST01-A/S01`)
- Observed: each `201` body has exactly `id`, `source`, `target`, `relationship` — no `correct`/`valid`/`score`/`hint`/`message` keys.
- Verdict reason: matches expected.

## TC-29 — Rejected requests change no state: PASS
- Command: created `S01/S02` (`C01`), then sent one unknown-id, self-link, same-dir dup, reverse dup, missing-field and unsupported-relationship request, then `GET /connections` and `GET /investigation`
- Observed: both still show exactly `[{"id":"C01","source":"S01","target":"S02",...}]`; a subsequent valid POST (`S03/S04`) got id `C02`.
- Verdict reason: matches expected — rejected requests did not consume an id.

## TC-30 — DELETE returns 204 and removes the link from GET /connections: PASS
- Command: created `C01` (`S01/S02`) and `C02` (`S03/S04`), `DELETE /connections/C01`, then `GET /connections`
- Observed: DELETE returned `204` with empty body; GET returned `[{"id":"C02",...}]` only.
- Verdict reason: matches expected.

## TC-31 — DELETE removes the link from investigation.connections: PASS
- Command: `GET /investigation` after TC-30's delete
- Observed: `connections` is exactly `[{"id":"C02",...}]`, matching `GET /connections`.
- Verdict reason: matches expected.

## TC-32 — DELETE of an unknown id is a 404: PASS
- Command: with only `C01` present, `DELETE /connections/C99` then `DELETE /connections/abc`
- Observed: both `404 {"error":"Connection not found"}`; `GET /connections` still has `C01`.
- Verdict reason: matches R-404 exactly.

## TC-33 — Deleting the same id twice: PASS
- Command: `DELETE /connections/C01` twice
- Observed: first `204`, second `404 {"error":"Connection not found"}`
- Verdict reason: matches expected.

## TC-34 — New connection after a delete gets a fresh, unique id: PASS
- Command: create A (`C01`, S01/S02), B (`C02`, S03/S04), delete B, create D (`S05/L01`)
- Observed: D is `C03`; `GET /connections` contains only `C01` and `C03`, no `C02`.
- Verdict reason: matches expected (observed sequence `C01, C02, then C03` as documented).

## TC-35 — Deleting a lower id does not cause a collision: PASS
- Command: create A(`C01`), B(`C02`), C(`C03`); delete A; create fourth (`S05/L03`)
- Observed: fourth link is `C04`; `GET /connections` shows `C02, C03, C04` — three distinct ids, no `C01`.
- Verdict reason: matches expected.

## TC-36 — Same pair can be linked again after deletion: PASS
- Command: create `S01/S02` (`C01`), delete it, `POST {"source":"S02","target":"S01"}`
- Observed: `201`, new id `C02` (differs from deleted `C01`); `GET /connections` has exactly one item.
- Verdict reason: matches expected.

## TC-37 — Connections are isolated per case: PASS
- Command: create `S01/S02` in 047; `GET /connections` and `GET /investigation` on fresh 048; `POST` the same pair in 048; `GET /connections` on 047
- Observed: 048's GET returns `[]` / `connections:[]`; 048's POST is `201` (`C01`, not a duplicate); 047 still shows exactly one item.
- Verdict reason: matches expected.

## TC-38 — DELETE only acts within the case in the URL: PASS
- Command: 047 has `C01`, 048 fresh (no links); `DELETE /cases/048/connections/C01`; then re-check 047
- Observed: `404 {"error":"Connection not found"}` for 048's delete; 047's `C01` still present afterward.
- Verdict reason: matches the "no link in 048" branch of the expected result exactly.

## TC-39 — Reset clears all connections: PASS
- Command: 047 with two links (incl. one on `E002` after Setup A), `POST /reset`, then `GET /connections` and `GET /investigation`
- Observed: `POST /reset` returns `200` with `connections: []` in the body; `GET /connections` is `[]`; `investigation.connections` is `[]`.
- Verdict reason: matches expected.

## TC-40 — Post-reset creation works and starts over: PASS
- Command: after TC-39, `POST {"source":"S01","target":"S02"}` and `POST {"source":"E002","target":"S02"}`
- Observed: first is `201` with id `C01` (counter reset); second is `422 {"error":"One of those items isn't in this case."}` (evidence re-locked by reset).
- Verdict reason: matches expected exactly, including the observed `C01` id.

## TC-41 — Reset of one case leaves other cases' connections alone: PASS
- Command: 047 and 048 each given one link; `POST /cases/047/reset`; `GET /cases/048/connections`
- Observed: 048 still returns its one unchanged link.
- Verdict reason: matches expected.

## TC-42 — Unknown caseId is a 404 on all three connection routes: PASS
- Command: `GET /cases/999/connections`, `POST /cases/999/connections {...}`, `DELETE /cases/999/connections/C01`
- Observed: all three return `404 {"error":"Case not found"}`; case 047's connections list unaffected afterward.
- Verdict reason: matches expected (error body has the required `{error: <string>}` shape).

## TC-43 — Connections can still be created after the case is closed: PASS
- Command: fresh 047, `POST /conclusion {"suspectId":null}` (confirmed `investigation.conclusion` became non-null, `ending: "criminal_escapes"`), then `POST /connections {"source":"S01","target":"S02"}`
- Observed: `201`, `{"id":"C01","source":"S01","target":"S02","relationship":"linked_to"}` — same shape as TC-03, no `422 "This case is closed."`
- Verdict reason: confirms the source-reading claim that `createConnection` never calls `assertOpen()`; matches expected exactly.

## TC-44 — Connections can still be deleted after the case is closed: PASS
- Command: continuing TC-43 (closed case), `DELETE /connections/C01`
- Observed: `204`, empty body; `GET /connections` no longer contains `C01`.
- Verdict reason: matches expected — `deleteConnection` is likewise not gated behind `assertOpen()`.

## TC-45 — Creating or deleting a connection does not spend time on the case clock: PASS
- Command: fresh 047, `GET /investigation` (clock snapshot), `POST /connections {...}`, `GET /investigation`, `DELETE /connections/<id>`, `GET /investigation`
- Observed: `clock` object (`start`, `deadline`, `now`, `minutesUsed:0`, `minutesLeft:960`, `timeUp:false`) byte-identical across all three GETs.
- Verdict reason: matches expected — no `TIME_COST` entry consumed for connection create/delete.

## TC-46 — Connections remain postable once the clock has run out but no accusation has been filed yet: PASS
- Command: fresh 047, alternated `POST /travel` between `L01`/`L02` for 32 calls until `GET /investigation` reported `clock.timeUp:true` (`minutesUsed:960`, `minutesLeft:0`), without calling `POST /conclusion`; then `POST /connections {"source":"S03","target":"S04"}`
- Observed: `201`, `{"id":"C01","source":"S03","target":"S04","relationship":"linked_to"}` — no `422 "Time is up. The District Attorney wants a name."`
- Verdict reason: matches expected — `createConnection` is not gated by `clock.timeUp`, unlike `travel`/`search`/`readPage`.

## TC-47 — [UI] Pinning a card from the panel adds it to the board and to localStorage: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: no browser automation tool (`mcp__plugin_playwright_playwright__*` / `mcp__Claude_Browser__*`) present in this session's toolset; per the rules this is BLOCKED, not guessed from source.

## TC-48 — [UI] Selecting two pinned cards and confirming creates a real connection and draws a line: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## TC-49 — [UI] Cancelling a pending link makes no API call: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## TC-50 — [UI] Refreshing the page keeps both the server connections and the pinned layout: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## TC-51 — [UI] Deleting a link via the panel removes the line and the server record, but leaves both cards pinned: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## TC-52 — [UI] A card that is part of a live connection cannot be unpinned until the connection is removed: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## TC-53 — [UI] Board layout does not leak between cases: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## TC-54 — [UI] Locked evidence never appears in the PIN TO BOARD dropdown: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## TC-55 — [UI] New Investigation / Play Again clears both the server connections and the localStorage layout: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## TC-56 — [UI] A raw API reset (outside the UI) does not clear the browser's board layout: BLOCKED
- Command / action run: none — no browser automation tool available in this run
- Observed: n/a
- Verdict reason: same as TC-47.

## Summary
Total: 56 | Pass: 46 | Fail: 0 | Blocked: 10

Original cases (TC-01–TC-42): 42 total, 42 pass, 0 fail, 0 blocked.
Added cases (TC-43–TC-56, 2026-09-24): 14 total, 4 pass (TC-43–TC-46, all API-level), 0 fail, 10 blocked (TC-47–TC-56, all UI-level, no browser automation tool available in this run).

Failures needing attention: none.

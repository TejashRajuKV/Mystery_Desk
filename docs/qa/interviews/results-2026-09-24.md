# Results — interviews
Run at: 2026-09-24T16:27:07Z
Backend reachable: yes (isolated instance on http://localhost:4205, DB at scratchpad/qa/interviews.sqlite — not the owner's :4000 save)
Frontend: not started. No browser automation tool (`mcp__plugin_playwright_playwright__*` or `mcp__Claude_Browser__*`) was present in this session's tool list, so every UI-marked case below is BLOCKED per the runner's rules rather than guessed from source.

All API calls below were run against `http://localhost:4205/api`. Case is 047 for every case except TC-02, TC-03 and TC-10, which also hit 048–051.

---

## TC-01 — Suspects list shape for Case 047: PASS
- Command / action run: `GET /cases/047/suspects`
- Observed: 200; array of 5 objects, ids `S01`..`S05` in order, names `Dr. Maren Voss`, `Alex Reyes`, `Victor Lang`, `Nina Okafor`, `Daniel Cho`, aliases `The Architect`, `The Technician`, `The Chairman`, `The Night Watch`, `The Wire`; all have non-empty `role`/`motive`.
- Verdict reason: matches expected shape and values exactly.

## TC-02 — Every case has five suspects: PASS
- Command / action run: `GET /cases/{047,048,049,050,051}/suspects`
- Observed: each 200, each an array of exactly 5 objects with valid `id` pattern and non-empty `name`/`alias`/`role`/`motive`; ids unique per case.
- Verdict reason: matches.

## TC-03 — No answer-key data in suspect, statement or dialogue responses: PASS
- Command / action run: for each case 047–051, `GET .../suspects`, `GET .../statements`, and `GET .../dialogue/{suspectId}` for every suspect id; grep raw text case-insensitively for `culprit|guilty|solution|requiredEvidence|requiredConnections`.
- Observed: all calls 200; grep across all captured response files found no matches.
- Verdict reason: no leaked answer-key terms or keys found.

## TC-04 — Single suspect fetch and 404s: PASS
- Command / action run: `GET /cases/047/suspects/S02`, `GET /cases/047/suspects/S99`, `GET /cases/999/suspects/S02`
- Observed: (1) 200, S02 object with `name: "Alex Reyes"`, `alias: "The Technician"`, `role: "Senior Lab Technician"`. (2) 404 `{"error":"Suspect not found"}`. (3) 404 `{"error":"Case not found"}`.
- Verdict reason: matches expected exactly.

## TC-05 — Statements list shape for Case 047: PASS
- Command / action run: `GET /cases/047/statements`
- Observed: 200; 5 statements, `suspectId` S01..S05, ids ST01..ST05, assertion ids matching spec (ST02 has ST02-A/B/C etc.); `ST02-A.claim` = `"I left the building at 21:00 and went straight home."`
- Verdict reason: matches exactly.

## TC-06 — Statement assertions leak no kind or parameters: PASS
- Command / action run: `GET /cases/047/statements`, inspected keys programmatically and grepped raw text for forbidden key names.
- Observed: statement objects have only `id, suspectId, takenAt, text, assertions`; assertion objects have only `id, claim`; no `kind/severity/mitigatedBy/note/actions/target/from/to/time` substrings found.
- Verdict reason: matches exactly.

## TC-07 — Opening dialogue node for a fresh interview (S02): PASS
- Command / action run: `POST /cases/047/reset`, then `GET /cases/047/dialogue/S02`
- Observed: 200; `nodeId: "a-start"`, `speaker: "S02"`, `speakerName: "Alex Reyes"`, `mood: "neutral"`, exact `narration` and `text` match, `canPresent: true`, choice ids in order exactly `a-start-friday, a-start-prototype, a-start-parked, a-start-access, a-start-voss, a-start-nina, a-start-leave`; only `a-start-leave` has `leaves:true`; no `next/requires/consequences/present` keys anywhere.
- Verdict reason: matches expected exactly.

## TC-08 — Choices and text follow story state; GET changes nothing: PASS
- Command / action run: `GET /cases/047/dialogue/S01` twice, `GET /investigation`, `POST /cases/047/file/F1/read`, `GET /cases/047/dialogue/S01`
- Observed: both GETs identical (`nodeId: "v-start"`, `mood: "angry"`, text starting `"You haven't even read the incident report, have you?"`, choices `v-start-prototype, v-start-kw, v-start-alex, v-start-accuse, v-start-leave`); `minutesUsed` 0 before read. After reading F1: `minutesUsed` 20, `storyFlags["file.F1"]` true. After: `mood: "suspicious"`, text starting `"Six years of my life were in that case, Detective."`, choices `v-start-discovery, v-start-prototype, v-start-kw, v-start-alex, v-start-accuse, v-start-leave`.
- Verdict reason: matches expected exactly.

## TC-09 — Dialogue for unknown suspect or case is a 404: PASS
- Command / action run: `GET /cases/047/dialogue/S99`, `GET /cases/999/dialogue/S02`
- Observed: (1) 404 `{"error":"Suspect not found"}`. (2) 404 `{"error":"Case not found"}`.
- Verdict reason: matches.

## TC-10 — Every suspect in every case has a well-formed opening node: PASS
- Command / action run: reset all 5 cases, `GET .../suspects` then `GET .../dialogue/{id}` for all 25 suspect/case combinations, validated shape programmatically.
- Observed: all 25 calls 200; every body has correct `suspectId`, non-empty `nodeId`, `speaker` in that case's suspect ids, `speakerName` matching, `mood` in the allowed set, non-empty `text`, `narration` string-or-null, boolean `canPresent`, choices array with unique ids, exact key set `{id,label,asked,leaves}`, and at least one `leaves:true` choice.
- Verdict reason: matches expected exactly.

## TC-11 — Malformed choice bodies are a 400: PASS
- Command / action run: `POST /cases/047/dialogue/S05/choice` with `{}`, `{"choiceId":5}`, `{"presentEvidenceId":7}`, both keys together, and no body.
- Observed: all five returned 400 with body exactly `{"error":"Expected { choiceId } or { presentEvidenceId }"}`; `GET /investigation` afterward showed `minutesUsed` 0, `interviewedSuspects` `[]`.
- Verdict reason: matches exactly.

## TC-12 — Choice for an unknown suspect is a 404: PASS
- Command / action run: `POST /cases/047/dialogue/S99/choice {"choiceId":"c-start-prototype"}`
- Observed: 404 `{"error":"Suspect not found"}`.
- Verdict reason: matches.

## TC-13 — Cannot question someone who is not where the player is: PASS
- Command / action run: `POST .../dialogue/S05/choice` before travel, then `POST /travel {"locationId":"L01"}`, repeat S05 choice, then `POST .../dialogue/S02/choice {"choiceId":"a-start-friday"}`.
- Observed: (1) 422 `{"error":"They aren't here. Go and find them."}`. (2) travel 200, `minutesUsed` 30; repeated S05 choice still 422 same body; `GET dialogue/S05` still `nodeId: "c-start"`, `minutesUsed` still 30. (3) 200 with `dialogue.nodeId: "a-friday"`.
- Verdict reason: matches exactly.

## TC-14 — Asking a question: new node, asked flag, time, interview bookkeeping: PASS
- Command / action run: reset, travel L02, `POST .../dialogue/S05/choice {"choiceId":"c-start-prototype"}`, then `GET .../dialogue/S05`.
- Observed: POST 200 with keys `dialogue, ended, presented, effects, investigation`; `ended:false`, `presented:null`, `effects` = `{"unlockedEvidence":[],"contradictions":[]}`; `dialogue.nodeId "c-prototype"`, speaker/speakerName/mood/text all match, single choice `c-prototype-back` (`asked:false, leaves:false`); `minutesUsed` 40, `minutesLeft` 920; `interviewedSuspects` `["S05"]`; `interviewLeads.S05` 2. Follow-up GET matches the POST's `dialogue`.
- Verdict reason: matches expected exactly.

## TC-15 — Returning to the opening line is free and marks the question as asked: PASS
- Command / action run: `POST .../dialogue/S05/choice {"choiceId":"c-prototype-back"}`, then `GET .../dialogue/S05`.
- Observed: `nodeId "c-start"`, `ended:false`, `minutesUsed` still 40; `c-start-prototype.asked:true`, others `asked:false`; `interviewLeads.S05` still 2.
- Verdict reason: matches exactly.

## TC-16 — Leaving the interview: PASS
- Command / action run: reset, travel L02, `POST .../dialogue/S05/choice {"choiceId":"c-start-leave"}`, then `GET .../dialogue/S05`.
- Observed: 200; `ended:true`; `dialogue.nodeId "c-start"`; `minutesUsed` still 30; follow-up GET shows `c-start-leave` `asked:false, leaves:true`.
- Verdict reason: matches exactly.

## TC-17 — Re-asking an already-asked question (spec ambiguity): PASS
- Command / action run: from TC-15's state, `POST .../dialogue/S05/choice {"choiceId":"c-start-prototype"}` again.
- Observed: 200, `dialogue.nodeId "c-prototype"`, `minutesUsed` 50 (charged the full 10 minutes again, as the spec-ambiguity note in the test case predicted the current code would do); `interviewLeads.S05` unchanged at 2.
- Verdict reason: matches the documented AMBIGUITY note's predicted observed value (50); recorded as-is per the runner instruction, not treated as a silent pass/fail dispute.

## TC-18 — Interview progress lives on the server and survives a backend restart: PASS
- Command / action run: reset, travel L02, `POST .../dialogue/S05/choice {"choiceId":"c-start-camera"}`, `GET .../dialogue/S05`; killed and restarted the isolated backend process (same `DB_PATH`, port 4205); then `GET .../dialogue/S05` and `GET .../investigation` again.
- Observed: dialogue GET before and after restart both `nodeId "c-camera"`, text starting `"Recorder three? Service mode happens."`; after restart `investigation.locationId "L02"`, `clock.minutesUsed 40`, `unlockedEvidence` still contains `E010`.
- Verdict reason: matches exactly; state genuinely persisted across a real process restart against the same SQLite file.

## TC-19 — A choice with an unlock consequence hands over the exhibit: PASS
- Command / action run: reset, travel L02 (`minutesUsed 30`); `GET .../evidence` and `GET .../evidence/E010` confirmed empty/404; then `c-start-camera`, `c-camera-back`, `c-start-camera` again, then `GET .../evidence` and `GET .../evidence/E010`.
- Observed: (1) `effects.unlockedEvidence` = `[{"id":"E010","title":"Modem Dial-In Log"}]`; `investigation.unlockedEvidence` contains E010; `nodeId "c-camera"`. (2) `nodeId "c-start"`, `effects.unlockedEvidence []`. (3) `effects.unlockedEvidence []` (not re-reported), `unlockedEvidence` still has E010 once. (4) evidence list has exactly one item `id:"E010"`; single fetch 200.
- Verdict reason: matches exactly.

## TC-20 — A choice id that is not on the current node is a 422: PASS
- Command / action run: reset, travel L02, `c-start-camera`; then `POST .../dialogue/S05/choice` with `{"choiceId":"c-start-management"}`, `{"choiceId":"does-not-exist"}`, `{"choiceId":""}`.
- Observed: all three 422 with body exactly `{"error":"That isn't something you can do at this point in the interview."}`; afterward `GET dialogue/S05` still `nodeId "c-camera"`, `minutesUsed` 40, no `grudge.S05` flag.
- Verdict reason: matches exactly.

## TC-21 — Unmet-requires and evidence-reaction ids are refused with the same generic reason: PASS
- Command / action run: reset, travel L01 (`minutesUsed 30`); `POST .../dialogue/S01/choice {"choiceId":"v-start-discovery"}`, `{"choiceId":"v-start-e017"}`, `POST .../dialogue/S02/choice {"choiceId":"a-start-latch"}`, `{"choiceId":"a-start-e014"}`.
- Observed: all four 422 with body exactly `{"error":"That isn't something you can do at this point in the interview."}`; `minutesUsed` remained 30; S01 still on `v-start`, S02 still on `a-start`.
- Verdict reason: matches exactly.

## TC-22 — A consequence flag locks routes and changes how the person reacts (S01 turns hostile): PASS
- Command / action run: reset, travel L01; `POST .../dialogue/S01/choice {"choiceId":"v-start-accuse"}`, `{"choiceId":"v-accused-leave"}`, `GET .../dialogue/S01`, `{"choiceId":"v-start-alex"}`, `{"choiceId":"v-start-accuse"}`.
- Observed: (1) `nodeId "v-accused"`, `mood "angry"`, `storyFlags["hostile.S01"] true`, `minutesUsed 40`, only choice `v-accused-leave leaves:true`. (2) `ended:true`, `minutesUsed` still 40, back to `v-start`. (3) `mood "angry"`, text `"I said we were finished. Ask your questions and go."`, choices exactly `v-start-prototype, v-start-kw, v-start-leave` (no alex/accuse). (4) and (5) both 422 with the generic message, no state change.
- Verdict reason: matches exactly.

## TC-23 — `evidenceViewed` requirement needs the exhibit examined, not just unlocked (S03): PASS
- Command / action run: reset; travel L05, search `L05-exec` (E008), travel L04 (`minutesUsed 75`); `GET dialogue/S03`; `POST .../choice {"presentEvidenceId":"E008"}`; `{"choiceId":"l-e008-back"}` then GET; `POST /viewed {"type":"evidence","id":"E009"}` then GET; `POST .../choice {"choiceId":"l-start-sale"}`.
- Observed: (2) choices exactly `l-start-friday, l-start-prototype, l-start-voss, l-start-leave` (no `l-start-sale`). (3) `presented.id "E008"`, `reaction "reaction"`, `nodeId "l-e008"`, `effects.unlockedEvidence` contains E009, `effects.contradictions` exactly one item `ST03-A`/`E008`, `minutesUsed 85`. (4) `nodeId "l-start"`, `l-start-sale` still absent although `unlockedEvidence` has E009 and `evidenceViewed` didn't yet. (5) `/viewed` 200 with E009 added to `evidenceViewed`; next GET lists `l-start-sale` between `l-start-voss` and `l-start-leave`. (6) 200, `nodeId "l-sale"`, `effects.unlockedEvidence` contains E016, `minutesUsed 95`.
- Verdict reason: matches exactly.

## TC-24 — Presenting an unlocked exhibit: reaction, viewed, time, contradiction, flag, unlock (S05, E010): PASS
- Command / action run: reset, travel L02, search `L02-modem`; confirmed `nodeId "c-start"`, `canPresent true`, `evidenceViewed []`, `minutesUsed 45`; then `POST .../dialogue/S05/choice {"presentEvidenceId":"E010"}`.
- Observed: `presented` = `{"id":"E010","title":"Modem Dial-In Log","reaction":"reaction"}`; `nodeId "c-e010"`, `mood "surprised"`, `ended:false`; `evidenceViewed` contains E010; `minutesUsed 55`; `effects.contradictions` exactly one item matching `assertionId ST05-A`, `evidenceId E010`, `suspectId S05`, claim and explanation text verbatim as specified; `effects.unlockedEvidence` contains E006; `contradictionsFound` contains the same pair; `storyFlags["confessed.S05"] true`; choices are exactly `c-e010-who, c-e010-back`.
- Verdict reason: matches exactly.

## TC-25 — Person reacts to story state with a variant line (S05 after confessing): PASS
- Command / action run: from TC-24's state, `POST .../dialogue/S05/choice {"choiceId":"c-e010-back"}` then `GET dialogue/S05`; separately reset and `GET dialogue/S05` fresh.
- Observed: after back — `nodeId "c-start"`, `mood "defeated"`, `narration "Daniel Cho has stopped talking quite so fast."`, `text "Ask me anything. I did the ticket. That's all I did. I've got nothing left to hide."` Fresh state — `mood "nervous"`, text starting `"I was at home all night, okay?"`
- Verdict reason: matches exactly.

## TC-26 — Presenting an exhibit the person has no reaction to gets the "unmoved" line: PASS
- Command / action run: re-established TC-24/25's state (reset, travel L02, search modem, present E010, back to c-start), then `POST .../dialogue/S05/choice {"presentEvidenceId":"E006"}`.
- Observed: `presented.id "E006"`, `reaction "unmoved"`; `nodeId "c-unmoved"`, text `"That's not... I mean, that's not mine. Is it? No. I've never seen that."`; `minutesUsed 65`; `evidenceViewed` contains E006; `effects` = `{"unlockedEvidence":[],"contradictions":[]}`; `contradictionsFound` length unchanged (1, from the earlier E010 presentation).
- Verdict reason: matches exactly.

## TC-27 — Presenting is refused for locked, unknown, or not-presentable situations: PASS
- Command / action run: reset, travel L02, search `L02-modem`; `POST .../choice {"presentEvidenceId":"E005"}` (locked), `{"presentEvidenceId":"E999"}` (nonexistent), `{"choiceId":"c-start-camera"}` then `{"presentEvidenceId":"E010"}`.
- Observed: (1) and (2) both 422 `{"error":"That exhibit isn't in your case file."}` — identical body for locked vs nonexistent. (3) first POST 200 (`minutesUsed 55`); second 422 `{"error":"That isn't something you can do at this point in the interview."}`. Final `minutesUsed 55`, `evidenceViewed []`, `contradictionsFound []`.
- Verdict reason: matches exactly.

## TC-28 — One exhibit can expose two claims; a route can lock (S02, E014): PASS
- Command / action run: reset; `POST file/F2/read`; travel L03, search `L03-reader`, travel L01 (`minutesUsed 95`); `POST .../dialogue/S02/choice {"presentEvidenceId":"E014"}`; `{"choiceId":"a-e014-press"}` then `{"choiceId":"a-e014-press-back"}`; `GET dialogue/S02`; `{"choiceId":"a-start-parked"}`.
- Observed: (2) `nodeId "a-e014"`, `effects.contradictions` exactly two items `ST02-A`/`E014` and `ST02-B`/`E014`, `contradictionsFound` contains both, `minutesUsed 105`. (3) after first POST `storyFlags["lawyered.S02"] true`, `minutesUsed 115`; after second, `nodeId "a-start"`, `minutesUsed` still 115. (4) `mood "defensive"`, text `"My lawyer says I've answered enough about cards and doors. I'll help where I can."`; choices exactly `a-start-friday, a-start-prototype, a-start-access, a-start-nina, a-start-leave` (no parked/voss). (5) 422 with the generic message.
- Verdict reason: matches exactly.

## TC-29 — After the case is closed, interview choices are a 422: PASS
- Command / action run: reset, travel L02, search `L02-modem`; `POST conclusion {"suspectId":null,"evidenceIds":[]}`; `POST .../dialogue/S05/choice {"choiceId":"c-start-prototype"}`; `{"presentEvidenceId":"E010"}`; `{}`; `GET investigation`; then `POST reset` and `POST .../dialogue/S05/choice {"choiceId":"c-start-prototype"}` (no travel).
- Observed: (1) 200. (2) and (3) both 422 `{"error":"This case is closed. The interviews are over."}`. (4) 400 `{"error":"Expected { choiceId } or { presentEvidenceId }"}`. (5) `conclusion` non-null, `minutesUsed 45`, `evidenceViewed []`, `contradictionsFound []`, `interviewedSuspects []`. After reset+no-travel, the same choice is 422 `{"error":"They aren't here. Go and find them."}`.
- Verdict reason: matches exactly.

## TC-30 — Log a real contradiction manually; duplicates are recorded once: PASS
- Command / action run: reset, travel L02, search `L02-modem`; `POST contradictions {"assertionId":"ST05-A","evidenceId":"E010"}` twice; `GET investigation`.
- Observed: both POSTs 200 with `assertionId ST05-A`, `evidenceId E010`, `suspectId S05`, exact `claim`/`explanation` text, no `severity` key; `contradictionsFound` length 1 with that pair once.
- Verdict reason: matches exactly.

## TC-31 — Interview-revealed contradictions are real pairs and never duplicate: PASS
- Command / action run: re-established TC-24's state; `POST contradictions {"assertionId":"ST05-A","evidenceId":"E010"}`; `POST .../choice {"choiceId":"c-e010-back"}` then `{"presentEvidenceId":"E010"}` again; `GET investigation`.
- Observed: (1) 200 (accepted as real). (2) `effects.contradictions []` (not re-announced), `presented.reaction "reaction"`. (3) `contradictionsFound` length 1; the pair was accepted by `POST contradictions` with 200 in step (1).
- Verdict reason: matches exactly.

## TC-32 — A pair that is not a real contradiction is a 422 and is not recorded: PASS
- Command / action run: reset, travel L02, search modem and recorder (E010 and E005); `POST contradictions {"assertionId":"ST05-A","evidenceId":"E005"}`; `{"assertionId":"ST01-A","evidenceId":"E010"}`.
- Observed: both 422 `{"error":"That exhibit doesn't contradict that statement."}`; `contradictionsFound []` afterward.
- Verdict reason: matches exactly.

## TC-33 — Contradiction requests with unknown, locked or missing parts: PASS
- Command / action run: reset, travel L02, search `L02-modem` only (E005 left locked); `POST contradictions {"assertionId":"ST05-A","evidenceId":"E005"}`; `{"assertionId":"ST99-Z","evidenceId":"E010"}`; `{"assertionId":"ST05-A"}`.
- Observed: (1) 404 `{"error":"Unknown statement or exhibit"}`. (2) 404 same body. (3) 400 `{"error":"Expected { assertionId, evidenceId }"}`. `contradictionsFound` stayed `[]`.
- Verdict reason: matches the source-derived behaviour documented in the test case exactly.

## TC-34 — Suspect viewed: recorded once, in first-viewed order; GET does not record: PASS
- Command / action run: reset; `GET suspects/S04` then `GET investigation`; `POST viewed {"type":"suspect","id":"S03"}`; `{"id":"S01"}`; `{"id":"S03"}` again.
- Observed: (1) `suspectsViewed []`. (2) 200, whole investigation object, `suspectsViewed ["S03"]`. (3) `["S03","S01"]`. (4) still `["S03","S01"]` (not duplicated/moved). `minutesUsed` 0 throughout.
- Verdict reason: matches exactly.

## TC-35 — Viewed rejects unknown ids and bad bodies: PASS
- Command / action run: `POST viewed {"type":"suspect","id":"S99"}`; `{"type":"suspect"}`; `{"type":"person","id":"S01"}`.
- Observed: (1) 404 `{"error":"Nothing with that id in this case"}`. (2) and (3) both 400 `{"error":"Expected { type: \"evidence\" | \"suspect\" | \"event\", id }"}`; `suspectsViewed` remained `[]`.
- Verdict reason: matches exactly.

## TC-36 — Reset returns interviews to their opening state: PASS
- Command / action run: reproduced TC-26's precondition state (S05 interviewed, contradiction found, E006/E010 unlocked, `confessed.S05` flag set); `POST reset`; `GET dialogue/S05`; `GET investigation`.
- Observed: reset 200 (returns investigation); dialogue `nodeId "c-start"`, `mood "nervous"`, all choices `asked:false`; investigation `storyFlags {}`, `unlockedEvidence []`, `contradictionsFound []`, `evidenceViewed []`, `interviewedSuspects []`, `interviewLeads.S05 3`, `conclusion null`, `locationId null`, `minutesUsed 0`.
- Verdict reason: matches exactly.

## TC-37 — Presenting an exhibit is refused by the "not here" check before the exhibit check: PASS
- Command / action run: reset (no travel, E010 not unlocked); `POST .../dialogue/S05/choice {"presentEvidenceId":"E010"}`.
- Observed: 422 `{"error":"They aren't here. Go and find them."}`; `GET investigation` afterward showed `minutesUsed 0`, `evidenceViewed []`.
- Verdict reason: matches exactly — confirms the location check runs before the exhibit-unlocked check on the `presentEvidenceId` branch too.

## TC-38 — `GET /dialogue` stays readable after the case is closed; only `POST choice` is blocked: PASS
- Command / action run: reset, travel L02; `POST conclusion {"suspectId":null,"evidenceIds":[]}`; `GET dialogue/S05`; `GET investigation`.
- Observed: (1) 200. (2) 200 (not 422) — `nodeId "c-start"`, `speaker "S05"`, `speakerName "Daniel Cho"`, `mood "nervous"`, same shape as before the case closed. (3) `conclusion` non-null; `minutesUsed` still 30.
- Verdict reason: matches exactly — the closed-case rule applies only to `POST /choice`.

## TC-39 — `POST /viewed {"type":"suspect"}` still records after the case is closed (current behaviour; spec ambiguity): PASS
- Command / action run: reset; `POST conclusion {"suspectId":null,"evidenceIds":[]}`; `POST viewed {"type":"suspect","id":"S01"}`; `GET investigation`.
- Observed: (1) 200. (2) 200 (whole investigation object), `suspectsViewed` gained `"S01"` — matches the AMBIGUITY note's predicted current behaviour (no closed-case guard on `recordViewed`), not a stricter 422. (3) `suspectsViewed` contains `"S01"`.
- Verdict reason: matches the documented current-behaviour expectation exactly; recorded as observed per the runner instruction.

## TC-40 — Closed case also blocks travel, so a suspect never yet met becomes unreachable: PASS
- Command / action run: reset (no travel yet); `POST conclusion {"suspectId":null,"evidenceIds":[]}`; `POST travel {"locationId":"L04"}`.
- Observed: (1) 200. (2) 422 with body exactly `{"error":"This case is closed."}` — distinct verbatim string from both the interview-closed message and the conclusion-resubmit message, confirmed by direct comparison against TC-29 and TC-9x wording.
- Verdict reason: matches exactly.

## TC-41 — The clock clamps a question's cost to the minutes left, then refuses any further question once time is up: PASS
- Command / action run: reset; 31 alternating `POST travel` calls (`L04, L01, L04, ...`, 31st = L04); `POST places/L04/search {"spotId":"L04-outbox"}`; `POST dialogue/S03/choice` with `l-start-friday`, `l-friday-back`, `l-start-prototype`, `l-prototype-back`, `l-start-voss`.
- Observed: after 31 travels, `minutesUsed 930`, `locationId L04`. Search → `945`. `l-start-friday` → `955`, `minutesLeft 5`. `l-friday-back` → free, `955`, back on `l-start`. `l-start-prototype` (clamp test) → 200, `nodeId "l-prototype"`, `minutesUsed` exactly `960` (not 965), `minutesLeft 0`, `timeUp true`. `l-prototype-back` → 200, `minutesUsed` still `960`. `l-start-voss` → 422 `{"error":"Time is up. The District Attorney wants a name."}`; follow-up `GET dialogue/S03` still on `l-start`, `minutesUsed` still `960`.
- Verdict reason: matches exactly — the clock-budget clamp from commit b45dd98 behaves as documented.

## TC-42 — Interview scene content matches the dialogue API — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: cannot observe rendered UI without a browser tool; not guessed from source.

## TC-43 — Number keys 1–9 pick a choice, same as clicking it — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: same as TC-42.

## TC-44 — The UI never lets the player interview someone who isn't at their current place — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: same as TC-42.

## TC-45 — The evidence tray lists only unlocked exhibits — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: same as TC-42.

## TC-46 — Presenting an exhibit shows the presentation moment before the reaction — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: same as TC-42.

## TC-47 — People page lists exactly the 5 suspects from the API — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: same as TC-42.

## TC-48 — Statement claim-test control checks a claim and shows the real result — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: same as TC-42. (Note: the underlying backend behaviour this control surfaces was independently confirmed via TC-30/TC-32's exact wording checks.)

## TC-49 — A contradiction confirmed via the People page survives a refresh — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: same as TC-42.

## TC-50 — An interview choice sent with the backend down shows an error, not a frozen scene — [UI]: BLOCKED
- Command / action run: not run.
- Observed: no browser automation tool available in this run.
- Verdict reason: same as TC-42.

---

## Summary
Total: 50 | Pass: 41 | Fail: 0 | Blocked: 9

By section:
- Original cases (TC-01–TC-36): 36 total — Pass: 36 | Fail: 0 | Blocked: 0
- Added cases (TC-37–TC-50): 14 total — Pass: 5 | Fail: 0 | Blocked: 9

Failures needing attention: none.

Notes:
- Every original case ran cleanly against current source with no discrepancy against fix commit b45dd98 or any other prior fix — no outdated-expectation FAILs to report.
- All 9 UI cases (TC-42–TC-50) are BLOCKED solely because no browser automation tool (`mcp__plugin_playwright_playwright__*` / `mcp__Claude_Browser__*`) was present in this run's toolset; no frontend was started since it would have been pointless without a browser driver. They should be re-run in a session with browser automation available.
- This run used an isolated backend on port 4205 with its own SQLite file (`scratchpad/qa/interviews.sqlite`), separate from the owner's :4000 save, including one full stop/restart of that isolated process for TC-18. The owner's real save on :4000 was never touched.

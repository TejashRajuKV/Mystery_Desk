# Results — evidence-collection
Run at: 2026-09-24T16:20:00Z (isolated run)
Backend reachable: yes (isolated instance on http://localhost:4204, DB `evidence-collection.sqlite`, started fresh for this run)

Notes on this run:
- Per the runner's overrides, this run used its own backend on port 4204 / its own SQLite
  file, not the owner's :4000 save. All API calls below use `BASE = http://localhost:4204/api`.
- No browser automation tool (`mcp__plugin_playwright_playwright__*` or `mcp__Claude_Browser__*`)
  was present in this session's tool list, so every `[UI]` case (TC-31 through TC-38) is
  recorded `BLOCKED` per the standing rule, and no frontend process was started for this run.
- Test cases mutate shared investigation state, so cases were executed in the order the
  test-cases file lists them, re-establishing each case's stated precondition (reset / Setup A /
  Setup B) exactly as written before running its steps.

## TC-01 — [API] Fresh investigation: evidence list is empty: PASS
- Command / action run: `POST /cases/047/reset`; `GET /cases/047/evidence`
- Observed: 200, `Content-Type: application/json; charset=utf-8`, body `[]`
- Verdict reason: Matches expected exactly.

## TC-02 — [API] Fresh investigation: real exhibit ids are 404: PASS
- Command / action run: `GET /cases/047/evidence/E014`, `GET /cases/047/evidence/E001` (fresh case)
- Observed: Both 404, body `{"error":"Evidence not found"}`
- Verdict reason: Matches expected exactly, no extra keys.

## TC-03 — [API] Unknown or malformed exhibit ids are the same 404 as locked ones: PASS
- Command / action run: Setup A, then `GET /evidence/E999`, `/abc`, `/e001`, `/E014`; `GET /evidence/E001`
- Observed: First four all 404 `{"error":"Evidence not found"}`; `E001` returns 200 with full detail shape.
- Verdict reason: Locked/unknown/malformed ids are indistinguishable, as expected.

## TC-04 — [API] Unknown case is a 404 on every evidence endpoint: PASS
- Command / action run: `GET /cases/999/evidence`, `GET /cases/999/evidence/E001`, `POST /cases/999/viewed {"type":"evidence","id":"E001"}`
- Observed: All three 404 `{"error":"Case not found"}`
- Verdict reason: Matches expected exactly.

## TC-05 — [API] Exhibit found by searching a spot appears in the list with the full list shape: PASS
- Command / action run: reset; `POST travel {"locationId":"L03"}`; `POST places/L03/search {"spotId":"L03-e001"}`; `GET /evidence`
- Observed: search 200, `effects.unlockedEvidence` = `[{"id":"E001","title":"Empty Prototype Case"}]`; list has exactly one item with keys `id, type, title, timestamp, time, locationId, location, personIds, people, summary` and values matching TC-05 exactly.
- Verdict reason: Exact match.

## TC-06 — [API] Every list item matches the shape, and detail-only fields are absent from the list: PASS
- Command / action run: Setup B; `GET /cases/047/evidence`
- Observed: 9 items (E001,E002,E003,E004,E007,E012,E013,E014,E017); all fields typed as expected; `details`, `source`, `relatedEvidenceIds` absent from every item.
- Verdict reason: Matches shape requirements exactly.

## TC-07 — [API] Exhibit with a null locationId: PASS
- Command / action run: reset; travel L01; search `L01-locker`; `GET /evidence`; `GET /evidence/E015`
- Observed: List has one item E015 with `locationId: null`, `location: null`, other fields matching; detail 200 adds `details`, `source`, `relatedEvidenceIds: []`.
- Verdict reason: No 500s, `locationId`/`location` present as `null` as specified.

## TC-08 — [API] Exhibit with a null timestamp (case 048): PASS
- Command / action run: case 048 reset; travel L03; search `L03-ashtray`; `GET /cases/048/evidence`
- Observed: 200, single item E009 with `timestamp: null`, `time: null`, all other fields matching expected.
- Verdict reason: Exact match.

## TC-09 — [API] List contains only unlocked exhibits: PASS
- Command / action run: fresh 047; unlock E001, E002; `GET /cases/047/evidence`
- Observed: Exactly 2 items, ids {E001, E002}; no E013/E014/others present.
- Verdict reason: Matches expected.

## TC-10 — [API] Detail of an unlocked exhibit adds details, source and relatedEvidenceIds: PASS
- Command / action run: fresh 047; unlock E001 only; `GET /cases/047/evidence/E001`
- Observed: 200, all TC-05 list-item fields plus `details` (exact string match), `source: "Recovered by Dr. M. Voss"`, `relatedEvidenceIds: []`.
- Verdict reason: Exact match, no forbidden keys.

## TC-11 — [API] relatedEvidenceIds contains only unlocked ids and grows as exhibits are found: PASS
- Command / action run: reset; travel L03; sequential searches `L03-e001`, `L03-e002`, `L03-e013`, then file F2 read + `L03-reader`, checking `relatedEvidenceIds` after each step.
- Observed: Step 1 E001=[]; step 2 E001=["E002"], E002=[]; step 3 E001=["E002","E013"], E002=[], E013=["E001"]; step 4 E002=["E014"], E013=["E001","E014"].
- Verdict reason: Matches every expected value at every step exactly; E014 never appeared before step 4.

## TC-12 — [API] Exhibits with no place of their own unlock from a file page (case 048): PASS
- Command / action run: case 048 reset; `GET /evidence/E002` (before); `POST /file/F2/read`; `GET /evidence`; `GET /evidence/E002`
- Observed: Before: 404 `{"error":"Evidence not found"}`. Read: 200, `effects.unlockedEvidence` = `[{"id":"E002","title":"Medical Examiner's Note"}]`. List has one item matching expected fields (`locationId: null`, `personIds: []`). Detail: 200.
- Verdict reason: Exact match.

## TC-13 — [API] A gated search spot stays closed, and its exhibit stays locked, until the file page is read: PASS
- Command / action run: reset; travel L03; search `L03-reader` (before read); `GET /evidence`; `POST /file/F2/read`; search `L03-reader` again; `GET /evidence`; `GET /evidence/E014`
- Observed: First search 404 `{"error":"There is nothing like that to search here"}`; first list `[]`. After read, second search 200 with `effects.unlockedEvidence` = `[{"id":"E014","title":"Keycard Access"}]`; final list has one item E014; detail 200 with `relatedEvidenceIds: []`.
- Verdict reason: Exact match.

## TC-14 — [API] Exhibit unlocked by an interview appears in the list: PASS
- Command / action run: reset; `POST /file/F1/read`; travel L01; `GET /dialogue/S01`; `POST /dialogue/S01/choice {"choiceId":"v-start-discovery"}`; `GET /evidence`
- Observed: Choice call 200, `effects.unlockedEvidence` contains `{"id":"E014","title":"Keycard Access"}`. List has exactly one item, E014, with all fields (`type: "keycard"`, timestamp/time/locationId/location/personIds/people) matching expected.
- Verdict reason: Choice succeeded (200), so evaluated as PASS not blocked.

## TC-15 — [API] Finding the same exhibit twice does not duplicate it: PASS
- Command / action run: fresh 047; travel L03; search `L03-e001` twice; `GET /evidence`; `GET /investigation`
- Observed: Second search 200 with `effects.unlockedEvidence: []`; list still one item E001; `unlockedEvidence` in investigation is exactly `["E001"]`.
- Verdict reason: Exact match.

## TC-16 — [API] Reading the list or a detail does not mark anything viewed: PASS
- Command / action run: Setup A; `GET /evidence`; `GET /evidence/E001`; `GET /evidence/E002`; `GET /investigation`
- Observed: `evidenceViewed: []`, `progress: 0`.
- Verdict reason: Exact match.

## TC-17 — [API] Viewing records the id once, in first-viewed order: PASS
- Command / action run: Setup A; `POST /viewed` E002, then E001, then E002 again; `GET /investigation`
- Observed: Responses `["E002"]`, `["E002","E001"]`, `["E002","E001"]`; final GET `["E002","E001"]`.
- Verdict reason: Exact match — no duplicate, order preserved.

## TC-18 — [API] Viewing a locked exhibit is rejected and records nothing: PASS
- Command / action run: fresh Setup A; `POST /viewed {"type":"evidence","id":"E014"}`; `GET /investigation`; then completed Setup B and repeated the same POST.
- Observed: First call 404 `{"error":"Nothing with that id in this case"}`, `evidenceViewed` stayed `[]`. After Setup B (E014 unlocked), same call returned 200 with `evidenceViewed: ["E014"]`.
- Verdict reason: Exact match.

## TC-19 — [API] Viewing an unknown exhibit id is rejected: PASS
- Command / action run: fresh Setup A; `POST /viewed` with ids `E999`, `e001`, `""`
- Observed: All three 404 `{"error":"Nothing with that id in this case"}`; `evidenceViewed` stayed `[]`.
- Verdict reason: Exact match.

## TC-20 — [API] Bad `type` is a 400: PASS
- Command / action run: Setup A; six malformed-`type` bodies posted to `/viewed`
- Observed: All six returned 400 with body exactly `{"error":"Expected { type: \"evidence\" | \"suspect\" | \"event\", id }"}`; `evidenceViewed`/`pagesRead` unchanged (`[]`).
- Verdict reason: Exact match.

## TC-21 — [API] Missing or non-string `id` is a 400: PASS
- Command / action run: Setup A (continuing from TC-20's state); six malformed-`id` bodies plus a no-body request
- Observed: All six returned 400 with the same error string as TC-20; `evidenceViewed` stayed `[]`.
- Verdict reason: Exact match.

## TC-22 — [API] Malformed JSON body is a 400: PASS
- Command / action run: `POST /cases/047/viewed` with raw body `{"type":"evidence",`
- Observed: 400, body `{"error":"Expected double-quoted property name in JSON at position 19 (line 1 column 20)"}` (non-empty string error, not 500).
- Verdict reason: Matches expected (exact message not asserted by the spec).

## TC-23 — [API] POST /viewed returns the whole investigation state: PASS
- Command / action run: Setup A; `POST /viewed {"type":"evidence","id":"E001"}`; `GET /investigation`
- Observed: 200, body contains all expected keys with correct values (`evidenceViewed: ["E001"]`, `unlockedEvidence: ["E001","E002","E013"]`, `conclusion: null`, etc.); response body identical to the subsequent GET.
- Verdict reason: Exact match.

## TC-24 — [API] Progress is an integer 0–100 and rises with evidence viewed: PASS
- Command / action run: fresh Setup A; `GET /investigation`; then `POST /viewed` for E001, E002, E013, E001 again, reading `progress` each time.
- Observed: `0, 1, 3, 4, 4` in order, all integers.
- Verdict reason: Exact match with `round(100*(n/18)/4)` formula for n=1,2,3.

## TC-25 — [API] Progress returns to 0 after a reset: PASS
- Command / action run: `POST /cases/047/reset`; `GET /investigation`
- Observed: Reset response and subsequent GET both show `progress: 0`, `evidenceViewed: []`, `unlockedEvidence: []`.
- Verdict reason: Exact match.

## TC-26 — [API] No evidence payload contains facts or any solution field: PASS
- Command / action run: Setup B; `GET /evidence`; `GET /evidence/<id>` for all 9 exhibits; `POST /viewed` E003; `GET /investigation`; `GET /timeline` — each response walked recursively for forbidden keys via a script, and the raw list body grepped for `"facts"`/`"requiredEvidence"`.
- Observed: All responses 200; no forbidden key (`facts`, `personId`, `action`, `target`, `kind`, `solution`, `culprit`, `requiredEvidence`, `requiredConnections`) found anywhere; substring grep found nothing.
- Verdict reason: No leak found.

## TC-27 — [API] Timeline evidence references leave out locked exhibits: PASS
- Command / action run: reset; `GET /timeline`; unlock E001+E002, `GET /timeline`; unlock E013, `GET /timeline`
- Observed: Step 1: `[]`. Step 2: exactly T08 (`evidenceIds: ["E002"]`) and T12 (`evidenceIds: ["E001","E002"]`), in that order. Step 3: T08 `["E002","E013"]`, T12 `["E001","E002","E013"]`.
- Verdict reason: Exact match.

## TC-28 — [API] Reset re-locks everything and clears viewed: PASS
- Command / action run: Setup B, view E001+E014; `POST reset`; `GET /evidence`; `GET /evidence/E001`; `GET /timeline`; `GET /investigation`; re-unlock E001.
- Observed: Reset 200; list `[]`; detail 404 `{"error":"Evidence not found"}`; timeline `[]`; investigation `evidenceViewed: []`, `unlockedEvidence: []`, `progress: 0`; re-search of `L03-e001` unlocked E001 again.
- Verdict reason: Exact match.

## TC-29 — [API] Evidence state is per case: PASS
- Command / action run: reset both 047 and 048; unlock+view E001 in 047; check 048 list/detail/investigation; unlock E009 in 048; recheck 047 list; reset 048; recheck 047 list and evidenceViewed.
- Observed: 048 list `[]`, detail 404, `evidenceViewed: []`, `progress: 0`. 047 list stayed exactly `[E001]` throughout, including after the 048 unlock and after the 048 reset; 047 `evidenceViewed` stayed `["E001"]`.
- Verdict reason: Exact match — no cross-case bleed.

## TC-30 — [API] investigation.unlockedEvidence agrees with the list: PASS
- Command / action run: Setup A; `GET /investigation`; `GET /evidence`
- Observed: `unlockedEvidence: ["E001","E002","E013"]`; list ids `["E001","E002","E013"]` (same set).
- Verdict reason: Exact match.

## TC-31 — [UI] Fresh case: Evidence Room shows an empty state, not a spinner: BLOCKED
- Command / action run: none
- Observed: No `mcp__plugin_playwright_playwright__*` or `mcp__Claude_Browser__*` tool was present in this session's tool list.
- Verdict reason: No browser automation tool available in this run.

## TC-32 — [UI] A card's fields match what the API returns as the list grows: BLOCKED
- Command / action run: none
- Observed: No browser automation tool available.
- Verdict reason: No browser automation tool available in this run.

## TC-33 — [UI] Opening a card loads details and source into the inspector and marks it viewed: BLOCKED
- Command / action run: none
- Observed: No browser automation tool available.
- Verdict reason: No browser automation tool available in this run.

## TC-34 — [UI] A viewed exhibit survives a refresh, checked via the network call: BLOCKED
- Command / action run: none
- Observed: No browser automation tool available.
- Verdict reason: No browser automation tool available in this run.

## TC-35 — [UI] Type filters narrow the list, and ALL restores it: BLOCKED
- Command / action run: none
- Observed: No browser automation tool available.
- Verdict reason: No browser automation tool available in this run.

## TC-36 — [UI] A filter with zero matches shows different copy than the fresh-case empty state: BLOCKED
- Command / action run: none
- Observed: No browser automation tool available.
- Verdict reason: No browser automation tool available in this run.

## TC-37 — [UI] Backend down on first load shows FILE UNAVAILABLE with a working retry: BLOCKED
- Command / action run: none
- Observed: No browser automation tool available.
- Verdict reason: No browser automation tool available in this run.

## TC-38 — [UI] A failure loading one exhibit's detail has no retry control (documented gap): BLOCKED
- Command / action run: none
- Observed: No browser automation tool available.
- Verdict reason: No browser automation tool available in this run.

## TC-39 — [API] Closed case: travel and search are rejected, so no more evidence can ever be unlocked: PASS
- Command / action run: reset; unlock E001; `POST /conclusion {"suspectId": null, "evidenceIds": []}` (closes case, evaluated `ending: "criminal_escapes"`); then `POST /travel {"locationId":"L03"}`; `POST /places/L03/search {"spotId":"L03-e002"}`; `GET /evidence`
- Observed: Both POSTs returned 422 `{"error":"This case is closed."}`. `GET /evidence` still returned exactly one item, E001; E002 never appeared.
- Verdict reason: Exact match.

## TC-40 — [API] Closed case: reading a file page is rejected too, but the evidence endpoints stay readable: PASS
- Command / action run: (continuing from TC-39, case closed, E001 unlocked) `POST /file/F2/read`; `GET /evidence`; `GET /evidence/E001`
- Observed: Read returned 422 `{"error":"This case is closed."}`. `GET /evidence` still 200 with the one item (E001). `GET /evidence/E001` still 200 with full detail shape (`details`, `source`, `relatedEvidenceIds`).
- Verdict reason: Exact match.

## TC-41 — [API] Closed case: marking an already-unlocked exhibit as viewed still works: PASS
- Command / action run: (continuing from TC-39/40, case closed, E001 unlocked, not yet viewed) `POST /viewed {"type":"evidence","id":"E001"}`
- Observed: 200 (not 422). `evidenceViewed` became `["E001"]`, `progress` rose from 0 to 1.
- Verdict reason: Matches the case's expected result exactly; confirms the documented asymmetry (recordViewed has no closed-case check, unlike travel/search/file-read/theory/conclusion). Flagging per the case's own note: this asymmetry against spec point 4 should be confirmed with the owner as intentional, not treated as a defect by this report.

## TC-42 — [API] The clock clamps at budget, and time-up then blocks any further evidence hunting: PASS
- Command / action run: reset; `POST /file/F1/read` (20 min); 31 alternating `POST /travel` calls between L03 and L01 (30 min each = 930 min), ordered to end at L03 (starting the alternation with L03 rather than L01, since ending at L03 is required to perform the subsequent search — the test text does not pin down which of the two locations the alternation starts with, only that it is 31 calls of 30 min each). Confirmed `clock.minutesUsed: 950`, `minutesLeft: 10` before continuing. Then `POST /places/L03/search {"spotId":"L03-e001"}`; `GET /investigation`; `POST /places/L03/search {"spotId":"L03-e002"}`; `GET /evidence`.
- Observed: First search 200, `effects.unlockedEvidence` = `[{"id":"E001","title":"Empty Prototype Case"}]`. GET showed `clock.minutesUsed: 960`, `minutesLeft: 0`, `timeUp: true`. Second search returned 422 `{"error":"Time is up. The District Attorney wants a name."}`; `GET /evidence` still lacked E002.
- Verdict reason: Exact match. Note for the runner: the precondition's travel alternation had to start with L03 (not L01) to land on L03 after an odd (31) number of calls — the test text doesn't specify the starting location of the alternation, so this is a minor precondition ambiguity, not a product defect.

## Summary
Total: 42 | Pass: 34 | Fail: 0 | Blocked: 8

Original cases (TC-01 to TC-30): 30 total — Pass: 30 | Fail: 0 | Blocked: 0
Added cases (2026-09-24, TC-31 to TC-42): 12 total — Pass: 4 | Fail: 0 | Blocked: 8 (all 8 UI cases TC-31–TC-38)

Failures needing attention: none — every API case passed against current source behaviour; no expectation from the original suite conflicted with post-b45dd98 behaviour. All 8 blocked cases are UI cases blocked solely because no browser automation tool was available in this session; they need a re-run with a browser tool attached, not a code fix.

# Results — detective-notes
Run at: 2026-09-24T16:20:00Z (backend started) — testing completed same session
Backend reachable: yes (isolated instance on http://localhost:4208, DB at scratchpad/qa/detective-notes.sqlite)
Frontend: not started — no browser automation tool (`mcp__plugin_playwright_playwright__*` or `mcp__Claude_Browser__*`) was available in this session's tool list. All UI-marked cases (TC-64–TC-71) are BLOCKED per the task rules; the frontend dev server was not started since it would serve no purpose without a browser tool.

All API calls were made with `curl`/Python `urllib` against `http://localhost:4208/api`, exactly as specified in the test cases (recipes R0–R6 as defined in the conventions section). Non-ASCII apostrophe/en-dash checks were done via Python code-point inspection to avoid shell/console re-encoding artifacts.

## TC-01 — GET /notes on a fresh case returns the exact prompt list: PASS
- Command / action run: `POST /api/cases/047/reset` then `GET /api/cases/047/notes`
- Observed: 200, 8 items in the exact order/ids/labels specified. Verified `’` is U+2019 and the array is top-level (not wrapped).
- Verdict reason: Matches expected exactly.

## TC-02 — Prompt items have id/label; only connect has picks; statement prompts match suspects: PASS
- Command / action run: `GET /notes` and `GET /suspects` for cases 047, 048, 049, 050, 051 (after reset)
- Observed: For all 5 cases — every item has non-empty id/label; exactly one item (`connect`) has `picks: 2`; `statement:*` id set equals `{suspect ids}`; each label contains that suspect's name; first id `contradictions`, last id `theory`.
- Verdict reason: Matches expected for every case.

## TC-03 — Window prompts appear only once a timeline event is known: PASS
- Command / action run: `GET /notes` before R1; then `POST /travel {L05}` + `POST /places/L05/search {L05-stairs}`; `GET /notes` after.
- Observed: Before: no `window:*` id. After: exactly `window:21:00-21:30` with label `"Review the 21:00–21:30 timeline"` (en dash confirmed U+2013), positioned after all `statement:*` and before `connect`.
- Verdict reason: Matches expected exactly.

## TC-04 — GET /notes does not change investigation state: PASS
- Command / action run: `POST /reset`; `GET /investigation` (A); `GET /notes` x3; `GET /investigation` (B)
- Observed: A deep-equals B (Python dict equality on full JSON body).
- Verdict reason: Matches expected.

## TC-05 — Unknown case id is a 404 on all three notes endpoints: PASS
- Command / action run: `GET /api/cases/999/notes`, `POST /api/cases/999/notes {promptId:theory}`, `GET /api/cases/999/facts/S01`
- Observed: All three returned `404 {"error":"Case not found"}`.
- Verdict reason: Matches expected.

## TC-06 — "contradictions" prompt on a fresh case: PASS
- Command / action run: `POST /notes {"promptId":"contradictions"}` on fresh 047
- Observed: 200, V1 passes, `confidence: "low"` (not "medium" — the test-case's own hedge note about a possible "medium" mismatch did NOT reproduce), `contradiction: false`, no `contradictions` key, no `facts` key, all related lists `[]`.
- Verdict reason: Matches expected exactly; the anticipated implementation deviation was not present (likely fixed by commit b45dd98 per the "Additional cases" preamble).

## TC-07 — "statement:S02" prompt on a fresh case: PASS
- Command / action run: `POST /notes {"promptId":"statement:S02"}` on fresh 047
- Observed: 200, `confidence: "low"`, `relatedSuspects: ["S02"]` (the source echoes the prompted suspect — recording the observed ambiguity as instructed), `facts` has 1 element for S02 with exactly 3 `?` lines.
- Verdict reason: Matches expected; relatedSuspects observed as `["S02"]` per the case's request to record which of the two allowed shapes appears.

## TC-08 — "theory" prompt on a fresh case: PASS
- Command / action run: `POST /notes {"promptId":"theory"}` on fresh 047
- Observed: 200, `confidence: "low"`, `contradiction: false`, all related lists `[]`, `facts: []`.
- Verdict reason: Matches expected exactly.

## TC-09 — "connect" prompt with no shared unlocked evidence: PASS
- Command / action run: `POST /notes {"promptId":"connect","items":["S02","L05"]}` on fresh 047
- Observed: 200, `confidence: "low"`, `relatedSuspects: ["S02"]`, `relatedEvidence: []`, `relatedEvents: []`, `contradiction: false`, no `facts` key.
- Verdict reason: Matches expected.

## TC-10 — Every listed prompt in every case answers 200 in fixed shape on a fresh investigation: PASS
- Command / action run: For each of 047–051: `POST /reset`, `GET /notes`, then `POST /notes` for every prompt (connect using first suspect + first place).
- Observed: All 200. V1 passes for every response. `contradiction: false` everywhere. `relatedEvidence: []` everywhere. Non-`window:*` prompts: `confidence: "low"`, `relatedEvents: []`. Case 049 has one fresh `window:17:30-18:00` prompt (T03, no exhibit behind it) with `confidence: "high"` and `relatedEvents: ["T03"]`, matching `GET /timeline`.
- Verdict reason: Matches expected for every case.

## TC-11 — promptId and title are echoed correctly: PASS
- Command / action run: `POST /notes` for `contradictions`, `statement:S04`, `theory`; compared to `GET /notes` labels.
- Observed: `promptId` and `title` matched the requested id and its `GET /notes` label character-for-character in all 3 cases.
- Verdict reason: Matches expected.

## TC-12 — `contradictions` key present only when `contradiction` is true: PASS
- Command / action run: `POST /notes {promptId:contradictions}` fresh, then again after R2 (E007 viewed).
- Observed: Fresh: `contradiction:false`, key absent. After R2: `contradiction:true`, `contradictions` is a non-empty 1-element array.
- Verdict reason: Matches expected.

## TC-13 — Unknown promptId is a 404: PASS
- Command / action run: 7 variants (`nope`, `statement:S99`, `statement:`, `Contradictions`, `window:21:00-21:30` before it's known, `window:03:00-03:30`, `nope` with bad `items`), all `POST /notes`.
- Observed: All returned `404 {"error":"There is no such note"}`.
- Verdict reason: Matches expected exactly.

## TC-14 — Malformed body is a 400: PASS
- Command / action run: no body, `{}`, `promptId:123`, `promptId:null`, `promptId:["theory"]`, `{items:[...]}` (no promptId), raw invalid JSON.
- Observed: All 400. Parseable-JSON cases returned `{"error":"Expected { promptId, items? }"}`; the raw-invalid-JSON case returned `{"error":"Unexpected end of JSON input"}` (a non-empty string error, as the case allows). No 500 anywhere.
- Verdict reason: Matches expected.

## TC-15 — connect with invalid `items` is a 400: PASS
- Command / action run: 13 invalid `items` shapes (omitted, `[]`, single item, duplicate, 3 items, unknown suspect, unknown evidence, malformed id, empty string, numbers, string instead of array, object, null element).
- Observed: All returned `400 {"error":"Expected two different clues from the case file in items"}`.
- Verdict reason: Matches expected.

## TC-16 — connect with a locked exhibit is a 400: PASS
- Command / action run: `POST /notes {promptId:connect, items:["E014","S02"]}` and reversed order, E014 locked.
- Observed: Both 400 with the same error message; body contains no title/text referencing E014.
- Verdict reason: Matches expected.

## TC-17 — connect with a timeline event the player does not yet know: PASS
- Command / action run: `POST /notes {promptId:connect, items:["T08","S02"]}`, T08 not known (all its exhibits locked).
- Observed: `400 {"error":"Expected two different clues from the case file in items"}` — no leak of the T08 title.
- Verdict reason: Matches expected; the case's own hedge about a possible defect in `describe()` did NOT reproduce — T08 was correctly rejected.

## TC-18 — `items` is ignored for non-connect prompts: PASS
- Command / action run: `POST /notes {promptId:theory, items:"garbage"}` and `{promptId:contradictions, items:[1,2,3]}`
- Observed: Both 200, same V1-passing shape as without `items`.
- Verdict reason: Matches expected (the case's preferred outcome, since a 400 was explicitly not to be treated as a fail either way).

## TC-19 — Evidence that is unlocked but not examined is not used: PASS
- Command / action run: R1 only (E007 unlocked, not viewed); `GET /investigation` confirms `evidenceViewed` empty; `POST /notes` for `statement:S02` and `contradictions`.
- Observed: Both 200, `contradiction:false`, no `contradictions` key, `relatedEvidence:[]`, `confidence:"low"`. Neither answer mentions `E007` or "Garage Stairwell". `facts` had no `✓` lines.
- Verdict reason: Matches expected.

## TC-20 — "contradictions" prompt after examining a contradicting exhibit: PASS
- Command / action run: R2 (E007 viewed); `POST /notes {promptId:contradictions}`
- Observed: 200, `contradiction:true`, `contradictions:[{ST02-A,E007}]`, `confidence:"high"`, `relatedEvidence:["E007"]`, `relatedSuspects:["S02"]`, `relatedEvents:["T06"]`, no `E014` in answer.
- Verdict reason: Matches expected exactly.

## TC-21 — "statement:S02" prompt after examining a contradicting exhibit: PASS
- Command / action run: R2; `POST /notes {promptId:statement:S02}`
- Observed: Matches expected exactly — same contradiction data, `facts` has one `✓` (E007) and three `?` (ST02-A/B/C).
- Verdict reason: Matches expected.

## TC-22 — Statement prompts are scoped to their suspect: PASS
- Command / action run: R2; `POST /notes` for `statement:S01` and `statement:S04`
- Observed: Both `contradiction:false`, no `contradictions` key, `relatedEvidence:[]`/`relatedEvents:[]`, no mention of E007/Alex/Reyes, `facts` scoped to one suspect each.
- Verdict reason: Matches expected.

## TC-23 — Several pairs across two statements: PASS
- Command / action run: R2 then R3 (E007 and E014 viewed); `POST /notes` for `contradictions` and `statement:S02`
- Observed: Both `contradiction:true`, `confidence:"high"`, `contradictions` contains exactly `{ST02-A,E007}`, `{ST02-A,E014}`, `{ST02-B,E014}`. `relatedEvidence` set-equal `["E007","E014"]`, `relatedSuspects:["S02"]`, `relatedEvents` includes T06 and T08.
- Verdict reason: Matches expected exactly.

## TC-24 — Locked mitigating evidence is not leaked: PASS
- Command / action run: R4 (E012 viewed, E018 confirmed locked via `unlockedEvidence`); `POST /notes` for `statement:S04` and `contradictions`
- Observed: Both `contradiction:true`, `contradictions:[{ST04-A,E012}]`, `confidence:"medium"`, `relatedEvidence:["E012"]`. `"E018"` and `"Switchboard Message Slip"` do not appear anywhere in either response body.
- Verdict reason: Matches expected exactly.

## TC-25 — Half-hour window prompt: PASS
- Command / action run: R1; `POST /notes {promptId:"window:21:00-21:30"}`
- Observed: `relatedEvents:["T06"]`, `relatedEvidence:["E007"]`, `relatedSuspects:["S02"]`, `contradiction:false`, no `contradictions`/`facts` key. No mention of T04/T07/T08/T09/T11.
- Verdict reason: Matches expected exactly.

## TC-26 — Window prompt boundary is inclusive and lists only known events: PASS
- Command / action run: R6 (E008, E003 unlocked); `GET /notes` then `POST /notes {promptId:"window:20:30-21:00"}`
- Observed: `GET /notes` lists `window:19:30-20:00`, `window:20:30-21:00`, `window:21:00-21:30` in ascending order. POST: `relatedEvents:["T03","T04"]` (T04 at 21:00 included, boundary inclusive), `relatedEvidence` set-equal `["E008","E003"]`, `relatedSuspects` set-equal `["S03","S02"]`. No `E009`/`E004` anywhere.
- Verdict reason: Matches expected.

## TC-27 — connect answers from shared unlocked evidence: PASS
- Command / action run: E007 unlocked and viewed (superset of the R1 precondition); `POST /notes {promptId:connect, items:["S02","L05"]}`
- Observed: `relatedEvidence:["E007"]`, `relatedSuspects:["S02"]`, `relatedEvents:["T06"]`, `confidence:"medium"`, `contradiction:false`, answer mentions "Garage Stairwell Door Log" and no other exhibit.
- Verdict reason: Matches expected. (Ran with E007 also viewed, not just unlocked — a superset of R1's requirement — so this does not distinguish whether viewing is required; recorded as observed.)

## TC-28 — connect with a claim id: PASS
- Command / action run: R2, then `POST /contradictions {ST02-A,E007}` (200), then `POST /notes {promptId:connect, items:["ST02-A","E007"]}`
- Observed: 200, `relatedEvidence:["E007"]`, `confidence:"medium"`, `contradiction:false`, no 500.
- Verdict reason: Matches expected.

## TC-29 — theory prompt reflects the player's board and text: PASS
- Command / action run: R2, `POST /connections {E007→S02}` (201), `PUT /theory {"text":"Reyes used the stairwell."}` (200), then `POST /notes {promptId:theory}`
- Observed: `relatedEvidence:["E007"]`, `relatedSuspects:["S02"]`, `contradiction:false`, `facts` has S02 with a `✓` E007 line, `answer` contains the player's text verbatim and names no suspect other than S02.
- Verdict reason: Matches expected.

## TC-30 — Every relatedEvidence id is an unlocked exhibit: PASS
- Command / action run: Scripted run across states R0, R1, R2, R2+R3, R4, R6 — for each, `GET /investigation` then `POST /notes` for every `GET /notes` prompt (connect with `["S02","L05"]`).
- Observed: In every state/prompt combination, every `relatedEvidence` id was in `unlockedEvidence`; every `E\d{3}` token found in `answer` and `facts[].lines[].text` was also in `unlockedEvidence`. No issues found by the script across all 6 states.
- Verdict reason: Matches expected in every state tested.

## TC-31 — Every related id exists in the case: PASS
- Command / action run: Same scripted run (state R2+R3, plus repeated across R0/R1/R2/R4/R6, and the "everything examined" state used for TC-36/78 which has non-empty `relatedEvents`), cross-checked against `GET /evidence`, `GET /suspects`, `GET /timeline`, `GET /statements`.
- Observed: Every `relatedEvidence` id in `GET /evidence`; every `relatedSuspects` id in `GET /suspects`; every `relatedEvents` id in `GET /timeline`; every `contradictions[].assertionId` a real claim id from `GET /statements`; every `contradictions[].evidenceId` in `GET /evidence`. No null/empty/duplicate ids found in any related list, across all states tested.
- Verdict reason: Matches expected.

## TC-32 — Every contradiction pair is one POST /contradictions accepts: PASS
- Command / action run: R2+R3; sent all 3 pairs from TC-23's `contradictions` array to `POST /contradictions`; also `POST /contradictions {ST01-A,E007}`.
- Observed: All 3 real pairs returned 200 with matching `assertionId`/`evidenceId`. The non-contradiction returned `422 {"error":"That exhibit doesn't contradict that statement."}`.
- Verdict reason: Matches expected.

## TC-33 — Notes never change investigation state and never auto-log a contradiction: PASS
- Command / action run: R2+ state with a connection and theory set; `GET /investigation` (A); `POST /notes` for `contradictions`, `statement:S02`, `theory`, `connect`; `GET /notes` x3; `GET /investigation` (B).
- Observed: A deep-equals B.
- Verdict reason: Matches expected.

## TC-34 — Logging a contradiction from a note records it in investigation.contradictionsFound: PASS
- Command / action run: R2; `POST /notes {statement:S02}` gave `{ST02-A,E007}`; `POST /contradictions {ST02-A,E007}`; `GET /investigation`; repeated the same POST.
- Observed: First POST 200. `contradictionsFound` has exactly one entry `{assertionId:"ST02-A", evidenceId:"E007", suspectId:"S02"}`. `progress` increased from 1 (R2 only) to 4 after logging. Second POST returned 200 with `contradictionsFound` still exactly 1 entry (no duplicate).
- Verdict reason: Matches expected.

## TC-35 — Reset returns notes to the fresh state: PASS
- Command / action run: R2 + logged contradiction, then `POST /reset`, `GET /notes`, `POST /notes {contradictions}`, `GET /investigation`.
- Observed: `GET /notes` again returns the 8-item fresh list matching TC-01. The `contradictions` prompt matches TC-06's fresh shape (`contradiction:false`, empty lists). `contradictionsFound: []`.
- Verdict reason: Matches expected.

## TC-36 — No note contains solution data or an ending: PASS
- Command / action run: For cases 047–051 fresh: `GET /notes`, `POST /notes` for every prompt, `GET /facts/:suspectId` for every suspect. Also for case 047 in an "everything examined" state (every reachable spot searched, every file page read, all suspects/evidence viewed, every offered contradiction logged — 13/14 evidence items unlocked, 1 mitigating exhibit correctly still locked, 7 contradictions logged).
- Observed: No response body (case-insensitive) contained any of: `solution`, `culprit`, `requiredEvidence`, `requiredConnections`, `perfect_investigation`, `true_criminal`, `criminal_escapes`, `wrong_suspect`, `innocent_accused`. No extra top-level keys beyond the V1/facts key sets.
- Verdict reason: Matches expected across all 5 fresh cases and the fully-played 047 state.

## TC-37 — Suspect names in an answer are backed by the related lists: PASS
- Command / action run: On the "everything examined" 047 state, for every `contradictions`/`statement:*`/`window:*`/`connect` response, checked every suspect `name` from `GET /suspects` found in `answer` against `relatedSuspects`; checked `statement:Sxx` answers name no other suspect.
- Observed: No mismatches found by the script.
- Verdict reason: Matches expected.

## TC-38 — Notes after an accepted conclusion still do not name the culprit or the ending: PASS
- Command / action run: R2, `POST /conclusion {suspectId:null, evidenceIds:[]}` → 200 (`criminal_escapes`). Then `GET /notes`, `POST /notes` for every prompt, `GET /facts/S02`.
- Observed: All 200 (no 422 closed-case guard on notes). No forbidden substrings from TC-36, no `closed`, no `accused` in any body.
- Verdict reason: Matches expected; the source has no closed-case guard on Detective's Notes, as the case anticipated.

## TC-39 — Theory echo is the player's own text only: PASS
- Command / action run: R2, `PUT /theory {"text":"It was Victor Lang."}` (200), `POST /notes {promptId:theory}`
- Observed: `answer` quotes the text verbatim: `"...Your working theory: "It was Victor Lang." Below is only what you have established..."`. `relatedSuspects` did not include S03 (no board links). No confirm/deny words (`correct`, `right`, `wrong`, `incorrect`, `confirmed`) present.
- Verdict reason: Matches expected.

## TC-40 — GET /facts on a fresh case: PASS
- Command / action run: `GET /api/cases/047/facts/S02` on fresh case.
- Observed: 200, exactly `suspectId`/`name`/`lines` keys, `suspectId:"S02"`, `name:"Alex Reyes"`, 3 lines all `mark:"?"` with `assertionId` ST02-A/B/C in order, none with `✓` or `evidenceId`.
- Verdict reason: Matches expected.

## TC-41 — GET /facts for an unknown suspect is a 404: PASS
- Command / action run: `GET /facts/S99`, `/facts/S06`, `/facts/E014`, `/facts/xyz`
- Observed: All `404 {"error":"Suspect not found"}`.
- Verdict reason: Matches expected.

## TC-42 — Facts show ✓ only for examined evidence: PASS
- Command / action run: R1 (E007 unlocked, not viewed): `GET /facts/S02`; then `POST /viewed {evidence,E007}`; `GET /facts/S02` again.
- Observed: First: 3 lines, all `?`, no `evidenceId`. Second: 4 lines, exactly one `✓` with `evidenceId:"E007"` and non-empty text; the other 3 remained `?` for ST02-A/B/C.
- Verdict reason: Matches expected.

## TC-43 — Logging a contradiction turns its claim from ? to ✓: PASS
- Command / action run: R2, `POST /contradictions {ST02-A,E007}` (200), `GET /facts/S02`
- Observed: 4 lines — two `✓` (exhibit line E007; contradiction line E007/ST02-A), two `?` (ST02-B, ST02-C). No `?` line for ST02-A. All marks exactly `✓` or `?`.
- Verdict reason: Matches expected.

## TC-44 — A true claim stays ? forever: PASS
- Command / action run: R5 (E017 viewed), `POST /contradictions {ST01-B,E017}` (200), then E003 viewed too; `GET /facts/S01`; `POST /contradictions {ST01-A,E003}` and `{ST01-A,E017}`
- Observed: `facts/S01` had ✓ lines for E017 (exhibit) and E017/ST01-B (contradiction), plus a ✓ for E003 (exhibit); exactly one `?` line remained, for `ST01-A`. Both extra POSTs returned `422 {"error":"That exhibit doesn't contradict that statement."}`.
- Verdict reason: Matches expected.

## TC-45 — Facts shape for every suspect in every case: PASS
- Command / action run: Fresh state, cases 047–051; `GET /facts/:id` for every suspect from `GET /suspects`.
- Observed: All 200, `suspectId`/`name` matched, `lines` array, every line `mark` exactly `✓` or `?` with non-empty text, all `?` on a fresh case.
- Verdict reason: Matches expected across all 5 cases.

## TC-46 — statement prompt facts match GET /facts: PASS
- Command / action run: R2 + logged contradiction; `POST /notes {statement:S02}` and `GET /facts/S02`.
- Observed: `facts[0]` from the notes response was deep-equal (Python `==`) to the `GET /facts/S02` body.
- Verdict reason: Matches expected.

## TC-47 — Facts and notes are scoped per case: PASS
- Command / action run: Case 047 in R2 + logged contradiction; case 048 freshly reset. `GET /api/cases/048/facts/S02`, `/notes`, `/investigation`, `/suspects`.
- Observed: 048's facts/S02 show only 048's own claim (`?`, no `✓`), name matches 048's own S02 (`Felix Marsh`, not 047's Alex Reyes). 048's prompt labels use 048's suspect names. 048's `investigation` has `contradictionsFound: []` and `evidenceViewed: []`.
- Verdict reason: Matches expected.

## TC-48 — assistant/query with an off-topic question: PASS
- Command / action run: `POST /assistant/query {"question":"What is the weather like today?"}`
- Observed: 200, exact body match to the spec's quoted text: `answer` = "I can compare a suspect's statement with the records, walk through what happened between two times, or show what ties a suspect to a place. Try one of those.", `confidence:"low"`, all related lists `[]`, `contradiction:false`, no `contradictions` key.
- Verdict reason: Matches expected exactly.

## TC-49 — assistant/query with a missing or empty question is a 400: PASS
- Command / action run: no body, `{}`, `{question:""}`, `{question:"   "}`, `{question:123}`, `{question:null}`, `{question:["a"]}`
- Observed: All 7 returned `400 {"error":"Expected { question } of 1–500 characters"}`.
- Verdict reason: Matches expected.

## TC-50 — assistant/query length boundary: PASS
- Command / action run: `question` = 500 chars of "a", then 501 chars.
- Observed: 500 chars → 200, off-topic shape as in TC-48. 501 chars → `400 {"error":"Expected { question } of 1–500 characters"}`.
- Verdict reason: Matches expected.

## TC-51 — assistant/query does not reason over locked evidence: PASS
- Command / action run: R0; `POST /assistant/query {"question":"Does Alex Reyes's statement contradict the records?"}`
- Observed: 200, `contradiction:false`, no `contradictions` key, `relatedEvidence:[]`/`relatedEvents:[]`, no `E\d{3}` token in `answer`. (`confidence` was `"medium"`, `relatedSuspects:["S02"]` — not addressed by this case's expectation and consistent with TC-74's documented "medium on empty" behaviour.)
- Verdict reason: Matches every condition this case actually specifies.

## TC-52 — assistant/query time window on a fresh case: PASS
- Command / action run: R0; `POST /assistant/query {"question":"What happened between 21:00 and 21:30?"}`
- Observed: 200, `confidence:"low"`, all related lists `[]`, `contradiction:false`, `answer`: "Nothing is recorded between 21:00 and 21:30." — names no timeline event.
- Verdict reason: Matches expected.

## TC-53 — assistant/query still returns its unchanged shape for an on-topic question: PASS
- Command / action run: R2; `POST /assistant/query {"question":"Does Alex Reyes's statement contradict the records?"}`
- Observed: 200, keys exactly `answer, confidence, relatedEvidence, relatedSuspects, relatedEvents, contradiction, contradictions` (7 keys — contradictions present since contradiction is true). No `promptId`/`title`/`facts` keys. `contradictions:[{ST02-A,E007}]`, both ids unlocked and real.
- Verdict reason: Matches expected exactly.

## TC-63 — Progress in one case does not affect another case's notes: PASS
- Command / action run: Case 047 in R2 + logged contradiction; case 048 freshly reset. `POST /notes` for every 048 prompt (connect with first 048 suspect + first 048 location).
- Observed: All 200, V1 passes, `contradiction:false`, `relatedEvidence`/`relatedEvents` `[]`. `"ST02-A"` did appear in 048's own `statement:S02` response, but this is 048's own genuine claim id for its own suspect S02 (`Felix Marsh`), not a leak of 047's data — the case explicitly permits this ("unless the id genuinely exists in 048's own data"). 047's `investigation.contradictionsFound` remained unchanged after the 048 calls.
- Verdict reason: Matches expected; the one id overlap is explicitly allowed by the case and confirmed to be 048's own data, not a cross-case leak.

## TC-64 — [UI] Notes page renders prompt buttons from the API, with no free-text question box: BLOCKED
- Reason: no browser automation tool available in this run.

## TC-65 — [UI] Choosing a prompt shows a busy state, then the answer: BLOCKED
- Reason: no browser automation tool available in this run.

## TC-66 — [UI] The connect prompt offers two dropdowns and disables submission until two distinct clues are chosen: BLOCKED
- Reason: no browser automation tool available in this run.

## TC-67 — [UI] Related evidence, suspects and events render as clickable cards to their own pages: BLOCKED
- Reason: no browser automation tool available in this run.

## TC-68 — [UI] Logging a contradiction from a note's "log this contradiction" button updates the UI and the backend: BLOCKED
- Reason: no browser automation tool available in this run. (The backend half of this — `POST /contradictions` after a note surfaces the pair, then `GET /investigation` showing exactly one entry — was independently exercised and confirmed via the API in TC-34.)

## TC-69 — [UI] GET /notes network failure shows an error state with working retry: BLOCKED
- Reason: no browser automation tool available in this run.

## TC-70 — [UI] A failed POST /notes shows an inline error without losing earlier notes or the ability to try again: BLOCKED
- Reason: no browser automation tool available in this run.

## TC-71 — [UI] A statement note's "established facts" list matches GET /facts marks: BLOCKED
- Reason: no browser automation tool available in this run. (The underlying data equality this case checks — a statement note's `facts` matching `GET /facts` exactly — was independently confirmed via the API in TC-46.)

## TC-72 — assistant/query answers "what ties a suspect to a place" from unlocked evidence: PASS
- Command / action run: R1 (E007 unlocked, not viewed); `POST /assistant/query {"question":"What ties Alex Reyes to the Parking Garage?"}`
- Observed: 200, `confidence:"high"`, `contradiction:false`, no `contradictions` key, `relatedEvidence:["E007"]`, `relatedSuspects:["S02"]`, `relatedEvents:["T06"]`, `answer`: "One exhibit ties Alex Reyes to Parking Garage: Garage Stairwell Door Log (E007)." — no other exhibit named.
- Verdict reason: Matches expected exactly.

## TC-73 — assistant/query "ties a suspect to a place" with no matching exhibit: PASS
- Command / action run: R0; `POST /assistant/query {"question":"What ties Victor Lang to the Communications Room?"}`
- Observed: 200, `answer` exactly "No exhibit places Victor Lang at Communications Room.", `confidence:"medium"`, `contradiction:false`, no `contradictions` key, `relatedEvidence:[]`/`relatedEvents:[]`, `relatedSuspects:["S03"]`.
- Verdict reason: Matches expected exactly.

## TC-74 — assistant/query naming a suspect with zero known contradictions returns "medium", not "low": PASS
- Command / action run: R0; `POST /assistant/query {"question":"Does Nina Okafor's account check out?"}`
- Observed: 200, `answer` exactly "I found no statement from Nina Okafor that conflicts with the records.", `confidence:"medium"`, `contradiction:false`, `relatedEvidence:[]`/`relatedEvents:[]`, `relatedSuspects:["S04"]`.
- Verdict reason: Matches expected exactly; confirms this is current/expected behaviour, not a defect, per the case's own framing.

## TC-75 — assistant/query may surface a contradiction from evidence the player has unlocked but never viewed: FAIL
- Command / action run: R1 only (E007 unlocked via `POST /places/L05/search {L05-stairs}`, **not** viewed — confirmed `GET /investigation.evidenceViewed` was `[]`); `POST /assistant/query {"question":"Does Alex Reyes's statement contradict the records?"}`
- Observed: 200 with `contradiction: true`, `contradictions:[{"assertionId":"ST02-A","evidenceId":"E007"}]`, `relatedEvidence:["E007"]`, and the answer text naming "Garage Stairwell Door Log (E007)" — while `evidenceViewed` was still empty at the time of the call.
- Verdict reason: This is the discrepancy the case explicitly asks to check for: `POST /assistant/query` answered from evidence the player unlocked but never examined, unlike `/notes` (see TC-19, which correctly withholds E007 in the identical unlocked-not-viewed state). Per CLAUDE.md's Assistant section ("the assistant only reasons over evidence in the player's case file... Check before responding") and the spec's "examined" framing, this is a real behavioural gap in `assistant.service.js`'s `knownPairs()`, which filters only by unlocked evidence and not by `evidenceViewed` the way `notes.service.js`'s `consult()`/`examined()` does.

## TC-76 — Notes, facts and assistant/query stay reachable after the case is closed: PASS
- Command / action run: R2, `POST /conclusion {suspectId:null, evidenceIds:[]}` → 200 (`criminal_escapes`). Then `GET /notes`, `POST /notes {contradictions}`, `GET /facts/S02`, `POST /assistant/query {"question":"Does Alex Reyes's statement contradict the records?"}`
- Observed: All four returned 200, not 422. No forbidden-substring hits (same list as TC-36) in any body.
- Verdict reason: Matches expected — confirms the closed-case guard added around the b45dd98 fix is scoped to legwork/time-costing actions (travel/search/file-read/theory) and does not block Detective's Notes or the assistant after an accusation.

## TC-77 — A "Named in <exhibit>" fact line appears for evidence that names a suspect with no structured fact: PASS
- Command / action run: `POST /travel {L03}`, `POST /places/L03/search {L03-e001}` (unlocks E001, personIds `["S01"]`, `facts: []`), `POST /viewed {evidence,E001}`, `GET /facts/S01`
- Observed: Among S01's lines, exactly one has `mark:"✓"`, `evidenceId:"E001"`, `text:"Named in Empty Prototype Case"`.
- Verdict reason: Matches expected exactly — exercises the `mine.length === 0` branch as described.

## TC-78 — Related-id lists never exceed the 6-item cap even when more would qualify: PASS
- Command / action run: "Everything examined" 047 state (same as TC-36/37); `POST /notes` for every listed prompt (connect with `["S02","L06"]`); `POST /assistant/query` for the Alex Reyes contradiction question.
- Observed: `relatedEvidence.length` and `relatedEvents.length` were `<= 6` in every response. Two prompts (`contradictions` and `window:21:00-21:30`) actually reached the cap exactly at 6/6, confirming `MAX_RELATED = 6` is enforced rather than merely never being tested. Full observed counts per prompt were recorded in the session log (contradictions 6/6, statement:S01 1/0, statement:S02 2/2, statement:S03 1/1, statement:S04 1/2, statement:S05 1/1, window:19:30-20:00 1/1, window:20:30-21:00 3/2, window:21:00-21:30 6/6, window:21:30-22:00 2/2, connect 1/2, theory 0/0).
- Verdict reason: Matches expected — cap enforced, never exceeded.

## Summary
Total: 69 | Pass: 60 | Fail: 1 | Blocked: 8

**Original cases (TC-01–TC-53, TC-63 — 54 total):** Pass: 54 | Fail: 0 | Blocked: 0

**Added cases (TC-64–TC-78 — 15 total):** Pass: 6 | Fail: 1 | Blocked: 8

Failures needing attention:
- TC-75 — `POST /assistant/query` answers a "does the statement contradict the records" question using evidence the player has unlocked but never viewed (`evidenceViewed` was empty), returning `contradiction:true` with `E007`/`ST02-A`. `/notes` correctly withholds this in the identical state (TC-19). This is a real gap between the two endpoints' "only reason over examined evidence" guarantee — `assistant.service.js`'s contradiction-matching path filters by unlocked evidence only, not by viewed evidence.

Blocked (not counted as pass/fail): TC-64, TC-65, TC-66, TC-67, TC-68, TC-69, TC-70, TC-71 — all UI cases, no browser automation tool was available in this session's tool list, so none could be exercised in the actual browser. Two of these (TC-68's backend half, TC-71's data-equality claim) were independently confirmed via direct API calls (see TC-34 and TC-46) but the UI rendering/interaction itself was never observed and must not be inferred as passing.

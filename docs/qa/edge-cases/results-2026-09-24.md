# Results — edge-cases
Run at: 2026-09-24T16:26:00Z
Backend reachable: yes (isolated instance, port 4210, `DB_PATH` under this session's scratchpad, never :4000)

Two runs were made against two fresh databases on the same isolated backend (port 4210),
per the task's overrides:

- Run 1 (EC-01..EC-48): `python edge_cases.py --base http://localhost:4210/api --yes --json
  <scratchpad>/qa/edge-cases.json`, unmodified, from the repo root. Full stdout captured to
  `<scratchpad>/qa/edge-cases-stdout.log`, machine-readable results to
  `<scratchpad>/qa/edge-cases.json` (60 entries — EC-33/34/35 recorded once per case).
- Backend stopped, `.sqlite`/`-wal`/`-shm` deleted, backend restarted fresh.
- Run 2 (EC-49..EC-62): manual `curl` calls against the same fresh backend, one case at a
  time, following each case's own Preconditions/Steps.

No answer-key content (culprit names, `requiredEvidence`, `requiredConnections`) is
reproduced anywhere below; findings that touch `solution.json` are described structurally,
matching Rohan's own script (which reads the key internally but never prints it).

---

## Section 1 — Rohan's edge cases (EC-01..EC-48, run via `edge_cases.py`)

## EC-01 — Unknown / malformed caseId is a 404 on the case and every sub-route: PASS
- Command / action run: `edge_cases.py`, EC-01 (`GET /cases/{999,047x,047%20,..%2F047,46}` and each `/evidence`).
- Observed: `[["999", 404, 404], ["047x", 404, 404], ["047%20", 404, 404], ["..%2F047", 404, 404], ["46", 404, 404]]`
- Verdict reason: all ten calls 404 as expected.

## EC-02 — Unknown API route returns 404 { error }: PASS
- Command / action run: `edge_cases.py`, EC-02 (`GET /nope`, `GET /cases/047/nope`).
- Observed: `404 {"error": "Not found"} | 404 {"error": "Not found"}`
- Verdict reason: both 404 with a truthy `error` string.

## EC-03 — Malformed JSON body is a 400 { error }, not a 500: PASS
- Command / action run: `edge_cases.py`, EC-03 (`POST /cases/047/viewed` with truncated JSON).
- Observed: `400 {"error": "Expected double-quoted property name in JSON at position 21 (line 1 column 22)"}`
- Verdict reason: 400 with a string `error`, no 500/stack trace.

## EC-04 — Non-JSON content-type body is treated as empty -> 400: PASS
- Command / action run: `edge_cases.py`, EC-04 (`POST /cases/047/viewed`, `content-type: text/plain`).
- Observed: `400 {"error": "Expected { type: \"evidence\" | \"suspect\" | \"event\", id }"}`
- Verdict reason: 400 as expected.

## EC-05 — Per-case isolation: evidence/clock/location in one case never leak into another: PASS
- Command / action run: `edge_cases.py`, EC-05 (search a free spot in 050, compare against 047).
- Observed: `050 unlocked=['E001'] used=45; 047 unlocked=[] used=0 loc=None; GET 047/evidence/E001 -> 404`
- Verdict reason: matches expected isolation exactly.

## EC-06 — No GET on any case exposes culprit / requiredEvidence / requiredConnections: PASS
- Command / action run: `edge_cases.py`, EC-06 (51 GET calls, regex sweep for the 4 key names).
- Observed: `0 leaks across 5 cases x 10 routes + desk`
- Verdict reason: no answer-key field names found anywhere.

## EC-07 — Hitting every GET route leaves investigation state byte-identical: PASS
- Command / action run: `edge_cases.py`, EC-07 (13 GET calls on case 048, before/after snapshot of `/investigation`).
- Observed: `identical`
- Verdict reason: GET never mutated state, matching the rule.

## EC-08 — Fresh case clock: 0 used, now = start, deadline = start + hours, not timeUp: PASS
- Command / action run: `edge_cases.py`, EC-08 (`GET /cases/050/investigation` vs. `case.json`'s clock).
- Observed: `{"start": "1984-06-10T09:00:00", "deadline": "1984-06-10T21:00:00", "now": "1984-06-10T09:00:00", "minutesUsed": 0, "minutesLeft": 720, "timeUp": false, ...}`
- Verdict reason: all four checks (minutesUsed, now, minutesLeft, timeUp) match.

## EC-09 — Repeat actions are free: travel to where you stand, re-search a spot, re-read a page: PASS
- Command / action run: `edge_cases.py`, EC-09 (travel/re-travel, search/re-search, read F1/re-read).
- Observed: `after travel=30, +same travel +search=45, +re-search +read F1=65, +re-read F1=65`
- Verdict reason: `(t1,t2,t3,t4) === (30,45,65,65)` exactly as specified.

## EC-10 — Last action may overshoot the deadline: allowed once, then now is clamped to deadline and minutesLeft = 0: PASS
- Command / action run: `edge_cases.py`, EC-10 (travel with `minutesLeft` just above 30).
- Observed: `before: left=25; travel -> 200; after: used=720 left=0 now=1984-06-10T21:00:00 deadline=1984-06-10T21:00:00`
- Verdict reason: overshoot allowed once, then clamped — matches expected.

## EC-11 — After time is up: every time-costing action is 422; free actions still 200: PASS
- Command / action run: `edge_cases.py`, EC-11 (7-part matrix continuing EC-10's state).
- Observed: `{"travelElsewhere": 422, "travelSame": 200, "readUnread": 422, "rereadF1": 200, "getPlace": 200, "search": 422, "question": 422, "leave": 200}`
- Verdict reason: every part matches the expected status exactly.

## EC-12 — Accusation is still accepted after time is up: PASS
- Command / action run: `edge_cases.py`, EC-12 (`POST /conclusion {"suspectId": null}` with time up).
- Observed: `200 {"suspectId": null, "evidenceIds": [], "ending": "criminal_escapes"}`
- Verdict reason: 200, accusation accepted despite time-up; case reset afterward by the script.

## EC-13 — Looking at a place you are not standing in is 422; an unknown place is 404 (404 wins): PASS
- Command / action run: `edge_cases.py`, EC-13 (`GET /places/L02` not visited, `GET /places/L99`).
- Observed: `422 {"error": "You aren't there. Go there first."} | 404 {"error": "There is no such place in this case"}`
- Verdict reason: statuses match; existence check runs before the "aren't there" check as documented.

## EC-14 — Bad travel bodies: missing/non-string -> 400, unknown/wrong-case ID -> 404, no time spent: PASS
- Command / action run: `edge_cases.py`, EC-14 (no body, numeric `locationId`, `"L99"`, `"l02"`).
- Observed: `no body: 400, locationId number: 400, unknown L99: 404, lowercase l02: 404; minutesUsed=0`
- Verdict reason: all four statuses match, and no time was spent by the rejected calls.

## EC-15 — Gated search spot is hidden and 404s exactly like a spot that does not exist, until its flag is set: PASS
- Command / action run: `edge_cases.py`, EC-15 (list spots at L01 before/after reading F2, search gated + foreign spot).
- Observed: `before F2: spots=['L01-e001', 'L01-bay', 'L01-neighbour']; gated -> 404 "There is nothing like that to search here"; foreign spot -> 404 "There is nothing like that to search here"; after F2: spots=['L01-e001', 'L01-panel', 'L01-bay', 'L01-neighbour']`
- Verdict reason: `L01-panel` absent before F2, present after; both 404s share the identical message.

## EC-16 — Search validation: missing/non-string spotId -> 400; searching a place you are not in -> 422: PASS
- Command / action run: `edge_cases.py`, EC-16 (`{}`, `{"spotId": 1}`, real spot at a place the detective isn't standing in).
- Observed: `400, 400, 422`
- Verdict reason: matches expected statuses.

## EC-17 — Unread page bodies are null, GET /cases/:id has no file text, unknown page is 404 and costs nothing: PASS
- Command / action run: `edge_cases.py`, EC-17 (`GET /file`, `GET /cases/050`, `POST /file/F9/read`).
- Observed: `bodies null: True; case has 'file': False; F9 -> 404`
- Verdict reason: matches expected (F9 also spent no time, per the script's own check).

## EC-18 — Questioning someone who is not where you are -> 422; unknown suspect -> 404 (GET and POST): PASS
- Command / action run: `edge_cases.py`, EC-18 (`POST /dialogue/S02/choice` not-there, `POST/GET /dialogue/S99`).
- Observed: `422 {"error": "They aren't here. Go and find them."} | 404 | 404`
- Verdict reason: matches expected — existence checked before location for the POST, and the GET behaves the same.

## EC-19 — Choice body must be exactly one of { choiceId } / { presentEvidenceId }: both, neither or non-string -> 400: PASS
- Command / action run: `edge_cases.py`, EC-19 (both keys set, neither key set, non-string `choiceId`).
- Observed: `400, 400, 400`
- Verdict reason: all three 400 as expected.

## EC-20 — { choiceId, presentEvidenceId: null } should be treated as a plain spoken choice: FAIL
- Command / action run: `edge_cases.py`, EC-20 (`POST /dialogue/S02/choice {"choiceId": "mf-start-service", "presentEvidenceId": null}`).
- Observed: `422 {"error": "That exhibit isn't in your case file."}` — the script's own annotation: `<- null is not undefined, so applyChoice takes the present-evidence branch`.
- Verdict reason: the test case's own analysis of the XOR check (`typeof null === 'string'` is `false`, so it should fall through to the spoken-choice branch) does not match observed backend behavior — the backend evidently treats an explicit `null` `presentEvidenceId` as "present mode" (checking `presentEvidenceId !== undefined` or similar, not `typeof ... === 'string'`), and 422s because `null` isn't in the case file. Expected 200, observed 422. Real backend behavior differs from the test case's stated reasoning; recorded as-is, no source was modified to make this pass.

## EC-21 — Evidence reactions are never listed and can't be triggered as a spoken choiceId; another suspect's choice -> 422: PASS
- Command / action run: `edge_cases.py`, EC-21 (`GET /dialogue/S02`, POST a reaction id as `choiceId`, POST a foreign suspect's choice id).
- Observed: `listed=['mf-start-service', 'mf-start-quiet', 'mf-start-key', 'mf-start-leave']; mf-start-e004 as choiceId -> 422; oh-start-night -> 422`
- Verdict reason: reaction id not listed, both misuse attempts 422 as expected.

## EC-22 — Presenting a locked or non-existent exhibit -> 422 with the same message (no existence oracle): PASS
- Command / action run: `edge_cases.py`, EC-22 (present a real-but-locked exhibit, then a nonexistent one).
- Observed: `422 {"error": "That exhibit isn't in your case file."} | 422 {"error": "That exhibit isn't in your case file."}`
- Verdict reason: identical status and message for both cases — no existence oracle.

## EC-23 — Presenting an unlocked exhibit with no scripted reaction -> presentFallback, 'unmoved', 10 min, marked viewed: PASS
- Command / action run: `edge_cases.py`, EC-23 (unlock E007 at S02's home spot, present it).
- Observed: `presented E007 (scripted reaction: False) -> 200, reaction=unmoved, node=mf-unmoved, cost=10`
- Verdict reason: matches expected — 200, `unmoved`, 10-minute cost, marked viewed.

## EC-24 — Leaving / stepping back costs no time; leaving resets the interview to its opening node: PASS
- Command / action run: `edge_cases.py`, EC-24 (leaving choice `mf-unmoved-back`, continuing EC-23's state).
- Observed: `choice mf-unmoved-back -> 200, cost=0, node now mf-start`
- Verdict reason: 200, zero cost, node reset to the interview's start node.

## EC-25 — A choice whose requires is unmet is not listed and is a 422 if sent anyway (tr-start-codes before F2): PASS
- Command / action run: `edge_cases.py`, EC-25 (`POST tr-start-codes` before F2 read; list choices at S03).
- Observed: `listed=['tr-start-night', 'tr-start-oliver', 'tr-start-lane', 'tr-start-bluff', 'tr-start-leave']; forced -> 422`
- Verdict reason: `tr-start-codes` absent from listed, forced POST 422. Case reset afterward by the script.

## EC-26 — Connection validation matrix: PASS
- Command / action run: `edge_cases.py`, EC-26 (12-step matrix on case 047).
- Observed: `self-link: 422; unknown S99: 422; lowercase s01: 422; empty string: 422; locked evidence: 422; unknown claim ST09-Z: 422; relationship caused_by: 422; source is number: 400; relationship null -> default: 201; no relationship -> default: 201; reverse duplicate: 422; claim to location: 201`
- Verdict reason: all 12 statuses match the matrix exactly.

## EC-27 — Delete: 204 then 404 on repeat, unknown -> 404; deleted IDs are never reused; reset restarts at C01: PASS
- Command / action run: `edge_cases.py`, EC-27 (delete, repeat delete, delete unknown C99, new connection, reset, new connection).
- Observed: `delete C01: 204, again: 404, C99: 404; next id=C04; after reset=C01`
- Verdict reason: deleted id not reused (next id was one past the pre-delete count, `C04`), reset restarts the sequence at `C01`.

## EC-28 — A timeline event whose exhibits are all still locked cannot be linked (422) or viewed (404): PASS
- Command / action run: `edge_cases.py`, EC-28 (`POST /connections` and `POST /viewed` on a fully-locked T id).
- Observed: `T01: link -> 422, view -> 404`
- Verdict reason: matches expected — a fully-locked event doesn't "exist" for either purpose.

## EC-29 — Contradiction validation: bad types 400, unknown claim 404, locked exhibit 404: PASS
- Command / action run: `edge_cases.py`, EC-29 (numeric `assertionId`, unknown claim, locked exhibit).
- Observed: `types wrong: 400, unknown claim: 404, locked evidence: 404`
- Verdict reason: matches expected 400/404/404.

## EC-30 — POST /viewed: only evidence|suspect|event, unknown/locked -> 404: PASS
- Command / action run: `edge_cases.py`, EC-30 (bad `type`, numeric `id`, unknown suspect, locked evidence).
- Observed: `type page: 400, id number: 400, unknown suspect: 404, locked evidence: 404`
- Verdict reason: matches expected 400/400/404/404.

## EC-31 — Theory boundaries: 5000 ok, 5001 -> 400, empty ok, non-string 400, quotes/emoji/SQL stored verbatim: PASS
- Command / action run: `edge_cases.py`, EC-31 (5000/5001-char strings, empty string, number, a quote/SQL/emoji string).
- Observed: `5000 chars: 200, 5001 chars: 400, empty: 200, number: 400, emoji / quotes / SQL: 200 | stored verbatim: True`
- Verdict reason: boundary at 5000/5001 exact, weird string stored unescaped/untruncated with no crash.

## EC-32 — Conclusion validation matrix; rejected bodies don't close the case; report before conclusion is 422: PASS
- Command / action run: `edge_cases.py`, EC-32 (8-step body matrix on case 047, `GET /report` before any conclusion).
- Observed: `{}: 400, suspect + []: 400, evidenceIds string: 400, evidenceIds [123]: 400, null + evidenceIds 'x': 400, unknown S99: 422, locked exhibit: 422, null + locked exhibit: 422 | report: 422`
- Verdict reason: all eight statuses match; the pre-conclusion report is 422 and none of the eight rejected bodies closed the case (checked by the script via a follow-up `GET /investigation`).

## EC-33 — [per case] Right name on thin evidence -> criminal_escapes, deduped cites, same shape as a wrong name; no whatHappened: PASS (all 5 cases)
- Command / action run: `edge_cases.py`, EC-33, run once per case (047–051): accuse the culprit with a duplicated exhibit id (`thin`), then accuse an innocent with the same single exhibit (`wrong_thin`), compare `GET /report` shapes.
- Observed (047): `culprit+thin -> criminal_escapes, cites=1; innocent+thin -> innocent_accused; keys equal: True; whatHappened: None / None`. Identical pattern (`criminal_escapes` / `innocent_accused`, `cites=1`, `keys equal: True`, both `whatHappened: None`) repeated for 048, 049, 050 and 051.
- Verdict reason: for every case, the right name on thin evidence resolves to `criminal_escapes` (not a solved ending), the duplicate exhibit id was deduped to a single cite, the two reports' top-level key sets are identical (no shape leak distinguishing "right name, thin" from "wrong name"), and neither report carries `whatHappened`.

## EC-34 — [per case] Second accusation -> 422; desk shows closed + ending; reset puts desk back to 'new': PASS (all 5 cases)
- Command / action run: `edge_cases.py`, EC-34, run once per case, continuing EC-33's `thin` accusation: a second `POST /conclusion`, `GET /cases` desk entry, `POST /reset`, `GET /cases` again.
- Observed (047): `second -> 422; desk=closed/criminal_escapes; after reset=new`. Same pattern for 048, 049, 050, 051.
- Verdict reason: second accusation always 422, desk always shows `closed` + the `criminal_escapes` ending from EC-33, and reset always restores `status: "new"` — for every one of the five cases.

## EC-35 — [per case] Innocent with >=2 board links -> wrong_suspect; with nothing -> innocent_accused: PASS (all 5 cases)
- Command / action run: `edge_cases.py`, EC-35, run once per case: two board links from an innocent suspect to two locations plus a thin cited exhibit (`reasoned`), compared against EC-33's `wrong_thin` (no board links).
- Observed (047): `2 links -> wrong_suspect; no support -> innocent_accused`. Same pattern for 048, 049, 050, 051.
- Verdict reason: `reasoned.ending === "wrong_suspect"` and `wrong_thin.ending === "innocent_accused"` for every case, matching the `REASONED_CASE = 2` threshold.

## EC-36 — After the case is closed the board, theory and viewed state should be frozen: FAIL
- Command / action run: `edge_cases.py`, EC-36, case 047 only, continuing EC-33/34/35's `thin`-closed state: new connection, theory edit, viewed, travel, two `GET /report` snapshots.
- Observed: `after close: new link -> 201, theory edit -> 422, viewed -> 200, travel -> 422; report changed after close: True`
- Verdict reason: the test case's own stated pass/fail boolean checks that (a) the new connection is **not** 201, (b) the theory edit is **not** 200, and (c) `report.connections`/`report.theory` are unchanged between the two `GET /report` calls. Observed: the new connection **was** accepted with 201 (not rejected), and the report's `connections`/`theory` **did** change between the two snapshots as a direct result. This is a real gap: `POST /connections` on a closed case is not blocked, contrary to "one accepted conclusion closes the case" / "board... frozen." (The theory edit was correctly blocked at 422, and `viewed`/`travel` were recorded for information only, as the test case specifies — `viewed` 200 as flagged, and `travel` here was also 422, consistent with `field.service.js`'s `assertOpen` gate on legwork routes; the new-connection gap is the actual failure.)

## EC-37 — Assistant question length/type boundaries (1-500 chars, whitespace-only rejected): PASS
- Command / action run: `edge_cases.py`, EC-37 (empty, whitespace-only, 500 chars, 501 chars, a number).
- Observed: `empty: 400, whitespace: 400, 500 chars: 200, 501 chars: 400, number: 400`
- Verdict reason: matches the expected 400/400/200/400/400.

## EC-38 — Impossible clock times fall back to the low-confidence help answer (no 500): PASS
- Command / action run: `edge_cases.py`, EC-38 (`"what happened between 25:00 and 26:99?"`).
- Observed: `200 {"answer": "I can compare a suspect's statement with the records, walk through what happened between two times, or show what ties a suspect to a place. Try one ...`
- Verdict reason: 200 with the low-confidence help answer, no crash.

## EC-39 — Regex metacharacters in a question do not crash the classifier: PASS
- Command / action run: `edge_cases.py`, EC-39 (a question full of regex metacharacters).
- Observed: `200 {"answer": "I found no statement from any suspect that conflicts with the records.", "confidence": "medium", ...}`
- Verdict reason: 200, no 500 from an unescaped regex special character.

## EC-40 — Detective's Notes validation (unknown prompt/suspect/window 404; bad connect picks 400): PASS
- Command / action run: `edge_cases.py`, EC-40 (7-part matrix: unknown prompt, unknown suspect, unoffered window, dup/3-item/locked `connect` picks, missing `promptId`).
- Observed: `unknown prompt: 404, statement:S99: 404, unlisted window: 404, connect same twice: 400, connect 3 items: 400, connect locked E: 400, no promptId: 400`
- Verdict reason: matches expected 404/404/404/400/400/400/400.

## EC-41 — Notes 'connect' must reject a timeline event the player has not uncovered: PASS
- Command / action run: `edge_cases.py`, EC-41 (`connect` with a fully-locked T id).
- Observed: `T01 (locked) -> 400; hidden event title in answer: False -> ""`
- Verdict reason: 400, and the hidden event's real title is not leaked in the response.

## EC-42 — 050: the 23:30 half-hour window must return the 23:45 event (T05), not the rest of the day: FAIL
- Command / action run: `edge_cases.py`, EC-42 (unlock E005 via a free spot, `GET /notes`, `POST /notes {"promptId": "window:23:30-00:00"}`).
- Observed: `windows offered=['window:23:30-00:00']; window:23:30-00:00 -> "Nothing is recorded between 00:00 and 23:30." relatedEvents=[]`
- Verdict reason: the window prompt was offered and answered, but the answer text is `"Nothing is recorded between 00:00 and 23:30."` with `relatedEvents=[]` — it does not include `"T05"` as the test case requires, and its own guard against "not the whole day" (`"Between 00:00"` check) is moot since the observed text uses a differently-worded but still generic-looking phrasing that reports no event at all in the window. This looks like a real backend/data mismatch: unlocking E005 did not surface T05 inside the `23:30` window band as expected, or the window's own time boundary math is off. Recorded as a genuine FAIL against the stated expected result, not a script-authoring issue.

## EC-43 — Events inside the incident window but after midnight (050) should get a half-hour window prompt once uncovered: FAIL
- Command / action run: `edge_cases.py`, EC-43 (best-effort unlock every timeline exhibit for case 050, check window coverage for next-day events).
- Observed: `incident window 1984-06-09T23:00:00 -> 1984-06-10T07:00:00; next-day events: ['T06', 'T07', 'T08', 'T09']; windows offered: ['window:17:00-17:30', 'window:23:30-00:00']`
- Verdict reason: four next-day events (`T06`–`T09`) exist inside the incident window, but only two window prompts were offered (`17:00-17:30` and `23:30-00:00`) — no window prompt with an hour prefix matching most of these events' own local hours was found among the offered windows, so `len(covered) !== len(off_day)`. The test case's own text hedges this as an approximation that may false-positive/negative on real data, but on this run it registered as a failure per the script's literal check; recorded as-is per instructions ("flag as an approximation... during a real run").

## EC-44 — Every case matches the README: 5 people, 6 places, 14-18 exhibits: PASS
- Command / action run: `edge_cases.py`, EC-44 (count entries in each case's seed files).
- Observed: `047: 5p/6L/18E; 048: 5p/6L/15E; 049: 5p/6L/16E; 050: 5p/6L/14E; 051: 5p/6L/15E`
- Verdict reason: every case has exactly 5 suspects, 6 locations, and 14-18 (inclusive) evidence entries.

## EC-45 — No endings.json names the culprit (checked without printing the key): PASS
- Command / action run: `edge_cases.py`, EC-45 (substring check of each case's culprit name against its own `endings.json`, key never printed).
- Observed: `0 hits`
- Verdict reason: no culprit name appears in any case's `endings.json`.

## EC-46 — Every answer key only refers to IDs that exist in its own case: PASS
- Command / action run: `edge_cases.py`, EC-46 (cross-check every `solution.json` id against real ids in the same case).
- Observed: `all 5 keys consistent`
- Verdict reason: no dangling/foreign ids found in any of the five answer keys.

## EC-47 — Evidence whose every unlock route can be shut by a bad interview choice (a 'ne' flag): FAIL
- Command / action run: `edge_cases.py`, EC-47 (for every exhibit in every case, check whether every unlock route is gated by an `ne` flag condition).
- Observed: `can be locked out: 049:E012, 050:E011, 051:E011, 051:E013, 051:E006`
- Verdict reason: five exhibits across three cases (049, 050, 051) have every one of their unlock routes gated behind an `ne` flag condition, meaning a single bad interview choice that sets that flag to the "wrong" value can permanently lock the player out of that exhibit with no fallback route — a real data-design gap against the stated invariant. No IDs here reveal culprit/solution content (evidence and flag IDs only).

## EC-48 — 051: after 'Accuse him of poisoning Gerald with foxglove' (S03 hostile), E006 must still be obtainable: FAIL
- Command / action run: `edge_cases.py`, EC-48 (provoke S03 hostile, present E005 to hostile S03, check `unlockedEvidence` for E006).
- Observed: `accuse -> 200; later present E005 at node sb-start -> 200 reaction=unmoved; E006 unlocked: False`
- Verdict reason: presenting E005 to the now-hostile S03 got an `unmoved` reaction (no scripted reaction fired) and E006 was **not** unlocked afterward, contradicting the expected invariant that provoking S03 must not close off the route to E006. This is consistent with EC-47's finding that some exhibits in case 051 (including E006 itself, per EC-47's list) have every unlock route gated by an `ne` flag — the hostility flag set by `sb-start-accuse` appears to be exactly the kind of "bad choice locks the route" scenario CLAUDE.md says should never fully close off an exhibit. Real regression, not a script issue; case reset afterward by the script.

---

## Section 2 — Additional edge cases (EC-49..EC-62, run manually via curl on a fresh DB)

## EC-49 — Fresh case: defaultUnlockedEvidence is empty in all five cases: PASS
- Command / action run: `POST /cases/<id>/reset` then `GET /cases/<id>/investigation` and `GET /cases/<id>/evidence` for 047, 048, 049, 050, 051.
- Observed: every case: `investigation.unlockedEvidence: []`, `GET /evidence: []`. Exact for all five: `047 unlockedEvidence=[] evidence=[]`, `048 unlockedEvidence=[] evidence=[]`, `049 unlockedEvidence=[] evidence=[]`, `050 unlockedEvidence=[] evidence=[]`, `051 unlockedEvidence=[] evidence=[]`.
- Verdict reason: matches expected — nothing unlocked by default in any case.

## EC-50 — Every exhibit with a non-null locationId is only ever unlocked by a search spot at that same location: FAIL
- Command / action run: direct read of `data/cases/<id>/{evidence,locations,case}.json` for all five cases (data-integrity sweep, no API call); cross-checked every `locationId`-bearing exhibit's unlocking search spot(s) against its own `locationId`, and confirmed none of those exhibits also appear in a file page's `attachments`.
- Observed: `047: E005 (locationId=L03) unlocked by spot L02-recorder at different location L02` — every other exhibit in all five cases passed (no other cross-location unlocks, and no locationId-bearing exhibit found in any file attachment).
- Verdict reason: one real violation found — case 047's `E005` ("Storage Room Recorder Timer Log", `locationId: "L03"`) is only unlocked by search spot `L02-recorder` ("Check CCTV recorder three") at location `L02`, not at `L03`. This contradicts the stated rule that a location-bound exhibit is only ever found via a spot at that same location. Confirmed by reading both `evidence.json` and `locations.json` directly (IDs and spot placement only, no solution-key content).

## EC-51 — File-page paperwork only appears in the evidence list after that page is read: PASS
- Command / action run: case 048, page `F2` (attaches `E002`, confirmed from `case.json`). Before: `GET /evidence`, `GET /evidence/E002`. `POST /file/F2/read`. After: `GET /evidence`, `GET /evidence/E002` again.
- Observed: before — `E002 in list: False`, `GET /evidence/E002 status: 404`. After `POST /file/F2/read` (200) — `E002 in list: True`, `GET /evidence/E002 status: 200` with a full evidence body (`{"id":"E002","type":"forensic","title":"Medical Examiner's Note",...}`).
- Verdict reason: exact round trip matches expected — absent/404 before, present/200 after.

## EC-52 — A timeline event with some (not all) exhibits unlocked is known, viewable, linkable, with evidenceIds filtered: PASS
- Command / action run: case 047, event `T08` (`evidenceIds: ["E014","E002","E013"]`). Unlocked only `E013` via free spot `L03-e013` (no `requires`). `GET /timeline`, `POST /viewed {"type":"event","id":"T08"}`, `POST /connections {"source":"T08","target":"S01"}`.
- Observed: `GET /timeline` returned T08 with `"evidenceIds": ["E013"]` only (E014, E002 filtered out). `POST /viewed`: 200. `POST /connections`: 201.
- Verdict reason: matches expected — event known/viewable/linkable once partially unlocked, and `evidenceIds` correctly filtered to only the unlocked one.

## EC-53 — One hidden timeline event is consistently gated across GET /timeline, POST /viewed, POST /connections and Notes 'connect': PASS
- Command / action run: fresh case 047 reset, `hidden_t` = `T01` (`evidenceIds: ["E006"]`, fully locked on a fresh case). `GET /timeline`; `POST /viewed {"type":"event","id":"T01"}`; `POST /connections {"source":"T01","target":"S01"}`; `POST /notes {"promptId":"connect","items":["T01","S01"]}`.
- Observed: `T01 present: False` in `GET /timeline`. `POST /viewed`: 404 `{"error":"Nothing with that id in this case"}`. `POST /connections`: 422 `{"error":"One of those items isn't in this case."}`. `POST /notes`: 400 `{"error":"Expected two different clues from the case file in items"}`. None of the four response bodies contained T01's real title ("Work order #2291 filed").
- Verdict reason: all four routes consistently treat the hidden event as nonexistent, and none leaked its title.

## EC-54 — Legwork routes are blocked once the case is closed; POST /viewed and POST /contradictions currently are not: PASS
- Command / action run: fresh case 049 reset, `POST /conclusion {"suspectId": null}` to close it. Then `POST /file/F1/read`, `POST /places/L01/search {"spotId":"x"}`, `POST /dialogue/S01/choice {"choiceId":"x"}`, `POST /viewed {"type":"suspect","id":"S01"}`.
- Observed: conclusion — 200 `{"suspectId":null,"evidenceIds":[],"ending":"criminal_escapes"}`. File read: 422 `{"error":"This case is closed."}`. Search: 422 `{"error":"This case is closed."}`. Dialogue choice: 422 `{"error":"This case is closed. The interviews are over."}`. Viewed: 200 (full investigation body returned, `suspectsViewed: ["S01"]`).
- Verdict reason: matches expected exactly — three legwork/interview routes 422 once closed, `POST /viewed` still 200 (the documented gap), consistent with EC-36's finding.

## EC-55 — Every GET route stays 200 (read-only) after the case is closed: PASS
- Command / action run: continuing EC-54's closed case 049: `GET` on the case itself, `/evidence`, `/suspects`, `/statements`, `/timeline`, `/connections`, `/investigation`, `/file`, `/places`, `/notes`, `/report`.
- Observed: all eleven paths returned `200`: `"" -> 200`, `/evidence -> 200`, `/suspects -> 200`, `/statements -> 200`, `/timeline -> 200`, `/connections -> 200`, `/investigation -> 200`, `/file -> 200`, `/places -> 200`, `/notes -> 200`, `/report -> 200`.
- Verdict reason: every GET stays 200 once closed, `/report` in particular now succeeds (contrast with EC-32's pre-conclusion 422).

## EC-56 — Per-case isolation extends to conclusions, connections and story flags, not just evidence/clock/location: PASS
- Command / action run: reset 047 and 048. In 047: travel to `L01`, advance one spoken choice in S01's interview (`v-start-prototype`, 200), make one connection (`S01`→`S02`, 201), close with `POST /conclusion {"suspectId": null}` (200, `criminal_escapes`). Then `GET /cases/048/investigation` and `GET /cases` for 048's desk entry.
- Observed: `048: conclusion: None, connections: [], storyFlags: {}, interviewedSuspects: []`. `048 desk: status="new", ending=None`.
- Verdict reason: none of case 047's interview/connection/conclusion actions touched case 048's investigation state or its desk entry.

## EC-57 — Duplicate POST /viewed for the same id is idempotent: PASS
- Command / action run: case 050 reset, travel `L01`, search `L01-e001` (unlocks E001). `POST /viewed {"type":"evidence","id":"E001"}` twice, checking `GET /investigation` after each.
- Observed: first call 200, `evidenceViewed: ["E001"]`, `progress: 2`. Second (duplicate) call 200, `evidenceViewed: ["E001"]` (still one entry), `progress: 2` (unchanged).
- Verdict reason: repeat view is a silent no-op — no duplicate entry, no progress change.

## EC-58 — Duplicate POST /contradictions for the same real pair is idempotent: PASS
- Command / action run: fresh case 047 reset, unlocked+viewed E007 (real contradiction pair `ST02-A`/`E007`, found via `POST /notes {"promptId":"contradictions"}`). `POST /contradictions {"assertionId":"ST02-A","evidenceId":"E007"}` twice back to back, then `GET /investigation`.
- Observed: both calls returned identical 200 bodies (`{"assertionId":"ST02-A","evidenceId":"E007","suspectId":"S02","claim":"...","explanation":"...","mitigatedBy":[]}`). `GET /investigation`'s `contradictionsFound` contained the pair exactly once after both calls.
- Verdict reason: matches expected idempotency — same payload both times, single stored row.

## EC-59 — Malformed/oversized/odd-typed JSON bodies never produce a 500: FAIL
- Command / action run: case 047. `POST /connections` with raw body `[]`. `POST /viewed` with raw body `null`. `PUT /theory` with a 1,000,000-character string. `POST /conclusion` with an unexpected `extra.__proto__`-shaped nested object. Then `GET /cases/047` to confirm the server survived.
- Observed: connections `[]` body: 400 `{"error":"Expected { source, target, relationship }"}`. viewed `null` body: 400 `{"error":"Unexpected token 'n', \"null\" is not valid JSON"}` (Express's JSON body-parser runs in strict mode, which rejects a bare `null` literal at the top level). theory 1,000,000-char string: **413** `{"error":"request entity too large"}` (Express's body-parser size limit, hit before the route's own 5000-char check ever runs). conclusion with `__proto__`/`extra`: 400 `{"error":"Expected { suspectId, evidenceIds: [...] } with at least one exhibit, or { suspectId: null }"}`. Follow-up `GET /cases/047`: 200 (server alive, no crash).
- Verdict reason: three of four calls matched (400, as one of the two allowed statuses); the oversized-theory call returned **413**, which is neither `400` nor `422` as the test case's expected result requires (though it is clearly not a 500 and the server did not crash — this is Express's own body-size guard firing before route validation, a real but minor status-code mismatch against the stated expectation).

## EC-60 — Case-id variants ("47", "0470", "047 ", " 047") are all 404 — no trim, pad or numeric coercion: PASS
- Command / action run: `GET /cases/47`, `/cases/0470`, `/cases/047%20`, `/cases/%20047`, and each corresponding `/evidence` sub-route.
- Observed: all eight calls returned `404 {"error":"Case not found"}` (`47`, `0470`, `047%20`, `%20047`, each ×2 for the base route and `/evidence`).
- Verdict reason: exact string match against the seeded ids, no trimming/padding/coercion — matches expected.

## EC-61 — GET /places/:id never includes a spot's text, consequences or evidence id/title, searched or not: PASS
- Command / action run: case 047 reset, travel to `L01`, `GET /places/L01` before searching, `POST /places/L01/search {"spotId":"L01-bench"}`, `GET /places/L01` again after.
- Observed: before searching, all three spot objects had exactly the keys `['id', 'label', 'searched']`. After searching `L01-bench`, the full place body showed all three spots still shaped `{id, label, searched}` (e.g. `{"id":"L01-bench","label":"Inspect the calibration bench","searched":true}`) — no `text`, `consequences` or evidence id/title present for any spot, searched or not.
- Verdict reason: matches expected — the place-listing endpoint never leaks a spot's unlock target.

## EC-62 — No GET response on a fresh, fully-locked case names any locked exhibit's id or title: PASS
- Command / action run: case 047 reset (nothing unlocked). `GET` on the case itself, `/evidence`, `/suspects`, `/statements`, `/timeline`, `/file`, `/places`, `/notes`, dumped to text; cross-checked against every id/title read directly from `data/cases/047/evidence.json` (18 exhibits).
- Observed: `total evidence: 18`, `hits: []`
- Verdict reason: none of the 18 locked exhibits' ids or titles appear anywhere in the eight dumped GET response bodies.

---

## Summary

**Section 1 — Rohan's EC-01..EC-48** (60 individual results, since EC-33/34/35 each ran once
per the five cases): 54 pass, 6 fail, 0 blocked.

**Section 2 — Added EC-49..EC-62** (14 headings, one result each): 12 pass, 2 fail, 0
blocked.

**Combined total:** 74 individual results | Pass: 66 | Fail: 8 | Blocked: 0

Failures needing attention:
- EC-20 — a spoken choice sent with `presentEvidenceId: null` returns 422 ("That exhibit
  isn't in your case file"), not the 200 the test case expects; real backend behavior
  diverges from the test case's own reasoning about the XOR type check.
- EC-36 — `POST /connections` is accepted (201) on a closed case; the board is not frozen
  after the case closes, contrary to "one accepted conclusion closes the case."
- EC-42 — case 050's `window:23:30-00:00` prompt answers "Nothing is recorded between
  00:00 and 23:30" with `relatedEvents: []` after unlocking E005, instead of surfacing
  T05 inside that window.
- EC-43 — case 050: only 2 of the expected window prompts were offered for events inside
  the incident window on the day after midnight (4 next-day events, 2 windows covering
  them), though the test case itself flags this check as an approximation.
- EC-47 — five exhibits across cases 049, 050 and 051 have every unlock route gated
  behind an `ne` flag condition, meaning a single bad interview choice can permanently
  lock the player out of them.
- EC-48 — case 051: after provoking S03 hostile and presenting E005, E006 is not
  unlocked — the hostility route appears to close off E006 entirely, consistent with
  EC-47's finding for that exhibit.
- EC-50 — case 047's exhibit E005 (`locationId: "L03"`) is only unlocked by a search
  spot at a different location (`L02-recorder`), violating the "found at that place"
  rule.
- EC-59 — an oversized `PUT /theory` body returns 413 (Express's own body-size limit)
  rather than the 400/422 the test case expects; not a 500/crash, but a status-code
  mismatch against the stated expectation.

# Results — city-map-and-places
Run at: 2026-09-24T16:20:00Z (backend calls between 16:21–16:24 UTC per response `Date` headers)
Backend reachable: yes

## Setup

Own isolated backend started per instructions: `cd backend && PORT=4203 DB_PATH=".../scratchpad/qa/city-map-and-places.sqlite" node src/server.js`. SQLite file deleted before start. Confirmed reachable with `GET /api/health` → 200 before running any case. No browser automation tool (`mcp__plugin_playwright_playwright__*` or `mcp__Claude_Browser__*`) was available in this session's tool list, so no frontend was started and every UI-marked case is `BLOCKED` per the task rules, not guessed from source.

All commands below were run against `http://localhost:4203/api/cases/...`. State-mutating cases were preceded by `POST /reset` (or a specific setup sequence) as needed to match the case's stated preconditions; where a case's precondition continues directly from the previous case's resulting state, no extra reset was run (noted inline).

---

## TC-01 — GET /places returns six places with the full shape: PASS
- Command / action run: `POST /reset` then `GET /api/cases/047/places`
- Observed: 200, JSON array of 6 objects `L01..L06`, each with `id,name,floor,description,map{x,y,kind},people,visited,here` (plus `district`); `people[]` items have `id,name,role,alias`; all `visited`/`here` false.
- Verdict reason: Matches expected shape and count exactly.

## TC-02 — 047 place data matches the seed: PASS
- Command / action run: `GET /places`, then `GET /suspects/S01`, `/S02`, `/S05`, `/S03`, `/S04`
- Observed: L01 "Main Laboratory"/"2nd Floor"/["S01","S02"]/"lab"; L02 "Communications Room"/"Basement"/["S05"]/"radio"; L03 "Storage Room B"/"1st Floor"/[]/"crate"; L04 "Executive Office"/"3rd Floor"/["S03"]/"office"; L05 "Parking Garage"/"Ground"/[]/"garage"; L06 "Lobby & Security Desk"/"Ground"/["S04"]/"door". Each suspect's `name` from `/suspects/:id` (Dr. Maren Voss, Alex Reyes, Daniel Cho, Victor Lang, Nina Okafor) matches the `people[].name` in `/places`.
- Verdict reason: Exact match to the seed table in the spec.

## TC-03 — Fresh investigation: no place visited, none "here": PASS
- Command / action run: `POST /reset`, `GET /places`, `GET /investigation`
- Observed: all 6 places `visited:false, here:false`; investigation `locationId:null, placesVisited:[], spotsSearched:[], clock.minutesUsed:0, clock.minutesLeft:960`.
- Verdict reason: Matches exactly.

## TC-04 — GET /places does not change state: PASS
- Command / action run: `POST /reset`, `GET /places` x3, `GET /investigation`
- Observed: all three `GET /places` calls identical (ETag unchanged); `investigation.clock.minutesUsed:0, locationId:null, placesVisited:[]`.
- Verdict reason: GETs produced no state change.

## TC-05 — First travel costs 30 minutes and returns { place, investigation }: PASS
- Command / action run: `POST /reset`, `POST /travel {"locationId":"L01"}`
- Observed: 200, body keys exactly `place, investigation`; `place.id:"L01"`, `place.name:"Main Laboratory"`, `arrival` non-empty, `people`, `spots` present; `investigation.locationId:"L01"`, `placesVisited:["L01"]`, `clock.minutesUsed:30`, `clock.minutesLeft:930`, `clock.now:"1984-03-10T08:45:00"`.
- Verdict reason: Exact match.

## TC-06 — After travel, GET /places marks the place here and visited: PASS
- Command / action run: (continuing from TC-05 state) `GET /places`
- Observed: L01 `visited:true, here:true`; L02–L06 `visited:false, here:false`.
- Verdict reason: Exact match.

## TC-07 — Travelling elsewhere moves "here" but keeps "visited": PASS
- Command / action run: (continuing from TC-05/06 state) `POST /travel {"locationId":"L02"}`, `GET /places`, `GET /investigation`
- Observed: L01 `here:false,visited:true`; L02 `here:true,visited:true`; others false/false. `investigation.locationId:"L02"`, `placesVisited:["L01","L02"]`, `clock.minutesUsed:60`.
- Verdict reason: Exact match.

## TC-08 — Travelling to the current place costs 0 minutes: PASS
- Command / action run: `POST /reset`, `POST /travel L01` (minutesUsed 30), then `POST /travel L01` x2
- Observed: both repeat calls 200 with `place.id:"L01"`, `investigation.clock.minutesUsed:30` (unchanged), `clock.now:"1984-03-10T08:45:00"`, `placesVisited:["L01"]` (no duplicate).
- Verdict reason: Exact match.

## TC-09 — Travel with missing locationId is 400: PASS
- Command / action run: `POST /reset`, `POST /travel {}`
- Observed: 400 `{"error":"Expected { locationId }"}`. `GET /investigation` afterwards: `minutesUsed:0, locationId:null`.
- Verdict reason: Exact match.

## TC-10 — Travel with a non-string locationId is 400: PASS
- Command / action run: `POST /reset`, `POST /travel {"locationId":1}`, `{"locationId":null}`, and a POST with `Content-Length: 0` / no body
- Observed: all three 400 `{"error":"Expected { locationId }"}`; `investigation.minutesUsed:0` afterward.
- Verdict reason: Exact match.

## TC-11 — Travel to an unknown place is 404: PASS
- Command / action run: `POST /reset`, `POST /travel {"locationId":"L99"}`, `{"locationId":""}`, `{"locationId":"l01"}`
- Observed: all three 404 `{"error":"There is no such place in this case"}`; `investigation.locationId:null` afterward, no time spent.
- Verdict reason: Exact match (case-sensitivity of IDs confirmed: `l01` lower-case is unknown too).

## TC-13 — Unknown case id is 404 on every endpoint: PASS
- Command / action run: `GET /api/cases/999/places`; `POST /api/cases/999/travel {"locationId":"L01"}`; `GET /api/cases/999/places/L01`; `POST /api/cases/999/places/L01/search {"spotId":"L01-locker"}`
- Observed: all four 404 `{"error":"Case not found"}`.
- Verdict reason: Exact match.

## TC-14 — GET /places/:id for a place you are not in is 422: PASS
- Command / action run: `POST /reset`, `GET /places/L01`
- Observed: 422 `{"error":"You aren't there. Go there first."}`; `investigation.minutesUsed:0` afterward.
- Verdict reason: Exact match.

## TC-15 — GET /places/:id for a different place than the current one is 422: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `GET /places/L02`, `GET /places/L01`
- Observed: `GET /places/L02` → 422 `{"error":"You aren't there. Go there first."}`; `GET /places/L01` → 200.
- Verdict reason: Exact match.

## TC-16 — GET /places/:id for the current place returns arrival, people, spots: PASS
- Command / action run: (continuing from TC-15 state) `GET /places/L01`, `GET /investigation`
- Observed: 200 with `id:"L01"`, `name:"Main Laboratory"`, `floor:"2nd Floor"`, `arrival` exactly "Oscilloscopes hum on every bench. Dr. Voss looks up from her notes; across the room, Alex Reyes is already on his feet to greet you.", `people` ids `["S01","S02"]` each with `id,name,role,alias`, `spots` exactly `L01-locker, L01-desk, L01-bench` each with `id,label,searched:false` and no `text/consequences/turnedUp/evidenceId` keys. `investigation.minutesUsed` unchanged at 30.
- Verdict reason: Exact match.

## TC-17 — GET /places/:id with an unknown id is 404: PASS
- Command / action run: `POST /reset`, `GET /places/L99`
- Observed: 404 `{"error":"There is no such place in this case"}`.
- Verdict reason: Exact match.

## TC-18 — Place with nobody in it returns an empty people list: PASS
- Command / action run: `POST /reset`, `POST /travel L03`, `GET /places/L03`, `GET /places`
- Observed: `GET /places/L03` → 200, `people:[]`, non-empty `arrival` ("A windowless room, a single shelf, and a foam-lined case with nothing in it."). `GET /places` shows `L03.people:[]` and `L05.people:[]`.
- Verdict reason: Exact match.

## TC-19 — First search costs 15 minutes and reveals text and unlocked evidence: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `GET /evidence` (confirmed `[]`), `POST /places/L01/search {"spotId":"L01-locker"}`
- Observed: 200, top-level keys `spot,effects,place,investigation`; `spot.id:"L01-locker"`, `spot.label:"Search Reyes's locker"`, `spot.text` exactly as specified, `spot.turnedUp:[{"id":"E015","title":"Loan Default Notice"}]`; `effects.unlockedEvidence` same; `place.spots` shows `L01-locker.searched:true`, others false; `investigation.clock.minutesUsed:45`, `clock.now:"1984-03-10T09:00:00"`, `spotsSearched:["L01-locker"]`, `unlockedEvidence` contains `"E015"`.
- Verdict reason: Exact match.

## TC-20 — Searching the same spot again costs 0 and unlocks nothing new: PASS
- Command / action run: (continuing from TC-19 state) `POST /places/L01/search {"spotId":"L01-locker"}` x2, `GET /investigation`
- Observed: both calls 200 with `effects.unlockedEvidence:[]`, `spot.turnedUp` still lists E015 (per source note, not a defect), `investigation.clock.minutesUsed` stays 45, `spotsSearched:["L01-locker"]` no duplicate, `unlockedEvidence:["E015"]` unchanged.
- Verdict reason: Exact match, including the documented non-defect about `turnedUp` on repeats.

## TC-21 — Searching a spot with no evidence still costs 15 and returns empty effects: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `POST /places/L01/search {"spotId":"L01-bench"}`
- Observed: 200, `spot.text` exactly "Neat as a pin. The gyro rig has been switched off since Friday. Nothing here but good housekeeping.", `spot.turnedUp:[]`, `effects.unlockedEvidence:[]`, `investigation.clock.minutesUsed:45`, `place.spots` L01-bench `searched:true`, `unlockedEvidence:[]`.
- Verdict reason: Exact match.

## TC-22 — Searching two different spots accumulates cost per spot: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, search `L01-locker`, then `L01-desk`, then `L01-locker` again (checking `/investigation` after each)
- Observed: `minutesUsed` after each: 45, 60, 60. `effects.unlockedEvidence` ids: `["E015"]`, `["E017"]`, `[]`. Final `unlockedEvidence` contains both `E015` and `E017`; `spotsSearched:["L01-locker","L01-desk"]`.
- Verdict reason: Exact match.

## TC-23 — Search from a place you are not at is 422: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `POST /places/L02/search {"spotId":"L02-recorder"}`
- Observed: 422 `{"error":"You aren't there. Go there first."}`; `minutesUsed` stays 30; `GET /evidence` is `[]` (no E005).
- Verdict reason: Exact match.

## TC-24 — Search before travelling anywhere is 422: PASS
- Command / action run: `POST /reset`, `POST /places/L01/search {"spotId":"L01-locker"}`
- Observed: 422 `{"error":"You aren't there. Go there first."}`; `investigation.minutesUsed:0` after.
- Verdict reason: Exact match.

## TC-25 — Search with missing spotId is 400: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `POST /places/L01/search {}`, `{"spotId":5}`, `{"spotId":null}`
- Observed: all three 400 `{"error":"Expected { spotId }"}`; `minutesUsed` stays 30.
- Verdict reason: Exact match.

## TC-26 — Search with a spot id that is not in this place is 404: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `POST /places/L01/search {"spotId":"L01-nope"}`, `{"spotId":"L02-recorder"}`
- Observed: both 404 `{"error":"There is nothing like that to search here"}`; `minutesUsed` stays 30.
- Verdict reason: Exact match.

## TC-27 — Validation order on search: 400, then unknown place 404, then not-there 422: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, (a) `POST /places/L99/search {}`, (b) `POST /places/L99/search {"spotId":"x"}`, (c) `POST /places/L02/search {"spotId":"nonexistent"}`
- Observed: (a) 400 `{"error":"Expected { spotId }"}`. (b) 404 `{"error":"There is no such place in this case"}`. (c) 422 `{"error":"You aren't there. Go there first."}`.
- Verdict reason: Exact match; confirms validation order.

## TC-28 — Evidence found by search appears in GET /evidence and investigation: PASS
- Command / action run: `POST /reset`, pre-checks `GET /evidence` (`[]`), `GET /evidence/E015` (404); then `POST /travel L01`, `POST /places/L01/search L01-locker`, `GET /evidence`, `GET /evidence/E015`, `GET /evidence/E017`, `GET /investigation`
- Observed: `GET /evidence` → array with exactly one item `id:"E015"`. `GET /evidence/E015` → 200. `GET /evidence/E017` → 404 `{"error":"Evidence not found"}`. `investigation.unlockedEvidence:["E015"]`.
- Verdict reason: Exact match.

## TC-29 — Evidence in a spot you have not searched stays out of the file: PASS
- Command / action run: `POST /reset`, `POST /travel L02`, search only `L02-recorder`, then `GET /evidence`, `GET /evidence/E010`, `POST /viewed {"type":"evidence","id":"E010"}`, then search `L02-modem`, re-check `GET /evidence` / `GET /evidence/E010`
- Observed: `GET /evidence` contains E005 only. `GET /evidence/E010` → 404. `POST /viewed` for E010 → 404 `{"error":"Nothing with that id in this case"}`. After searching L02-modem: `GET /evidence` contains both E005, E010; `GET /evidence/E010` → 200.
- Verdict reason: Exact match.

## TC-30 — Gated spot L03-reader is hidden until file page F2 is read: PASS
- Command / action run: `POST /reset`, `POST /travel L03`; (a) `GET /places/L03`; (b) `POST /places/L03/search {"spotId":"L03-reader"}`; (c) `POST /file/F2/read`; (d) `GET /places/L03`; (e) `POST /places/L03/search {"spotId":"L03-reader"}`; `GET /evidence/E014`, `GET /evidence`
- Observed: (a) spots are exactly `L03-e013, L03-e002, L03-e001, L03-case`, no `L03-reader`. (b) 404 `{"error":"There is nothing like that to search here"}`, minutesUsed unchanged at 30. (c) 200, `storyFlags.file.F2:true`. (d) spots now include `L03-reader, searched:false`. (e) 200, `effects.unlockedEvidence:[{"id":"E014","title":"Keycard Access"}]` matching `GET /evidence/E014`'s title; E014 present in `GET /evidence`.
- Verdict reason: Exact match to the documented (ambiguity-noted) behaviour.

## TC-31 — Time cost sanity across a mixed sequence: PASS
- Command / action run: `POST /reset`; travel L01; travel L01; search L01-locker; search L01-locker; travel L02; search L02-recorder; search L02-modem; travel L02 — checking `minutesUsed` after each
- Observed: minutesUsed sequence: 30, 30, 45, 45, 75, 90, 105, 105. Final `clock.minutesLeft:855`, `clock.now:"1984-03-10T10:00:00"`.
- Verdict reason: Exact match.

## TC-32 — Time up blocks travel to a new place with 422: PASS
- Command / action run: `POST /reset`; 32 alternating `POST /travel` calls L01/L02 starting L01 (checked `minutesUsed` climbs 30→960 exactly); `GET /investigation`; then call 33 `POST /travel {"locationId":"L01"}`
- Observed: after call 32: `clock.minutesUsed:960, minutesLeft:0, timeUp:true, now:"1984-03-11T00:15:00"`. Call 33 → 422 `{"error":"Time is up. The District Attorney wants a name."}`; `investigation` afterward still `locationId:"L02", minutesUsed:960`.
- Verdict reason: Exact match.

## TC-33 — Time up blocks searching a not-yet-searched spot with 422: PASS
- Command / action run: (continuing from TC-32 end state, standing in L02, timeUp true, no spots searched) `POST /places/L02/search {"spotId":"L02-recorder"}`
- Observed: 422 `{"error":"Time is up. The District Attorney wants a name."}`. `GET /evidence` → `[]` (no E005); `spotsSearched:[]`; `minutesUsed:960`.
- Verdict reason: Exact match.

## TC-34 — Time-up behaviour for zero-cost actions (ambiguity to record): PASS
- Command / action run: `POST /reset`; travel L01; search L01-locker; search L01-desk; then 30 more alternating travels L02/L01/L02/... (31st travel overall lands on L01); confirmed `minutesUsed:960, timeUp:true`. Then (a) `POST /travel L01` (current place); (b) `POST /places/L01/search L01-locker` (already searched); (c) `POST /places/L01/search L01-bench` (not searched); (d) `POST /travel L02`.
- Observed: setup reached exactly `minutesUsed:960, timeUp:true, locationId:"L01"` as prescribed. (a) 200, `minutesUsed` unchanged at 960. (b) 200, `effects.unlockedEvidence:[]`, `minutesUsed` unchanged at 960. (c) 422 `{"error":"Time is up. The District Attorney wants a name."}`. (d) 422 same message.
- Verdict reason: Matches the case's own documented expectation that (a) and (b) return 200 (source only spends time when time is actually spent) while (c) and (d) are 422 — recorded as designed, not a failure.

## TC-35 — Reset clears location, visited places, searched spots and found evidence: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `POST /places/L01/search L01-locker` (minutesUsed 45, E015 unlocked); then `POST /reset`, `GET /places`, `GET /investigation`, `GET /evidence`, `GET /places/L01`; then travel L01 + search L01-locker again
- Observed: reset → 200 with full investigation state. Afterward: all 6 places `visited:false, here:false`; `locationId:null`; `placesVisited:[]`; `spotsSearched:[]`; `unlockedEvidence:[]`; `GET /evidence:[]`; `minutesUsed:0`; `GET /places/L01` → 422 "You aren't there. Go there first.". Re-travelling and re-searching L01-locker again costs 15 and returns `effects.unlockedEvidence` = E015 again.
- Verdict reason: Exact match.

## TC-36 — Progress is per case (travel in one case does not affect another): PASS
- Command / action run: `POST /api/cases/047/reset`, `POST /api/cases/048/reset`; took `X = "L01"` (first id from `GET /api/cases/048/places`); `POST /api/cases/048/travel {"locationId":"L01"}`; then `GET /api/cases/047/places`, `GET /api/cases/047/investigation`, `GET /api/cases/048/places`, `GET /api/cases/048/investigation`
- Observed: 047 unchanged — all `visited:false, here:false`, `locationId:null`, `minutesUsed:0`. 048: `L01` (The Stage & Prompt Corner) `here:true, visited:true`, `minutesUsed:30` (048's `clock.costs.travel` is 30).
- Verdict reason: Exact match.

## TC-37 — Every case has six places with the same list shape: PASS
- Command / action run: `POST /reset` for 047, 048, 049, 050, 051; then `GET /places` for each
- Observed: each of the 5 cases returned 200 with exactly 6 places, correct key shape (`id,name,floor,description,map{x,y,kind},people,visited,here`), all `visited`/`here` false. Spot-checked `people[].id`→name resolution against `GET /suspects/:id`: 047 (Dr. Maren Voss etc., confirmed in TC-02), 048 (S03 "Harriet Pell" matches list), 049 (`S01` → "Graham Pike" matches "Graham Pike" in places list), 050 (`S01` → "Oliver Hale" matches), 051 (`S01` → "Constance Ashcombe" matches).
- Verdict reason: Shape and name-resolution match across all five cases.

## TC-38 — Map screen shows six labelled places matching the API (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-39 — Selecting a pin shows the API's description and people (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-40 — Clicking GO THERE travels with a scene transition and the HUD clock advances 30 min (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-41 — Walking back into the current place is free (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-42 — Place page shows arrival, people and spots from the API (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-43 — Searching a spot in the browser shows the result, a new-evidence notice, and advances the clock 15 min (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-44 — Searching an already searched spot in the UI costs no time (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-45 — Found evidence is visible in the Evidence Room (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-46 — Loading a place URL directly without travelling shows a sensible state (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-47 — GO THERE on the away screen travels and shows the place (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-48 — BACK TO THE MAP on the away screen (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-49 — Place page for an unknown place id does not crash (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-50 — Refreshing the place page after travelling keeps the state (UI): BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

## TC-51 — Time up disables travel controls in the UI: BLOCKED
- Command / action run: not run — no browser automation tool available in this session
- Observed: n/a
- Verdict reason: no browser automation tool available in this run.

---

## Additional cases (added 2026-09-24)

## TC-52 — A closed case blocks travel to a new place with 422 "This case is closed.": PASS
- Command / action run: `POST /reset`, `POST /travel L01` (minutesUsed 30), `POST /conclusion {"suspectId":null,"evidenceIds":[]}` (200, `ending:"criminal_escapes"`); then `POST /travel {"locationId":"L02"}`
- Observed: 422, body exactly `{"error":"This case is closed."}`. `GET /investigation` afterward: `locationId:"L01"`, `placesVisited:["L01"]`, `minutesUsed:30` — all unchanged.
- Verdict reason: Exact match.

## TC-53 — A closed case blocks travel even back to the current place (no longer free): PASS
- Command / action run: (continuing from TC-52 state, standing in L01, case closed) `POST /travel {"locationId":"L01"}`
- Observed: 422 `{"error":"This case is closed."}`, not 200. `minutesUsed` unchanged at 30.
- Verdict reason: Exact match — confirms `assertOpen()` runs before the same-location short-circuit.

## TC-54 — A closed case blocks searching a new spot with 422 "This case is closed.": PASS
- Command / action run: `POST /reset`, `POST /travel L01` (minutesUsed 30, no spots searched), `POST /conclusion {"suspectId":null,"evidenceIds":[]}` (200); then `POST /places/L01/search {"spotId":"L01-bench"}`
- Observed: 422, body exactly `{"error":"This case is closed."}`. `GET /evidence` afterward is `[]`; `investigation.spotsSearched:[]`; `minutesUsed:30`.
- Verdict reason: Exact match.

## TC-55 — A closed case blocks re-searching an already-searched spot too: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `POST /places/L01/search {"spotId":"L01-locker"}` (200, E015 unlocked, minutesUsed 45), `POST /conclusion {"suspectId":null,"evidenceIds":[]}` (200); then `POST /places/L01/search {"spotId":"L01-locker"}` again
- Observed: 422 `{"error":"This case is closed."}`, not a 200 with empty effects. `investigation.unlockedEvidence` still only `["E015"]`; `minutesUsed:45`.
- Verdict reason: Exact match — confirms `assertOpen()` runs before the already-searched short-circuit.

## TC-56 — GET /places and GET /places/:locationId still work read-only after the case is closed: PASS
- Command / action run: (continuing from TC-55 state, closed case, L01 visited and locker searched) `GET /places`, `GET /places/L01`
- Observed: both 200 (not 422). `GET /places` shows L01 `visited:true, here:true`, unchanged. `GET /places/L01` still returns `arrival`, `people`, and `spots` with `L01-locker.searched:true`. Neither call changed `minutesUsed` (still 45 per the investigation body embedded in TC-55's own re-check).
- Verdict reason: Exact match — confirms only state-changing calls are blocked by the closed-case rule.

## TC-57 — Closed-case validation order on travel: an unknown place is still 404, not the closed-case 422: PASS
- Command / action run: `POST /reset`, `POST /travel L01`, `POST /conclusion {"suspectId":null,"evidenceIds":[]}` (200); then `POST /travel {"locationId":"L99"}`
- Observed: 404, body `{"error":"There is no such place in this case"}` — not the closed-case message.
- Verdict reason: Exact match — confirms `placeOr404()` runs before `assertOpen()` in `travel()`.

## TC-58 — Closed-case validation order on search: malformed body (400) and unknown place (404) still take precedence: PASS
- Command / action run: (continuing from TC-57 state, closed case, standing in L01) (a) `POST /places/L01/search {}`; (b) `POST /places/L99/search {"spotId":"x"}`
- Observed: (a) 400, body `{"error":"Expected { spotId }"}` — not the closed-case message. (b) 404, body `{"error":"There is no such place in this case"}` — not the closed-case message.
- Verdict reason: Exact match.

## TC-59 — Closed-case validation order on search: the closed-case check pre-empts the "not there" check: PASS
- Command / action run: `POST /reset` (locationId null, never travelled), `POST /conclusion {"suspectId":null,"evidenceIds":[]}` (200); then `POST /places/L01/search {"spotId":"L01-locker"}`
- Observed: 422, body `{"error":"This case is closed."}` — not `{"error":"You aren't there. Go there first."}`.
- Verdict reason: Exact match — confirms `assertOpen()` runs before the current-location comparison in `search()`.

## TC-60 — The clock clamps to the exact budget instead of overshooting when the next action costs more than the time left: PASS
- Command / action run: `POST /reset`; 31 alternating `POST /travel` calls L01/L02/L01/... starting L01, ending on L01 (confirmed `minutesUsed:930, timeUp:false` after call 31); `POST /places/L01/search {"spotId":"L01-locker"}` (confirmed 200, `minutesUsed:945, minutesLeft:15, timeUp:false`); then `POST /travel {"locationId":"L02"}` (a normal 30-min travel with only 15 min left); then a further `POST /travel {"locationId":"L01"}`
- Observed: setup matched the prescribed state exactly. The 30-minute travel with 15 minutes left returned 200 (not 422): `investigation.locationId:"L02"`, `clock.minutesUsed:960` exactly (not 975 — clamped), `clock.minutesLeft:0`, `clock.timeUp:true`. The further travel afterward returned 422 `{"error":"Time is up. The District Attorney wants a name."}`.
- Verdict reason: Exact match — confirms the `Math.min(TIME_COST[action], minutesLeft)` clamp from commit b45dd98.

## TC-61 — GET /cases/:caseId location entries do not leak people, arrival or spots: PASS
- Command / action run: `POST /reset`, `GET /api/cases/047`
- Observed: 200; `locations` array of 6 objects, each with exactly the keys `id, name, floor, district, description, map` — no `people`, `arrival`, `spots`, `visited`, or `here` present on any entry.
- Verdict reason: Exact match — confirms the case resource never leaks place detail that only `GET /places` / `GET /places/:id` should carry.

---

## Summary
Total: 60 test cases run (61 numbered in the source file, but TC-12 does not exist in `test-cases.md` — the numbering skips from TC-11 to TC-13, so there is no case 12 to execute).

- Original section (TC-01–TC-51, minus the absent TC-12): 50 cases — 36 PASS, 0 FAIL, 14 BLOCKED (all UI cases TC-38–TC-51, blocked for lack of a browser automation tool in this session).
- Added section (TC-52–TC-61, 2026-09-24): 10 cases — 10 PASS, 0 FAIL, 0 BLOCKED.
- Grand total: 46 PASS, 0 FAIL, 14 BLOCKED.

Failures needing attention: none. Every executable API case passed, including all ten of the closed-case / clock-clamp cases added to cover commit b45dd98's behaviour. No case's expectation was found to be outdated against current source behaviour (override rule 4 did not need to be invoked). The fourteen UI cases (TC-38–TC-51) remain unverified in this run and should be exercised with a browser automation tool in a follow-up session.
